---
phase: "02.1"
slug: "posthog-consent-gate-hotfix"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: false
wave_0_complete: true
created: "2026-09-14"
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `02.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.10, `environment: "node"` (no RTL / jsdom) |
| **Config file** | `vitest.config.ts` — `include: ["lib/**/*.test.ts", "app/**/*.test.ts"]` |
| **Quick run command** | `export PATH="$HOME/.local/node20/bin:$PATH"; npx vitest run lib/consent.test.ts lib/geo.test.ts app/api/geo/route.test.ts` |
| **Full suite command** | `export PATH="$HOME/.local/node20/bin:$PATH"; npx vitest run && npx tsc --noEmit` |
| **Estimated runtime** | ~2 s (vitest, 14 files / 137 tests at baseline) + ~60 s (tsc) |

Never run bare `npx vitest` (watch mode never exits). `npm run build` is Vercel-only.

---

## Sampling Rate

- **After every task commit:** Run the quick run command
- **After every plan wave:** Run the full suite command (+ `npm run lint` — total must not exceed the `main` baseline)
- **Before `/gsd-verify-work`:** Full suite green **and** D-10 preview netlog recorded **and** D-08 live-traffic HogQL evidence pasted into `docs/OPS-01-SHIP-GATE.md`
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.1-01-01 | 01 | 1 | MONY-01 (D-03) | T-02.1-01 | Unknown/missing country ⇒ consent region (fail closed) | unit | `npx vitest run lib/geo.test.ts` | ✅ | ✅ green |
| 02.1-01-02 | 01 | 1 | MONY-01 (D-02) | T-02.1-02 | `/api/geo` reads only `x-vercel-ip-country`, returns `{ isConsentRegion }`, `Cache-Control: private, no-store`, no cookie | unit | `npx vitest run app/api/geo/route.test.ts` | ✅ | ✅ green |
| 02.1-01-03 | 01 | 1 | MONY-01 / OPS-01 (D-04, D-05, D-06, D-07) | T-02.1-03 | Consent-region path unchanged; non-consent-region opts in without `__tcfapi`; `consent_gate_path` on every event; never `startReplay` without `optIn` | unit | `npx vitest run lib/consent.test.ts` | ✅ | ✅ green |
| 02.1-01-04 | 01 | 1 | OPS-01 (SC1, D-04 ordering, D-10) | — | First `$pageview` of a fresh non-consent-region visitor reaches `/ingest/e/*` on the preview deploy | tracer (manual) | bounded headless-Chrome netlog (see Manual-Only) | n/a | ✅ green — 02.1-03 netlog: 3 capture requests vs prod baseline 0 (gate doc Part 4 "The D-10 netlog proof") |
| 02.1-02-01 | 02 | 1 | OPS-01 (D-08, D-09) | T-02.1-07/08 | Gate doc requires post-deploy live-traffic evidence; addenda present and append-only | source assertion | `grep -c "live-traffic" docs/OPS-01-SHIP-GATE.md` ≥ 1 (=9); `grep -c "Addendum" 01-/02-VERIFICATION.md` (=1 each); `git diff main --numstat` shows 0 removed lines in both VERIFICATION files | ✅ | ✅ green |
| 02.1-02-02 | 02 | 1 | OPS-01 (SC3, SC4) | — | ≥ 20 `$pageview` from ≥ 2 non-consent-region countries within 60 min of prod deploy; ≥ 50 % of Vercel Web Analytics page views | manual (HogQL, project 337485) | see Manual-Only | n/a | ✅ green — 02.1-04 window (25 pageviews / 3 countries) + 02.1-05 ratio 25/23; signed 2026-09-15 |
| 02.1-03-01..03 | 03 | 2 | OPS-01 / MONY-01 | T-02.1-12..16 | Preview deploy via personal Vercel profile; netlog proof; evidence recorded, bypass secret redacted | manual + source assertion | `grep -c "<redacted>" docs/OPS-01-SHIP-GATE.md` ≥ 1; secret-pattern scan returns 0 | ✅ | ✅ green |
| 02.1-04-01..03 | 04 | 3 | OPS-01 / MONY-01 | T-02.1-17..22 | Human-approved prod deploy; live-traffic gate run; honest unsigned status when threshold 3 unreadable | manual + source assertion | gate doc Part 4 "Production deploy (2026-09-14, 02.1-04)" and "Phase 2.1 Sign-off — NOT SIGNED" present | ✅ | ✅ green |
| 02.1-05-01..02 | 05 | GC1 | OPS-01 | T-02.1-23..27 | Vercel Web Analytics figure read by developer; threshold 3 scored; gate signed with date; append-only | source assertion | `grep -c "Sign-off — SIGNED (2026-09-15" docs/OPS-01-SHIP-GATE.md` = 1; NOT SIGNED heading still present | ✅ | ✅ green |
| 02.1-06-01..03 | 06 | GC2 | OPS-01 (SC3 automatable half) | T-02.1-28..31 | `theme_changed`, `timeline_viewed`, `timeline_filter_used` counted-observed with `consent_gate_path=geo-non-consent-region` | manual (HogQL) + source assertion | gate doc Part 4 "Targeted custom-event observation (gap closure 02.1-06)" carries six status lines | ✅ | ✅ green |
| 02.1-07-01..03 | 07 | GC3 | OPS-01 | T-02.1-32..36 | Developer-approved redeploy of five review fixes; fresh 60-min window measured; FAIL/NOT EVALUABLE recorded, not softened | source assertion | commits 90f5a46 0e03d9b 7a7cf0c 9657535 9da21c4 are ancestors of deployed dd19b0b; "Re-deploy gate run (gap closure 02.1-07)" present | ✅ | ✅ green (gate row for dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5 honestly UNSIGNED) |
| 02.1-08-01..02 | 08 | GC4 | OPS-01 / MONY-01 (SC2, Truth 4) | T-02.1-37..41 | EEA/UK/CH TCF session (reject / accept / re-confirm) observed in a real browser, or recorded as not-performed | manual (human UAT) | see Manual-Only | n/a | ✅ green — performed by the developer and passed as 02.1-UAT.md Test 1 (2026-09-15) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Wave "GC*n*" = gap-closure plan added 2026-09-14 from `02.1-VERIFICATION.md`.*

---

## Wave 0 Requirements

- [x] `lib/geo.test.ts` — D-03 (`CONSENT_REGIONS` exact set; unknown/missing ⇒ consent region)
- [x] `app/api/geo/route.test.ts` — D-02 (handler imported directly, called with a constructed `NextRequest` carrying `x-vercel-ip-country`)
- [x] No framework install needed — existing Vitest node-env covers both shapes

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| First `$pageview` of a fresh non-consent-region visitor is sent | OPS-01 SC1 / D-10 | React effect ordering + real SDK + real Vercel geo header; no browser harness in the project | Fresh Chrome profile, bounded run: `perl -e 'alarm 45; exec @ARGV' "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --no-first-run --user-data-dir=<fresh dir> --virtual-time-budget=12000 --log-net-log=<file> --dump-dom <preview-url>`; then `grep -oE '"url":"[^"]*/ingest/(e|i/v0/e)/[^"]*"' <file>` must return ≥ 1 line. Record verbatim in `docs/OPS-01-SHIP-GATE.md`. |
| EEA/UK behaviour unchanged (no capture before dialog resolves; cookieless on reject; replay only on full opt-in) | MONY-01 SC2 | Requires an EEA egress IP and the live Google CMP dialog | Load prod via an EEA IP/VPN with a fresh profile: no `/ingest/e/*` request before interacting with the dialog; after **Reject**, requests carry `$cookieless_mode: true` and no `ph_*` cookie is set; after **Accept**, `consent_resolved {accepted:true}` and a `$snapshot` (replay) request appear. If no EEA egress is available, record `human_needed`, never a pass. |
| Live traffic proves the fix in production | OPS-01 SC3 / SC4 / D-08 | Needs real visitors after the prod deploy | Within 60 min of prod deploy run in PostHog project 337485: `SELECT properties.$geoip_country_code AS country, count() AS pageviews FROM events WHERE event = '$pageview' AND timestamp >= now() - INTERVAL 60 MINUTE GROUP BY country ORDER BY pageviews DESC` — require ≥ 20 total from ≥ 2 non-consent-region countries; and `SELECT properties.consent_gate_path, count() FROM events WHERE timestamp >= now() - INTERVAL 60 MINUTE GROUP BY 1`. Compare total `$pageview` to Vercel Web Analytics page views for the same window (≥ 50 %). Paste both results into the gate doc. |

---

## Manual-Only Outcomes (2026-09-15)

| Behavior | Outcome | Evidence |
|----------|---------|----------|
| First `$pageview` of a fresh non-consent-region visitor is sent | ✅ observed | 02.1-03 preview netlog (3 capture requests; HeadlessChrome bot-filter trap found and worked around) — gate doc Part 4 |
| EEA/UK behaviour unchanged | ✅ observed by developer | `02.1-UAT.md` Test 1 passed 2026-09-15 (reject: no capture/replay/cookie; accept: capture + replay + `consent_resolved`; re-confirm: `consent_resolved` once — WR-03 dedupe live in dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5). 02.1-08 had recorded this as not-performed; the UAT session supersedes that record. |
| Live traffic proves the fix in production | ✅ observed | 02.1-04 window 25 `$pageview` / 3 non-consent-region countries; 02.1-05 ratio 25/23 → gate signed 2026-09-15. The later 02.1-07 redeploy's own row remains honestly UNSIGNED (low-traffic window) and is a carried-forward operational item, not a validation gap. |

---

## Validation Audit 2026-09-15

| Metric | Count |
|--------|-------|
| Requirements mapped | 12 task groups (8 plans) |
| Automated & green | 5 (unit ×3, source assertion ×2) + 5 source-assertion groups for ops/doc plans |
| Manual-only, observed | 3 |
| Gaps found | 0 |
| Resolved | 0 (nothing to generate — all automatable rows already covered) |
| Escalated | 0 |

Full suite at audit time: `npx vitest run` → 16 files / 162 tests passed; `npx tsc --noEmit` → clean.
`nyquist_compliant` stays `false` by definition: three behaviours are manual-only (real geo egress, real CMP, real visitors) and cannot be automated in this project's node-only Vitest harness (D-06). Status is therefore **validated (partial)**.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (manual-only rows documented with observed outcomes)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter — not applicable: 3 manual-only behaviours remain by design

**Approval:** validated (partial) 2026-09-15 — verify-work verify:post Nyquist step
