---
phase: 02.1-posthog-consent-gate-hotfix
plan: 06
subsystem: ops
tags: [ops-01, ship-gate, posthog, custom-events, consent-gate, roadmap-sc3]

requires:
  - phase: 02.1-05
    provides: OPS-01 gate SIGNED, production deployment dpl_HY5319wSDVw3M4ibBU42JrSTgw4e confirmed as the build in force
provides:
  - Counted observation of theme_changed (2), timeline_viewed (1) and timeline_filter_used (2), each carrying consent_gate_path = geo-non-consent-region, in a named follow-up window (2026-09-15T18:26:27Z-18:28:57Z)
  - timeline_error recorded not triggered by design (optional error-inducing step was not authorized this session)
  - consent_resolved / consent_unavailable recorded structurally unobservable from a non-consent-region egress, explicitly handed off to gap plan 02.1-08
  - A dated Part 4 section in docs/OPS-01-SHIP-GATE.md carrying the interaction log, both query forms, the result table and a per-event status line for all six named events
affects: [02.1-08, phase-03]

actuals:
  tokens: 3800
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Targeted manual test in place of a live-traffic window: drive the exact interactions that fire an event, from the exact geo path being verified, then query the exact named window — the same tracer-slice-gap-closure pattern 02.1-05 established, applied to custom events instead of a Vercel figure."

key-files:
  created: []
  modified:
    - docs/OPS-01-SHIP-GATE.md

key-decisions:
  - "Route: automation-performed. The developer replied \"you drive it\" in-session, authorizing browser automation scoped to parseforge.gg only. The Claude-in-Chrome extension was not connected, so the orchestrator itself drove production with a fresh-profile headless Chrome instance over the DevTools Protocol, with the User-Agent overridden to a standard desktop Chrome string (reusing the 02.1-03 bot-filter fix) and real pointer events for every click. No page other than parseforge.gg was navigated."
  - "Optional step 5 (the fight=999999&source=999999 error-inducing load) was not performed — the developer did not explicitly agree to it in-session — so timeline_error is recorded as \"not triggered by design\", not as absent-and-fine."
  - "Only the explicit-bounds query form was actually executed. Part 1 item 7's relative now() - INTERVAL 60 MINUTE query form is copied verbatim for comparison but was not re-run, because the window this section observes was already in the past by the time the query ran — a relative form at that point would answer a different (empty) window. This is stated plainly in the gate document rather than implied."
  - "consent_resolved and consent_unavailable are recorded as structurally unobservable from this egress, not as failures or as not-yet-observed — the geo-non-consent-region path never starts the __tcfapi listener (D-04), so there is no consent event a US visitor could ever produce. Both are explicitly hand-off to gap plan 02.1-08 (EEA/UK session), matching this plan's own must_haves truth."
  - "The PostHog connector's active project was confirmed as \"ParseForge\" (id 337485, org LootList+, timezone UTC) via switch-project 337485 before any number was read, and that confirmation is recorded in the gate document without pasting any token — the connector's default project is a different application (id 310668)."

patterns-established:
  - "When a query result includes a taxonomy warning (\"Event 'X' was not found in this project taxonomy\"), that is itself evidence the event has never ingested in this project — worth recording alongside a zero count rather than treating the zero as ambiguous."

requirements-completed: [OPS-01]

coverage:
  - id: D1
    description: "theme_changed, timeline_viewed and timeline_filter_used each observed in PostHog project 337485 with count >= 1 and consent_gate_path = geo-non-consent-region, in a window whose UTC start and end are recorded in docs/OPS-01-SHIP-GATE.md"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "PostHog execute-sql query (explicit-bounds form, six named events, grouped by event + consent_gate_path) — result table pasted verbatim in docs/OPS-01-SHIP-GATE.md Part 4"
        status: pass
    human_judgment: false
  - id: D2
    description: "The recorded evidence names the observed deployment (dpl_HY5319wSDVw3M4ibBU42JrSTgw4e, built from commit 8693498) and states plainly this is the targeted follow-up test 02.1-VERIFICATION.md accepts, not the original 2026-09-14 60-minute window"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "grep -qF 'dpl_HY5319wSDVw3M4ibBU42JrSTgw4e' against the new Part 4 section (Task 3 verify 1)"
        status: pass
    human_judgment: false
  - id: D3
    description: "consent_resolved and consent_unavailable recorded as structurally unobservable from this egress (not absent-and-fine), with the __tcfapi-listener reason stated and the explicit hand-off to 02.1-08"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "docs/OPS-01-SHIP-GATE.md Part 4, '### Targeted custom-event observation' section, per-event status lines for consent_resolved / consent_unavailable"
        status: pass
    human_judgment: false
  - id: D4
    description: "Both query forms recorded (Part 1 item 7's verbatim relative form, and the explicit-bounds form actually executed) with the interpreting timezone (UTC) stated"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "docs/OPS-01-SHIP-GATE.md Part 4 new section, 'Both query forms' subsection"
        status: pass
    human_judgment: false
  - id: D5
    description: "All six named events carry exactly one status line each (observed / not triggered by design / structurally unobservable from this egress), none left without a line"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "Task 3 verify 1 — grep for '^- Event \\`EVENT\\`' across all six event names, event-lines=6"
        status: pass
    human_judgment: false
  - id: D6
    description: "The gate document was edited additive-only (zero removed lines) and no added line matches a credential, network-address or per-person identifier pattern"
    requirement: OPS-01
    verification:
      - kind: other
        ref: "Task 3 verify 2 (removed=0 added=92) and verify 3 (added-lines-clean-ok) against git diff HEAD -U0 -- docs/OPS-01-SHIP-GATE.md"
        status: pass
    human_judgment: false

duration: 20min (continuation session; interaction-driving and PostHog querying were performed by the orchestrator before this continuation started)
completed: 2026-09-15
status: complete
---

# Phase 02.1 Plan 06: Targeted Custom-Event Observation Summary

**Drove the four production interactions from a non-consent-region egress and counted three of six named custom events landing in PostHog with `consent_gate_path = geo-non-consent-region` attached, closing the automatable half of the Truth 5 gap.**

## Performance

- **Duration:** ~20 min for this continuation (checkpoint answer, verification, PostHog query authoring, and gate-document write were split across the original executor, the orchestrator, and this continuation agent — see Deviations)
- **Started:** 2026-09-15T18:39:03Z (this continuation)
- **Completed:** 2026-09-15T18:46:47Z
- **Tasks:** 3 (checkpoint + 2 auto)
- **Files modified:** 1 (`docs/OPS-01-SHIP-GATE.md`)

## Accomplishments

- `theme_changed` (2), `timeline_viewed` (1), and `timeline_filter_used` (2) each counted-observed in PostHog project 337485 with `consent_gate_path = geo-non-consent-region`, in the named window `2026-09-15T18:26:27Z`–`2026-09-15T18:28:57Z`.
- `timeline_error` recorded not triggered by design — the optional, one-attempt-only error-inducing step was not authorized this session.
- `consent_resolved` and `consent_unavailable` recorded structurally unobservable from a non-consent-region egress and explicitly handed off to gap plan 02.1-08, rather than left as an unexplained zero.
- `docs/OPS-01-SHIP-GATE.md` Part 4 gained a dated, additive-only section (`### Targeted custom-event observation (gap closure 02.1-06, 2026-09-15)`) carrying the interaction log, both query forms, the result table, and a per-event status line for all six named events.

## Task Commits

Task 1 (checkpoint) recorded no repo change of its own — the route decision is captured in this SUMMARY per its `<done>` criterion. Task 2 (perform the interactions) wrote no repo file — the interaction record lives outside the repo at `${TMPDIR:-/tmp}/pf-0206-window.txt`, per the plan's own `<files>` note. Only Task 3 produced a commit:

1. **Task 3: Query PostHog for the window and record what actually ingested** — `ba9b686` (docs)

**Plan metadata:** this SUMMARY commit (below).

## Files Created/Modified

- `docs/OPS-01-SHIP-GATE.md` — new dated Part 4 section recording the window, route, interaction log, both query forms, the result table, and six per-event status lines.

## Decisions Made

See `key-decisions` in the frontmatter above — route choice, the skipped optional step, the relative-vs-explicit query-form handling, the structurally-unobservable framing for the two consent events, and the PostHog project-switch confirmation.

## Deviations from Plan

### Auto-fixed Issues

None — no code bug, missing functionality, or blocking issue triggered Rules 1-3.

### Process deviation (not a Rule 1-4 case, recorded for the record)

**Split execution across three actors, driven by a checkpoint answer.** Task 1's `gate="blocking-human"` checkpoint asked the developer to choose how the four production interactions would be performed. The developer chose "you drive it" (automation-performed), but the Claude-in-Chrome extension was not connected in this session. Rather than block on that missing connection, the **orchestrator** — which holds both the headless-Chrome driving capability and the PostHog connector this specific executor does not have — performed Task 2's interactions directly against production and ran Task 3's PostHog query itself, then handed the verbatim interaction log, query, and result table to this continuation agent as the authoritative input. This continuation agent verified Task 2's already-produced interaction record against its own `<verify>` gates (both passed unchanged), verified the deep link independently (`deep-link-status=200`), and wrote/verified/committed Task 3's gate-document section from that record — it did not re-drive the browser or re-query PostHog. This is recorded here because it is a deviation from a single-agent linear execution, not because anything went wrong; every acceptance criterion and `<verify>` block in Tasks 2 and 3 was re-run and passed independently by this continuation agent before committing.

---

**Total deviations:** 0 auto-fixed (Rules 1-4 not triggered). 1 process note (split execution via checkpoint hand-off, not a defect).
**Impact on plan:** None on scope or correctness — every task's own acceptance criteria and `<verify>` blocks were run and passed by this continuation agent regardless of which actor performed the underlying interaction or query.

## Issues Encountered

None. The interaction record file (`${TMPDIR:-/tmp}/pf-0206-window.txt`) was already present at the expected `TMPDIR` resolution and matched the orchestrator's reported contents exactly — no reconstruction was needed.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- The `theme_changed` / `timeline_viewed` / `timeline_filter_used` half of `02.1-VERIFICATION.md`'s Truth 5 gap is closed by counted observation with `consent_gate_path` attached.
- The remaining half — the TCF consent-region paths and the two consent events (`consent_resolved`, `consent_unavailable`) — is explicitly named, explained, and handed to gap plan **02.1-08**, which needs an EEA/UK session to observe them.
- No blockers for 02.1-07 or 02.1-08.

---
*Phase: 02.1-posthog-consent-gate-hotfix*
*Completed: 2026-09-15*

## Self-Check: PASSED

- `docs/OPS-01-SHIP-GATE.md` exists and contains the new section: confirmed (`grep -c '^### Targeted custom-event observation (gap closure 02.1-06, 2026-09-15)' docs/OPS-01-SHIP-GATE.md` → 1).
- Commit `ba9b686` exists: confirmed (`git log --oneline --all | grep ba9b686` → found).
- All three of Task 3's `<verify>` blocks re-run and passed: `event-observation-ok`, `additive-ok` (removed=0 added=92), `added-lines-clean-ok`.
- Both of Task 2's `<verify>` blocks re-run and passed: `deep-link-status=200`, `interaction-record-ok` (start=1 end=1 route=1 did=5).
- All acceptance criteria for Tasks 1, 2 and 3 checked and satisfied.
