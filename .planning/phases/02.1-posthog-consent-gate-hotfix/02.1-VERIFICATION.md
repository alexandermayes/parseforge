---
phase: 02.1-posthog-consent-gate-hotfix
verified: 2026-09-14T23:45:00Z
status: gaps_found
score: 5/8 must-haves verified
behavior_unverified: 1
overrides_applied: 0
gaps:
  - truth: "The named custom events (consent_resolved, consent_unavailable, theme_changed, timeline_viewed, timeline_filter_used, timeline_error) appear in PostHog project 337485 within one hour of the prod deploy, with consent_gate_path distinguishing geo/TCF/timeout (ROADMAP SC3)."
    status: failed
    reason: "The production live-traffic query for the deploy window (2026-09-14T22:16:59Z-23:16:59Z) shows consent_gate_path=geo-non-consent-region only (89 events, 8 people). All five named custom events recorded exactly 0 ingestions in the window. No EEA/UK/CH visitor arrived, so the tcf-accept/tcf-reject/tcf-timeout gate paths remain entirely unobserved in production — only one of the three paths this criterion asks to be distinguished was actually distinguished."
    artifacts:
      - path: "docs/OPS-01-SHIP-GATE.md"
        issue: "Part 4, 'Live-traffic check (item 7)' section: consent_gate_path breakdown table shows only geo-non-consent-region; the five-event table shows Count=0 for every row"
    missing:
      - "A live-traffic window (or targeted manual test) containing an EEA/UK/CH visitor exercising TCF accept/reject/timeout, and a session that toggles the theme and opens the Timeline tab, so consent_resolved/consent_unavailable/theme_changed/timeline_* can actually be observed ingesting with the correct consent_gate_path"
  - truth: "This phase passes the hardened OPS-01 post-deploy live-traffic check for real, closing the gate for its own production deploy (ROADMAP SC4, second clause)."
    status: failed
    reason: "Two of the three item-7 thresholds passed with counted evidence (25 $pageview >= 20; 3 distinct non-consent-region countries >= 2), but the third (PostHog $pageview >= 50% of Vercel Web Analytics page views for the same window) is recorded as PENDING, not evaluated, because the Vercel Web Analytics figure could not be read in the session that ran the check. docs/OPS-01-SHIP-GATE.md's own 'Phase 2.1 Sign-off' section states NOT SIGNED, and .planning/REQUIREMENTS.md's 2026-09-14 addendum states the OPS-01 PostHog criterion is 'therefore not-yet-met' for Phase 2.1. The gate that this phase itself built and applied to itself has not been passed."
    artifacts:
      - path: "docs/OPS-01-SHIP-GATE.md"
        issue: "Line ~1196: '### Phase 2.1 Sign-off — NOT SIGNED (2026-09-14, Task 3)'; line ~1163: 'an un-evaluated threshold is not a pass — item 7 is not signed as complete'"
      - path: ".planning/REQUIREMENTS.md"
        issue: "OPS-01 Addendum (2026-09-14, Phase 2.1 Plan 04): 'OPS-01's PostHog criterion is therefore not-yet-met for Phase 2.1.'"
    missing:
      - "Read the real Vercel Web Analytics pageview count for 2026-09-14 22:16:59Z-23:16:59Z from https://vercel.com/loot-list-plus/parseforge/analytics and compute the ratio against the 25 PostHog $pageview events already recorded (passes at a Vercel figure <= 50)"
      - "If it passes: sign docs/OPS-01-SHIP-GATE.md Part 4's Phase 2.1 section with a date, and update REQUIREMENTS.md's OPS-01 row/addendum with the real met-date (D-09)"
      - "If it fails: report honestly per the gate's own no-lowering-the-bar rule; do not sign"
behavior_unverified_items:
  - truth: "An EEA/UK visitor's real browser behaviour is unchanged: no capture before the TCF dialog resolves, cookieless on explicit reject (no replay), replay only on full opt-in (ROADMAP SC2, MONY-01)."
    test: "Load the production site from a real EEA/UK egress IP with a fresh browser profile; observe network traffic before/after interacting with the Google CMP dialog for reject and for full-accept flows; separately, trigger a CMP 're-confirm consent' re-resolution (e.g. via the CMP's 'manage options' re-open) to exercise the WR-03 dedupe path."
    expected: "No /ingest capture request before the dialog resolves; after an explicit reject, no session-recording start and no non-essential cookie is set; after full opt-in, session replay starts and posthog.capture('consent_resolved', {accepted:true, gdpr_applies:true}) fires exactly once even if the CMP re-emits a resolved TCF event a second time for the same visit."
    why_human: "No EEA/UK egress IP was available in this environment across the whole phase; the Vercel preview used for D-10 proof never rendered the Google CMP at all because NEXT_PUBLIC_GOOGLE_CMP_PUB_ID is a Production-only env var; and the production live-traffic window (2026-09-14T22:16:59Z-23:16:59Z) contained zero EEA/UK/CH visitors, so the TCF path is unobserved end-to-end. The underlying logic (deriveConsentGateOutcome) is exhaustively unit-tested and code review confirms the SDK-call mapping matches pre-hotfix behavior exactly, but no automated or live-browser test exercises PostHogProvider's TCF wiring — including the WR-03 dedupe fix, which the phase's own fixer flagged as having no automated harness."
human_verification:
  - test: "Read Vercel Web Analytics for parseforge.gg, custom range 2026-09-14 22:16-23:17 UTC, and record the pageview count."
    expected: "A number that, compared against 25 PostHog pageviews, either clears the >= 50% threshold (Vercel figure <= 50, pass) or does not (fail, reported honestly, no threshold change)."
    why_human: "No Vercel Web Analytics API/dashboard access was available to this verification session (MCP connector scoped to a different team; CLI token has no Analytics endpoint)."
  - test: "Load the production site from a real EEA/UK egress IP; exercise TCF reject, TCF full-accept, and a CMP re-confirmation, watching network/PostHog activity."
    expected: "See behavior_unverified_items above."
    why_human: "No EEA/UK egress available in this environment; the SDK-level TCF wiring and the WR-03 dedupe fix have no automated test harness."
  - test: "Trigger the theme toggle and open the Timeline tab on a live production session, then re-run the item-7 consent_gate_path/all-events query for that window."
    expected: "theme_changed and timeline_viewed (at minimum) should appear in the query results with consent_gate_path populated."
    why_human: "Requires deliberate user interaction against production plus a follow-up PostHog query; not observable from static code or from the passive traffic already captured."
---

# Phase 02.1: PostHog Consent Gate Hotfix Verification Report

**Phase Goal:** Every visitor is measured again — non-EEA/UK visitors are captured without depending
on the Google CMP, EEA/UK visitors keep the exact TCF gate, and the OPS-01 gate can never again pass
on pre-deploy data.

**Verified:** 2026-09-14T23:45:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A fresh non-EEA/UK visitor's first `$pageview` reaches PostHog within seconds of load, decided server-side from `x-vercel-ip-country`, with no `__tcfapi` involvement (ROADMAP SC1). | ✓ VERIFIED | `lib/geo.ts` + `app/api/geo/route.ts` are the sole reader/classifier of the header (grep confirms no other reader in `app/`/`lib/`); `PostHogProvider.tsx` gates `PostHogPageView` on a `consentReady` flag set from `/api/geo`'s response with no TCF listener started on this path. Proven live: preview netlog (3 capture requests vs. baseline 0), production netlog (post-deploy), and the production live-traffic query (25 `$pageview` from US/BR/CA, all classified non-consent-region, first at 22:18:13Z). |
| 2 | Country codes missing/blank/whitespace/mixed-case are treated as consent-region (fail closed), case/whitespace-normalized (D-03). | ✓ VERIFIED | `lib/geo.ts#isConsentRegionCode`: `code?.trim().toUpperCase()`, nullish/blank → `true`. `lib/geo.test.ts` (5 tests) covers true/false/fail-closed/case/whitespace/Crown-dependency exclusion. `app/api/geo/route.ts` logs `geo_header_missing` on the trimmed value so blank headers are counted, not just fully-missing ones (WR-02 fix present in current source). |
| 3 | Every event carries a `consent_gate_path` super property (one of 4 literals), registered before opt-in, via a single SDK-call executor; no route's rendering mode changes (D-01/D-02/D-06/D-07). | ✓ VERIFIED | `lib/consent.ts#deriveConsentGateOutcome` is a pure function (21 tests in `lib/consent.test.ts` covering all rows + invariants); `PostHogProvider.tsx#applyOutcome` is the sole call site each for `posthog.register`/`opt_in_capturing`/`startSessionRecording` (grep confirms exactly one each); `app/layout.tsx` and other routes show no diff in this phase (`git diff --stat` over the phase's full commit range touches only `app/api/geo/*`, `app/components/PostHogProvider.tsx`, `lib/*`, docs, and `.planning/*`). Confirmed live: production query shows `consent_gate_path=geo-non-consent-region` on all 89 events in the window. |
| 4 | An EEA/UK visitor's real-browser behaviour is unchanged: no capture before the TCF dialog resolves, cookieless on reject (no replay), replay only on full opt-in (ROADMAP SC2, MONY-01). | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `deriveConsentGateOutcome`'s consent-region branch reproduces the pre-hotfix SDK-call mapping exactly (code comment + code review both confirm byte-for-byte equivalence); exhaustively unit-tested. But no EEA/UK egress IP was available anywhere in this phase, the Vercel preview never rendered the Google CMP (Production-only env var), and zero EEA/UK/CH visitors appeared in the measured production window — the TCF wiring in `PostHogProvider.tsx`, including the WR-03 dedupe fix, has never been exercised by a real browser or an automated test. See Human Verification. |
| 5 | The named custom events (`consent_resolved`, `consent_unavailable`, `theme_changed`, `timeline_viewed`, `timeline_filter_used`, `timeline_error`) appear in PostHog project 337485 within one hour of the prod deploy, with `consent_gate_path` distinguishing geo/TCF/timeout (ROADMAP SC3). | ✗ FAILED | Production query for the deploy window returned 0 ingestions for every one of the five named events; only `geo-non-consent-region` appears in the `consent_gate_path` breakdown — `tcf-accept`/`tcf-reject`/`tcf-timeout` are entirely unobserved (no EEA/UK/CH traffic arrived). See Gaps. |
| 6 | `docs/OPS-01-SHIP-GATE.md` is hardened to require a mandatory post-deploy live-traffic check with inclusive thresholds and a zero-is-FAIL rule, and Phase 1/2 VERIFICATION docs carry a dated, append-only addendum (ROADMAP SC4, clauses 1 & 3). | ✓ VERIFIED | `docs/OPS-01-SHIP-GATE.md` Part 1 item 7 present (HogQL, thresholds with stated passing side of equality, zero-events-is-FAIL rule); `01-VERIFICATION.md` and `02-VERIFICATION.md` each carry a dated `## Addendum (2026-09-14)` section; `git diff -U0` shows 0 removed lines in either file (confirmed by reading both files in full — pre-existing sign-off lines and sections untouched). `.planning/REQUIREMENTS.md`'s OPS-01 row and its own dated addendum both reference the correction. |
| 7 | This phase passes the hardened live-traffic gate for real, for its own production deploy (ROADMAP SC4, clause 2). | ✗ FAILED | Two of three item-7 thresholds recorded PASS with counted query evidence (25 pageviews >= 20; 3 countries >= 2); the third (PostHog >= 50% of Vercel Web Analytics pageviews) is recorded PENDING — the figure could not be read this session. `docs/OPS-01-SHIP-GATE.md`'s own "Phase 2.1 Sign-off" section reads **NOT SIGNED**, and `.planning/REQUIREMENTS.md`'s 2026-09-14 addendum states the criterion is "therefore not-yet-met for Phase 2.1." See Gaps. |
| 8 | No cookie is set for a consent-region visitor before consent, and no route other than `/api/geo` reads a Vercel geolocation header. | ✓ VERIFIED | `app/api/geo/route.ts` returns `Cache-Control: private, no-store` with no `Set-Cookie`; `app/api/geo/route.test.ts` asserts `res.headers.get("Set-Cookie")` is null. `grep -rn "x-vercel-ip-country" app/ lib/` (excluding tests) returns only `app/api/geo/route.ts`. |

**Score:** 5/8 truths verified (1 present, behavior-unverified; 2 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/geo.ts` | `CONSENT_REGIONS` + `isConsentRegionCode`, fail-closed classifier | ✓ VERIFIED | 32-code set (EU-27 + IS/LI/NO + GB/CH), trims+uppercases, fails closed on blank/nullish |
| `lib/geo.test.ts` | Region set, fail-closed default, case normalization coverage | ✓ VERIFIED | 5 tests, including `CONSENT_REGIONS.size === 32` and Crown-dependency exclusion |
| `app/api/geo/route.ts` | Sole geo-header reader; boolean response, no cookie, no cache | ✓ VERIFIED | `force-dynamic`, `Cache-Control: private, no-store`, explicit `Promise<NextResponse>` return type (WR-04 present) |
| `app/api/geo/route.test.ts` | `NextRequest`-driven Route Handler coverage | ✓ VERIFIED | Includes whitespace-only-header `geo_header_missing` test (WR-02) and no-Set-Cookie assertion |
| `lib/consent.ts` | `deriveConsentGateOutcome` pure merge function | ✓ VERIFIED | All branches present, matches code-review-confirmed mapping |
| `lib/consent.test.ts` | Coverage for `deriveConsentGateOutcome` outcome rows + invariants | ✓ VERIFIED | 21 total test cases in file, `deriveConsentGateOutcome` covered by 10+ of them incl. gatePath-literal and optIn/startReplay invariants |
| `app/components/PostHogProvider.tsx` | Module-scope init, consent-readiness gate, single `applyOutcome` executor | ✓ VERIFIED | Runtime-validated `/api/geo` body (CR-01 fix present), `AbortSignal.timeout(5000)` (WR-01 fix present), `capturedGatePathRef` dedupe (WR-03 fix present) |
| `docs/OPS-01-SHIP-GATE.md` | Hardened gate (item 7) + Part 4 phase evidence | ✓ VERIFIED (artifact) / ✗ gate itself unresolved | Document exists, is well-formed, and item 7 is well-specified; but its own Part 4 records itself as unsigned (see Truth 7) |
| `.planning/phases/01-foundation-themes-consent/01-VERIFICATION.md` | Dated append-only addendum | ✓ VERIFIED | `## Addendum (2026-09-14)` present, 0 lines removed |
| `.planning/phases/02-accuracy-analysis-depth/02-VERIFICATION.md` | Dated append-only addendum | ✓ VERIFIED | `## Addendum (2026-09-14)` present, 0 lines removed |
| `.planning/REQUIREMENTS.md` | OPS-01 row + addendum recording the correction | ✓ VERIFIED | Traceability row references the correction; a separate dated addendum (lines 125-141) states the criterion remains not-yet-met for Phase 2.1 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `app/components/PostHogProvider.tsx` | `app/api/geo/route.ts` | `fetch("/api/geo", ...)` result drives `applyDecision` | ✓ WIRED | Fetch result runtime-validated, feeds `deriveConsentGateOutcome` |
| `app/components/PostHogProvider.tsx` | `lib/consent.ts` | `deriveConsentGateOutcome(isConsentRegion, tcfAction)` | ✓ WIRED | Both the geo path (`false, null`) and TCF path route through the same function/executor |
| `docs/OPS-01-SHIP-GATE.md` (item 7) | PostHog project 337485 | HogQL query executed post-deploy | ✓ WIRED (executed) | Query run and output pasted verbatim; see Truth 7 for the incomplete result |
| `.planning/REQUIREMENTS.md` | `01-VERIFICATION.md` / `02-VERIFICATION.md` | OPS-01 row references the phase addenda | ✓ WIRED | Row text and addendum both present and mutually consistent |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite passes at HEAD | `npx vitest run` | 16 files / 162 tests passed, 0 failed | ✓ PASS |
| Typecheck clean | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| `deriveConsentGateOutcome` / `isConsentRegionCode` / geo route unit tests | `npx vitest run lib/geo.test.ts lib/consent.test.ts app/api/geo/route.test.ts` | 3 files / 36 tests passed | ✓ PASS |
| Sole geo-header reader | `grep -rn "x-vercel-ip-country" app/ lib/ \| grep -v .test.` | 1 match (`app/api/geo/route.ts`) | ✓ PASS |
| Single SDK call sites | `grep -n "posthog.register(\|posthog.opt_in_capturing(\|posthog.startSessionRecording(" app/components/PostHogProvider.tsx` | 1 each | ✓ PASS |
| No cookie set by `/api/geo` | `grep -n "Set-Cookie\|cookie" app/api/geo/route.ts` | 0 matches | ✓ PASS |
| Debt markers in changed source files | `grep -n "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` over `lib/geo.ts`, `lib/consent.ts`, `app/api/geo/route.ts`, `app/components/PostHogProvider.tsx` | 0 matches | ✓ PASS |
| Production/preview netlog proof, live-traffic HogQL queries | (not re-run by this verifier — no PostHog/Vercel/browser access) | See docs/OPS-01-SHIP-GATE.md Part 4 for pasted output | ? SKIP (relied on recorded artifact evidence per task's `known_facts`) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| OPS-01 (re-verification) | 02.1-01, 02.1-02, 02.1-03, 02.1-04 | Every user-facing change ships WITH PostHog instrumentation, verified live | ⚠️ PARTIALLY SATISFIED | The gate itself is correctly hardened (item 7, honest addenda) and the phase ran it for real against production — but the gate's own sign-off is explicitly NOT SIGNED (1 of 3 thresholds PENDING). `.planning/REQUIREMENTS.md`'s own addendum agrees: "not-yet-met for Phase 2.1." No plan in this phase marked `requirements-completed: [OPS-01]` for the 04 plan — consistent with this. |
| MONY-01 (consent semantics preserved) | 02.1-01, 02.1-03, 02.1-04 | EEA/UK TCF consent gating must remain exactly as it was | ⚠️ SATISFIED IN CODE, LIVE PROOF OUTSTANDING | `deriveConsentGateOutcome`'s consent-region branch is unit-tested exhaustively and confirmed by code review to reproduce pre-hotfix SDK calls exactly; the non-consent-region path (the actual bug fixed) is proven live. But no real EEA/UK browser session was ever observed in this phase (see Truth 4 / Human Verification). |

No orphaned requirements found — `REQUIREMENTS.md`'s Phase 2.1 section (OPS-01, MONY-01) matches both plans' declared `requirements` fields.

### Anti-Patterns Found

None. Scanned `lib/geo.ts`, `lib/geo.test.ts`, `app/api/geo/route.ts`, `app/api/geo/route.test.ts`, `lib/consent.ts`, `lib/consent.test.ts`, `app/components/PostHogProvider.tsx` for debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER), empty implementations, and hardcoded-empty stub patterns — no matches. All 5 code-review findings from `02.1-REVIEW.md` (CR-01, WR-01, WR-02, WR-03, WR-04) are confirmed present and fixed in the current source, matching `02.1-REVIEW-FIX.md`'s claims. Note: those five fixes (commits `90f5a46`, `0e03d9b`, `7a7cf0c`, `9657535`, `9da21c4`) are **not** in the production deployment measured for Truth 7 (`dpl_HY5319wSDVw3M4ibBU42JrSTgw4e`, built from `8693498`, which predates all five) — this is honestly recorded in `docs/OPS-01-SHIP-GATE.md` itself and does not change this phase's own code-quality verdict, but means the live-traffic evidence gathered does not reflect the fully-fixed code.

### Human Verification Required

### 1. Read the Vercel Web Analytics figure and complete the gate scoring

**Test:** Open `https://vercel.com/loot-list-plus/parseforge/analytics`, set the custom range to `2026-09-14 22:16-23:17 UTC`, and read the pageview count.
**Expected:** A number that, compared against the 25 PostHog `$pageview` events already recorded, either clears the ≥50% threshold (Vercel figure ≤ 50 → PASS) or does not (→ FAIL, reported honestly).
**Why human:** No Vercel Web Analytics API/dashboard access is available to this session (the Vercel MCP connector is scoped to a different team; the personal CLI token exposes no Analytics endpoint).

### 2. Observe real EEA/UK browser behaviour end-to-end

**Test:** From a real EEA/UK (or CH) egress IP, load parseforge.gg with a fresh browser profile. Watch network traffic through the Google CMP dialog: (a) explicit reject, (b) full opt-in, and (c) a CMP "manage consent" re-confirmation that re-emits a resolved TCF event for the same visit.
**Expected:** No `/ingest` capture request before the dialog resolves; reject stays cookieless with no session-replay start; full opt-in starts replay and fires `consent_resolved` (`accepted: true, gdpr_applies: true`) exactly once, even across the re-confirmation in (c).
**Why human:** No EEA/UK egress IP was available anywhere in this phase; the only preview deployment tested never rendered the Google CMP (`NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` is Production-only); the production live-traffic window contained zero EEA/UK/CH visitors. This is the WR-03 dedupe fix's only outstanding verification gap, per the phase's own code-review-fix report.

### 3. Confirm the remaining custom events actually ingest with the correct consent_gate_path

**Test:** On a live production session, toggle the theme and open the Timeline tab; then re-run Part 1 item 7's `consent_gate_path`/all-events HogQL query for that window.
**Expected:** `theme_changed` and `timeline_viewed` (at minimum) appear with a populated `consent_gate_path`.
**Why human:** Requires deliberate production interaction plus a follow-up PostHog query — not observable from code or from the passive traffic already captured in the measured window.

### Gaps Summary

Two of eight verified truths are genuine gaps, both traceable to the same root fact: **the phase's
own hardened OPS-01 live-traffic gate has not yet closed for its own production deploy.** The
non-EEA/UK consent-region fix itself (ROADMAP SC1, the actual regression this phase exists to
repair) is solidly proven — in code, in unit tests, in a preview netlog, and now in real production
traffic (25 pageviews, 3 non-consent-region countries, zero `__tcfapi` involvement). What remains
open is (1) the specific named custom events and the TCF/timeout consent paths were never
*observed* in the one measured production window, because no EEA/UK visitor arrived and no session
happened to trigger the theme/Timeline UI, and (2) the gate's own third threshold (PostHog vs.
Vercel Web Analytics pageview ratio) could not be evaluated in the executing session and remains
PENDING — the document the phase itself produced says so explicitly and is NOT SIGNED. Neither gap
reflects a code defect discovered by this verification; both are honestly recorded by the phase's
own artifacts and require either more live traffic/time, a manual EEA test, or a human reading a
Vercel dashboard number to close.

---

_Verified: 2026-09-14T23:45:00Z_
_Verifier: Claude (gsd-verifier)_
