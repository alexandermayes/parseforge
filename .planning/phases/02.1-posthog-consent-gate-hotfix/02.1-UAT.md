---
status: testing
phase: 02.1-posthog-consent-gate-hotfix
source: [02.1-VERIFICATION.md]
started: 2026-09-15T21:25:37Z
updated: 2026-09-15T21:25:37Z
---

## Current Test

number: 1
name: Observe real EEA/UK/Swiss browser behaviour end-to-end
expected: |
  (a) Zero /ingest/e/ or /ingest/i/v0/e/ requests before the CMP dialog resolves; after reject, no session-replay start and no ph_* / __ph_opt_in_out_* cookie for parseforge.gg.
  (b) Capture requests and session replay both start after accept-all; consent_resolved fires.
  (c) consent_resolved fires exactly once across both resolutions of the re-confirm session, proving the WR-03 dedupe (commit 9da21c4, live in production build dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5) holds in a real browser.
awaiting: user response

## Tests

### 1. Observe real EEA/UK/Swiss browser behaviour end-to-end
expected: From a real EEA, UK or Swiss VPN egress, in a fresh private browser window opened AFTER the VPN connects (a reused profile may carry a prior consent choice): (a) load parseforge.gg and reject the CMP dialog, watching Network (filtered to "ingest") and Application/Storage — zero /ingest/e/ or /ingest/i/v0/e/ requests before the dialog resolves, no session-replay start and no ph_* / __ph_opt_in_out_* cookie after reject; (b) in a new fresh private window, accept all — capture requests and session replay start, consent_resolved fires; (c) accept, then re-open the CMP's manage-options entry point and re-confirm — consent_resolved fires exactly once across both resolutions (WR-03 dedupe, commit 9da21c4, live in dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5). Report the egress country and UTC bounds per session so the PostHog consent_gate_path breakdown (tcf-accept / tcf-reject, not geo-non-consent-region) can be read for the window.
result: [pending]

### 2. Confirm the judgment-tier "no bar-lowering" prohibitions hold
expected: A human re-reads docs/OPS-01-SHIP-GATE.md Part 4's four new sections (gap closure 02.1-05 through 02.1-08) against each plan's prohibitions and confirms the record is honest: no item 7 threshold was widened, re-picked or averaged to manufacture a pass; the dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5 redeploy's own gate row is unsigned and stays unsigned; the EEA/UK "not performed" outcome is not treated as a pass anywhere downstream; MONY-01 / ROADMAP SC2 stay recorded as behaviour-unverified rather than complete.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
