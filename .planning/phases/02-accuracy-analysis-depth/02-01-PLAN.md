---
phase: 02-accuracy-analysis-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/record-wcl-fixtures.mjs
  - lib/__fixtures__/README.md
  - lib/__fixtures__/demo-player-dps.json
  - lib/__fixtures__/demo-player-healer.json
  - lib/__fixtures__/demo-raid-overview.json
  - lib/__fixtures__/demo-raid-combatant-info.json
  - lib/__fixtures__/demo-raid-death-events.json
  - lib/__fixtures__/demo-timeline-casts.json
  - lib/__fixtures__/fixtures.test.ts
  - lib/wcl-queries.ts
  - lib/wcl-types.ts
  - lib/constants.ts
  - lib/utils.ts
  - lib/analysis-engine.ts
  - package.json
autonomous: true
requirements: [ACC-02, ACC-03, ACC-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `npm run record-fixtures` with WCL production credentials in the environment writes six non-empty JSON fixtures under lib/__fixtures__/ recorded from the PUBLIC demo report ZjKgNYxVcAqR8pGJ (D-13)."
    - "lib/__fixtures__/README.md answers each of RESEARCH.md assumptions A1-A5 from the recorded response: the argument names the cast-events field actually accepts, the real field names on a single cast event, whether a next-page timestamp appeared, the page size honoured, and whether a sourceID-scoped Healing row carries per-ability overheal and activeTime."
    - "No recorded fixture contains a WCL OAuth token or client credential, and the pulled Vercel env file is gone from the filesystem when the recording run ends."
    - "lib/wcl-queries.ts exports TIMELINE_CASTS_QUERY and TIMELINE_CASTS_PAGE_QUERY whose cast-events arguments match the recorded response rather than an assumption."
    - "PLAYER_FULL_DATA_QUERY_HEALING and TOP_PLAYER_DATA_QUERY_HEALING each carry a second, separately-aliased healingByPlayer table field with no sourceID argument, so player-level overheal and active time are available on both the player fetch and the per-top-player fetch with no new round trip (RESEARCH.md Pitfall 1 and Pitfall 2)."
    - "lib/wcl-types.ts exports CastTimelineResult, TimelineRow, TimelineAbilityCount, WCLCastEvent, TimelineRequest, HealerTableRow, HealerMetricsComputed and HealerComparison, and AnalysisResult carries an optional healer field — the contracts plans 02-02, 02-04, 02-05 and 02-07 build against."
    - "RATE_LIMITS in lib/constants.ts contains an explicit timeline entry, so the new route can never fall through to the unknown-bucket default flagged in .planning/codebase/CONCERNS.md."
    - "lib/utils.ts exports formatFightTime, the single mm:ss fight-relative formatter both RaidOverview's death timeline and the new cast timeline use, so the two surfaces cannot drift apart on how a fight time reads (02-UI-SPEC.md, Cast Timeline row anatomy)."
    - "lib/analysis-engine.ts exports JUNK_SPELL_IDS and isJunkSpell, so the cast timeline reuses the one junk-spell exclusion set rather than declaring a second copy of the same spell ids (D-04)."
    - "`npx vitest run lib/__fixtures__/fixtures.test.ts` passes and asserts each fixture exposes the fields the engines read, so a re-recorded fixture whose shape changed fails loudly instead of silently."
  prohibitions:
    - statement: "MUST NOT record or commit a fixture from a Warcraft Logs report that is not publicly viewable — a committed fixture is permanent, world-readable data about named players who never consented to it."
      status: unverified
      verification: judgment
  artifacts:
    - path: "scripts/record-wcl-fixtures.mjs"
      provides: "One-off recorder capturing real WCL GraphQL responses for the public demo report"
      min_lines: 90
    - path: "lib/__fixtures__/README.md"
      provides: "Fixture provenance plus the resolved answers to RESEARCH.md assumptions A1-A5"
      min_lines: 30
    - path: "lib/__fixtures__/fixtures.test.ts"
      provides: "Shape guard over every recorded fixture"
      min_lines: 30
    - path: "lib/wcl-queries.ts"
      provides: "Timeline queries plus healingByPlayer on both healing queries"
      contains: "TIMELINE_CASTS_QUERY"
    - path: "lib/wcl-types.ts"
      provides: "Timeline and healer type contracts consumed by 02-02, 02-04, 02-05 and 02-07"
      contains: "CastTimelineResult"
    - path: "lib/constants.ts"
      provides: "Explicit RATE_LIMITS timeline bucket"
      contains: "timeline:"
  key_links:
    - from: "scripts/record-wcl-fixtures.mjs"
      to: "lib/__fixtures__/demo-timeline-casts.json"
      via: "writes every recorded page envelope, so the pagination contract is captured and not just the flattened rows"
      pattern: "demo-timeline-casts"
    - from: "lib/__fixtures__/fixtures.test.ts"
      to: "lib/__fixtures__/demo-raid-overview.json"
      via: "imports the fixture and asserts healing entries carry total, overheal and activeTime"
      pattern: "demo-raid-overview"
    - from: "lib/wcl-queries.ts"
      to: "lib/wcl-types.ts"
      via: "the timeline query's response is typed by the WCLCastEvent and CastTimelineResult contracts declared for it"
      pattern: "WCLCastEvent"
---

## Phase Goal

Raiders get verifiably correct analysis plus the per-fight depth competitors already offer.

> **MVP note for the orchestrator:** MVP_MODE is enabled, but the ROADMAP `**Goal:**` line for
> Phase 2 is not in `As a / I want to / so that` form. Per the MVP planning rules no user story is
> invented here — the ROADMAP goal is carried verbatim. Run `/gsd mvp-phase 02` if the phase goal
> should be restated as a user story before execution.

<objective>
Record the real WCL GraphQL responses this phase's engines consume, then turn what those responses
actually contain into the query and type contracts every later Phase 2 plan builds against.

RESEARCH.md flags five assumptions (A1-A5) about WCL response shapes that no research session could
verify, and 02-VALIDATION.md names `lib/__fixtures__/` plus a live-query spike as this phase's Wave 0
requirements. The two consumers of those fixtures are independent — the cast timeline (ACC-03) and
the engine regression tests (ACC-02) — so recording once, up front, is a genuine shared Wave 0
artifact rather than a horizontal layer.

This is the only plan in Phase 2 that ships no user-visible change. It exists because every later
plan would otherwise be written against an unverified API shape.

Purpose: replace five assumptions with recorded facts, and hand plans 02-02, 02-04, 02-05 and 02-07
their contracts so no executor has to go hunting for them (D-13).
Output: the recorder script, six recorded fixtures with a provenance README, a fixture shape test,
the new and extended GraphQL queries, the timeline and healer type contracts, and the explicit
timeline rate-limit bucket.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accuracy-analysis-depth/02-CONTEXT.md
@.planning/phases/02-accuracy-analysis-depth/02-RESEARCH.md
@.planning/phases/02-accuracy-analysis-depth/02-PATTERNS.md
@.planning/phases/02-accuracy-analysis-depth/02-VALIDATION.md
@./CLAUDE.md
@./.claude/CLAUDE.md

<interfaces>
Contracts already in the codebase that this plan extends. Use these directly — no exploration needed.

From lib/demo-report.ts:
```typescript
export const DEMO_REPORT = { code: "ZjKgNYxVcAqR8pGJ", fight: 23, source: 12 } as const;
```

From lib/wcl-client.ts:
```typescript
export async function wclQuery<T>(query: string, variables?: Record<string, unknown>): Promise<T>;
export class WCLError extends Error { readonly kind: WCLErrorKind; readonly status: number; readonly userMessage: string }
export type WCLErrorKind = "not_found" | "private" | "rate_limited" | "timeout" | "upstream";
```

From lib/constants.ts:
```typescript
export const WCL_API_URL = "https://www.warcraftlogs.com/api/v2/client";
export const WCL_TOKEN_URL = "https://www.warcraftlogs.com/oauth/token";
export const RATE_LIMIT_WINDOW = "60 s";
export const RATE_LIMITS: Record<string, number> = { analyze: 30, "raid-overview": 30, cla: 10, report: 60, "report-players": 60 };
export const HEALER_SPECS = new Set(["Restoration", "Holy", "Discipline", "Mistweaver"]);
export function isHealerSpec(spec: string): boolean;
```

From lib/wcl-helpers.ts:
```typescript
export function flattenPlayerDetails(raw: { data?: { playerDetails?: Record<string, WCLPlayerDetails[]> } } | undefined): WCLPlayerDetails[];
export function parsePlayerSpec(player: WCLPlayerDetails): string;
```

From lib/wcl-types.ts:
```typescript
export interface WCLCastEntry { name: string; guid: number; type: number; abilityIcon: string; total: number }
export interface HealerMetrics { sourceId: number; name: string; className: string; spec: string; hps: number; totalHealing: number; overhealPercent: number; activityPercent: number }
```

The per-player Healing row shape the un-scoped table returns, verbatim from
app/api/raid-overview/route.ts lines 39-52 — this is the shape `healingByPlayer` must yield:
```typescript
healing: { data: { entries: Array<{ id: number; name: string; type: string; icon: string; total: number; overheal?: number; activeTime: number; activeTimeReduced: number }> } };
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Record the real WCL responses for the public demo report, end to end</name>
  <precondition>`vercel env ls production --scope loot-list-plus` lists WCL_CLIENT_ID and WCL_CLIENT_SECRET, i.e. the Vercel CLI is authenticated for the loot-list-plus team.</precondition>
  <files>scripts/record-wcl-fixtures.mjs, package.json, lib/__fixtures__/README.md, lib/__fixtures__/demo-player-dps.json, lib/__fixtures__/demo-player-healer.json, lib/__fixtures__/demo-raid-overview.json, lib/__fixtures__/demo-raid-combatant-info.json, lib/__fixtures__/demo-raid-death-events.json, lib/__fixtures__/demo-timeline-casts.json</files>
  <read_first>
    - lib/demo-report.ts — the public report code, fight and source this records against
    - lib/wcl-client.ts — the OAuth and query mechanics the recorder mirrors (do not import it; the recorder is a standalone .mjs)
    - lib/wcl-queries.ts — the existing query bodies to record, and the events(...) formatting to copy
    - app/api/analyze/route.ts — which fields off each response the engines actually read
    - app/api/raid-overview/route.ts — the three raid queries and their response typing
    - scripts/token-audit.mjs — the shebang, header doc comment, argv mode parsing and exit-code contract this script family follows
    - ./CLAUDE.md — the secrets rule: a pulled env file is deleted after use and credentials are never printed
  </read_first>
  <action>
Create `scripts/record-wcl-fixtures.mjs`, a recorder in the same family as `scripts/token-audit.mjs`
(shebang, header doc comment, `process.argv` mode parsing, explicit `process.exit` codes). Add
`"record-fixtures": "node scripts/record-wcl-fixtures.mjs"` to the `scripts` block of `package.json`
directly after `seo-invariants`.

The recorder reads `WCL_CLIENT_ID` and `WCL_CLIENT_SECRET` from `process.env`, mints an OAuth token
against the WCL token URL with `grant_type=client_credentials`, and POSTs GraphQL to the WCL client
API URL. Both URLs are duplicated as local constants — this is a standalone `.mjs` and must not
import TypeScript from `lib/`. Exit 2 with a one-line message naming the missing variable names if
either is absent. Never write any part of a credential or token to stdout or to a file.

Record against the public demo report only: code `ZjKgNYxVcAqR8pGJ`, fight `23`, source `12`.
Resolve a healer source id at run time instead of hardcoding one: run the raid-overview query,
flatten `playerDetails`, and take the first player whose parsed spec is one of Restoration, Holy,
Discipline or Mistweaver. If fight 23 has no healer, exit 2 with a message naming the fight and
asking for a different one — never fall back to a damage-dealer source and label it a healer fixture.

Write these six files under a new `lib/__fixtures__/` directory, each the verbatim `data` payload WCL
returned, pretty-printed with two-space indentation and a trailing newline so future diffs read
cleanly:

  - `demo-player-dps.json` — the existing full player-data query for fight 23 and source 12
  - `demo-player-healer.json` — the existing healing player-data query for fight 23 and the resolved
    healer source, PLUS a sibling top-level key holding a separate Healing table response taken with
    the `sourceID` argument omitted, so the per-ability shape and the per-player shape sit side by
    side in one file and assumptions A3 and A5 can be answered from it
  - `demo-raid-overview.json` — the existing raid-overview query for fight 23
  - `demo-raid-combatant-info.json` — the existing raid combatant-info query for fight 23
  - `demo-raid-death-events.json` — the existing raid death-events query for fight 23
  - `demo-timeline-casts.json` — the cast-events probe described next

For `demo-timeline-casts.json`, probe the `events` field with `dataType: Casts` for fight 23 and
source 12, passing `fightIDs`, `sourceID`, `dataType`, `startTime` and `limit: 300`, following the
exact shape of the existing raid death-events and combatant-info calls. Select `data` and
`nextPageTimestamp`. Loop while a next-page timestamp comes back, passing it as the next call's
`startTime`, capped at 20 pages. Store the result as an object with a `pages` array holding each page
envelope verbatim so the pagination contract itself is recorded, a `pageCount`, and a
`truncatedByCap` boolean. Alongside it record the aggregated Casts table response for the same
player, since that table is where the timeline gets each ability's display name and icon, and a
`masterData` actor list with no type filter, since cast targets include NPCs as well as players.

If WCL rejects a field or argument name in the probe, adjust to what the API accepts and record what
worked. The point of this task is to learn the real contract, not to defend the guessed one.

Write `lib/__fixtures__/README.md` documenting: the report code, fight and source recorded; the date;
that the report is public and that a non-public report must never be recorded here; the exact
`npm run record-fixtures` invocation; and a findings section answering each of assumptions A1, A2,
A3, A4 and A5 with what the recorded response actually contained — the accepted argument names on
the cast-events field, the real field names on a single cast event (whichever of an ability id, a
source id, a target id and a timestamp are present, under their real names), whether a next-page
timestamp appeared for this fight, the page size WCL honoured, and whether a sourceID-scoped Healing
entry carries per-ability overheal and active time.

Run the recorder like this, and delete the pulled env file when done:
`vercel env pull "$SCRATCH/.env.wcl" --environment=production --scope loot-list-plus`, then
`set -a; . "$SCRATCH/.env.wcl"; set +a`, then `npm run record-fixtures`, then
`rm -f "$SCRATCH/.env.wcl"`, where `$SCRATCH` is the session scratchpad directory. Never copy that
file into the repository tree.
  </action>
  <acceptance_criteria>
    - `scripts/record-wcl-fixtures.mjs` exists and its first line is the node shebang
    - `package.json` scripts contains a `record-fixtures` entry
    - All six named JSON files exist under `lib/__fixtures__/` and every one is non-empty
    - `lib/__fixtures__/demo-timeline-casts.json` parses and its top level has a `pages` array with at least one element
    - `lib/__fixtures__/demo-player-healer.json` parses and holds both the sourceID-scoped healing response and the un-scoped per-player healing response
    - `lib/__fixtures__/README.md` names assumptions A1 through A5 and gives a written answer for each
    - No file under `lib/__fixtures__/` contains an OAuth token or a client credential value
    - The Vercel env file pulled during recording is absent from the filesystem and from `git status` afterwards
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; [ -d node_modules ] || npm ci; test -f scripts/record-wcl-fixtures.mjs && head -1 scripts/record-wcl-fixtures.mjs | grep -q '^#!/usr/bin/env node' && node -e 'const p=require("./package.json");process.exit(p.scripts&&p.scripts["record-fixtures"]?0:1)'</automated>
    <fails_when>non-zero exit — the script file is absent, its first line is not the shebang, or package.json has no record-fixtures entry</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; for f in demo-player-dps demo-player-healer demo-raid-overview demo-raid-combatant-info demo-raid-death-events demo-timeline-casts; do test -s "lib/__fixtures__/$f.json" || { echo "MISSING OR EMPTY: $f.json"; exit 1; }; done; node -e 'const t=require("./lib/__fixtures__/demo-timeline-casts.json");if(!Array.isArray(t.pages)||t.pages.length===0){console.error("no recorded pages");process.exit(1)}'</automated>
    <fails_when>non-zero exit, a "MISSING OR EMPTY" line naming a fixture, or "no recorded pages" on stderr</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; ! grep -rq 'client_secret' lib/__fixtures__/ && ! grep -rq 'access_token' lib/__fixtures__/ && ! grep -rqi 'authorization' lib/__fixtures__/</automated>
    <fails_when>non-zero exit — a credential-bearing key name was found inside a recorded fixture</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; [ "$(grep -oE '\bA[1-5]\b' lib/__fixtures__/README.md | sort -u | wc -l | tr -d ' ')" = "5" ]</automated>
    <fails_when>non-zero exit — fewer than five distinct assumption references appear in the fixture README, meaning at least one of A1 through A5 was left unanswered</fails_when>
    <human-check>Open https://www.warcraftlogs.com/reports/ZjKgNYxVcAqR8pGJ in a logged-out browser window and confirm it loads before the fixtures are committed. A fixture recorded from a non-public report must be deleted, not committed.</human-check>
  </verify>
  <done>Six fixtures recorded from the public demo report exist under `lib/__fixtures__/`, the recorder is committed and wired to `npm run record-fixtures`, the README answers A1 through A5 from real response data, and no credential material reached the repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn the recorded shapes into the query, type and rate-limit contracts Phase 2 builds on</name>
  <files>lib/__fixtures__/fixtures.test.ts, lib/wcl-queries.ts, lib/wcl-types.ts, lib/constants.ts, lib/utils.ts, lib/analysis-engine.ts</files>
  <read_first>
    - lib/__fixtures__/README.md — the A1-A5 findings from Task 1; the queries and types below must match these, not the guesses in RESEARCH.md
    - lib/__fixtures__/demo-timeline-casts.json — the real cast-event field names and the recorded page envelopes
    - lib/__fixtures__/demo-player-healer.json — whether a sourceID-scoped Healing row carries overheal and active time
    - lib/wcl-queries.ts — the file being modified; copy its existing events(...) and table(...) formatting exactly
    - lib/wcl-types.ts — the file being modified; match its interface and section-divider conventions
    - lib/constants.ts — the RATE_LIMITS block and the comment above it explaining per-route tuning
    - app/api/raid-overview/route.ts — the per-player Healing row shape the un-scoped table returns
    - lib/utils.ts — the shared util module gaining formatFightTime
    - app/components/RaidOverview.tsx — the private formatFightTime implementation at line 319 that formatFightTime must reproduce byte-for-byte in behaviour
    - lib/analysis-engine.ts — the private JUNK_SPELL_IDS set at line 48 and isJunkSpell at line 55, and the pre-existing lint debt in this file that must not grow
  </read_first>
  <behavior>
    - Test 1: every recorded fixture parses as JSON and exposes a report object
    - Test 2: demo-raid-overview healing entries carry total and activeTime, and at least one entry carries overheal — the three fields the shared healer helper will read
    - Test 3: demo-player-healer exposes a per-player healing row whose id equals the recorded healer source id, under the un-scoped response
    - Test 4: demo-timeline-casts pages[0].data is a non-empty array and every element carries the ability-id and timestamp field names recorded in README.md
    - Test 5: demo-timeline-casts records an aggregated casts table whose entries carry guid, name and abilityIcon, so the timeline can resolve display names by guid
    - Test 6: demo-raid-combatant-info and demo-raid-death-events expose the arrays buildRaidOverview reads
  </behavior>
  <action>
Write `lib/__fixtures__/fixtures.test.ts` first, asserting the six behaviours above by importing each
fixture JSON directly — `vitest.config.ts` already resolves the `@` alias and includes
`lib/**/*.test.ts`. Assert on the field names Task 1 recorded in the README. Where a recorded field
name differs from the RESEARCH.md assumption, the test asserts the recorded name and the types below
use the recorded name. Run it, watch it fail while the types are absent, then add the contracts.

In `lib/wcl-queries.ts`, add a Timeline Queries section divider after the raid-overview section and
export two queries.

`TIMELINE_CASTS_QUERY` takes `$code: String!`, `$fightIDs: [Int!]!`, `$sourceID: Int!` and
`$startTime: Float`, and fetches the first page plus everything the timeline needs exactly once.
Alias the paged field as `castEvents`, using `events` with `fightIDs`, `sourceID`, `dataType: Casts`,
`startTime` and `limit: 300`, selecting `data` and `nextPageTimestamp`. Alongside it select
`castTable: table(dataType: Casts, fightIDs: $fightIDs, sourceID: $sourceID)` for guid-to-name and
guid-to-icon resolution; `deathEvents: events(fightIDs: $fightIDs, sourceID: $sourceID, dataType:
Deaths, limit: 10) { data }` for the death marker; `masterData { actors { id name type subType } }`
with no type filter for target-name resolution; and `fights(fightIDs: $fightIDs) { id name startTime
endTime }` for the fight-relative timestamp baseline.

`TIMELINE_CASTS_PAGE_QUERY` takes the same variables with `$startTime: Float!` required, and selects
only the aliased `castEvents` field with `data` and `nextPageTimestamp`. Splitting the two avoids
re-fetching the actor list, the casts table and the fight bounds on every page of a long fight.

Still in `lib/wcl-queries.ts`, add a second aliased healing field to both
`PLAYER_FULL_DATA_QUERY_HEALING` and `TOP_PLAYER_DATA_QUERY_HEALING`: `healingByPlayer:
table(dataType: Healing, fightIDs: $fightIDs)` — the same field with the `sourceID` argument
omitted. Put a one-line comment above each explaining why the second field exists: the scoped table
returns a per-ability breakdown, so player-level overheal and active time only appear on the
un-scoped per-player row. Do not remove or rename the existing `healing:` field —
`app/api/analyze/route.ts` and `lib/wcl-fetchers.ts` both read it today.

In `lib/wcl-types.ts`, add a Cast Timeline Types section exporting: `WCLCastEvent`, the recorded
cast-event shape carrying a timestamp, a type, a source id, an optional target id and the ability id,
each under the field name Task 1 recorded; `TimelineRowKind` as the union of `"cast"`, `"idle"` and
`"death"`; `TimelineRow` with `kind: TimelineRowKind`, `fightTimeMs: number` and optional
`abilityGameID`, `abilityName`, `abilityIcon`, `targetName` and `idleMs`; `TimelineAbilityCount` with
`abilityGameID`, `abilityName`, optional `abilityIcon` and `count`; `CastTimelineResult` with
`encounterName`, `fightDuration`, `playerName`, `rows: TimelineRow[]`, `abilityCounts:
TimelineAbilityCount[]`, `idleThresholdMs`, `truncated: boolean` and `castCount`; and
`TimelineRequest` with `reportCode: string`, `fightId: number` and `sourceId: number`.

Add a Healer Metrics Types section exporting: `HealerTableRow` with `id: number`, optional `name`,
`total: number`, optional `overheal` and `activeTime: number` — the un-scoped per-player Healing row
narrowed to what the shared helper reads; `HealerMetricsComputed` with `effectiveHps`,
`overhealPercent`, `activityPercent` and `hasHealing: boolean`, where `hasHealing` is false when the
player recorded no healing so a surface can render an em dash instead of a zero that looks measured;
and `HealerComparison` with `effectiveHps`, `overhealPercent`, `activityPercent`,
`topOverhealPercent`, `topActivityPercent` and `topSampleCount`.

Then add `healer?: HealerComparison` to the existing `AnalysisResult` interface, documented as set
only when `playerRole` is `"healer"`, and add `healerRow?: HealerTableRow` to the existing
`TopPlayerFullData` interface, documented as populated only on the healer fetch path — that field is
how a top healer's own overheal and uptime reach the comparison without a second round trip, since
`WCLRanking` carries no such fields (RESEARCH.md Pitfall 2). Leave the existing `HealerMetrics`
interface untouched — `RaidOverviewResult` still returns it.

In `lib/constants.ts`, add a `timeline: 20,` entry to `RATE_LIMITS` after the `cla` entry, and extend
the comment above the map to record that the timeline bucket is tighter than analyze because one
request can page the cast-events field up to twenty times.

Two shared helpers move here so plans 02-02 and 02-04 both consume one implementation instead of two.

In `lib/utils.ts`, add and export `formatFightTime(ms: number): string`, reproducing the behaviour of
the private helper at `app/components/RaidOverview.tsx` line 319 exactly: clamp negatives to zero,
floor to whole seconds, and render minutes and zero-padded seconds separated by a colon. Give it a
JSDoc block naming both call sites — the raid overview's death timeline and the cast timeline — and
stating that a fight time is always relative to the fight's own start, never to report start. Leave
`app/components/RaidOverview.tsx` untouched in this plan; plan 02-04 owns that file and switches it
to the shared import.

In `lib/analysis-engine.ts`, add the `export` keyword to the existing `JUNK_SPELL_IDS` set at line 48
and to the existing `isJunkSpell` helper at line 55, and extend the comment above the set to record
that the cast timeline reads the same exclusion list. Change nothing else in that file — it carries
pre-existing lint debt that must not grow.
  </action>
  <acceptance_criteria>
    - `npx vitest run lib/__fixtures__/fixtures.test.ts` exits 0 with at least six assertions passing
    - `lib/wcl-queries.ts` contains `TIMELINE_CASTS_QUERY` and `TIMELINE_CASTS_PAGE_QUERY`
    - `lib/wcl-queries.ts` contains exactly two `healingByPlayer:` occurrences, one in each healing query
    - `lib/wcl-queries.ts` still contains at least three `healing: table(` occurrences, proving no original field was renamed away
    - `lib/wcl-types.ts` exports `CastTimelineResult`, `TimelineRow`, `TimelineAbilityCount`, `WCLCastEvent`, `TimelineRequest`, `HealerTableRow`, `HealerMetricsComputed` and `HealerComparison`
    - `AnalysisResult` in `lib/wcl-types.ts` declares an optional `healer` field of type `HealerComparison`
    - `TopPlayerFullData` in `lib/wcl-types.ts` declares an optional `healerRow` field of type `HealerTableRow`
    - `RATE_LIMITS` in `lib/constants.ts` has a numeric `timeline` key
    - `lib/utils.ts` exports `formatFightTime`
    - `lib/analysis-engine.ts` exports `JUNK_SPELL_IDS` and `isJunkSpell`
    - `npx tsc --noEmit` exits 0 and `npm run lint` reports no new findings
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npx vitest run lib/__fixtures__/fixtures.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" or "No test files found" in the vitest summary line</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; grep -q 'TIMELINE_CASTS_QUERY' lib/wcl-queries.ts && grep -q 'TIMELINE_CASTS_PAGE_QUERY' lib/wcl-queries.ts && [ "$(grep -c 'healingByPlayer:' lib/wcl-queries.ts)" = "2" ] && [ "$(grep -c 'healing: table(' lib/wcl-queries.ts)" -ge 3 ]</automated>
    <fails_when>non-zero exit — a timeline query name is absent, the healingByPlayer count is not exactly 2, or fewer than three original healing table fields remain</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; for t in CastTimelineResult TimelineRow TimelineAbilityCount WCLCastEvent TimelineRequest HealerTableRow HealerMetricsComputed HealerComparison; do grep -qE "export (interface|type) $t\b" lib/wcl-types.ts || { echo "MISSING TYPE: $t"; exit 1; }; done; grep -q 'healer?: HealerComparison' lib/wcl-types.ts && grep -q 'healerRow?: HealerTableRow' lib/wcl-types.ts && node -e 'const s=require("fs").readFileSync("lib/constants.ts","utf8");process.exit(/RATE_LIMITS[\s\S]*?timeline:\s*\d+/.test(s)?0:1)'</automated>
    <fails_when>non-zero exit, or a "MISSING TYPE" line naming a contract that was not exported</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; grep -q 'export function formatFightTime' lib/utils.ts && grep -q 'export const JUNK_SPELL_IDS' lib/analysis-engine.ts && grep -q 'export function isJunkSpell' lib/analysis-engine.ts</automated>
    <fails_when>non-zero exit — the shared fight-time formatter is not exported from lib/utils.ts, or one of the two junk-spell symbols is still module-private in lib/analysis-engine.ts</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npx tsc --noEmit && npm run lint</automated>
    <fails_when>non-zero exit, or any eslint finding outside the two pre-existing debt files components/ui/meteors.tsx and lib/analysis-engine.ts</fails_when>
  </verify>
  <done>The fixture shape test passes, both timeline queries and both `healingByPlayer` fields exist, all eight new types plus the optional `AnalysisResult.healer` field are exported, the timeline rate-limit bucket is explicit, `formatFightTime` and the junk-spell filter are shared exports, and typecheck plus lint are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel production env -> local filesystem | Live WCL credentials cross into the developer machine for the recording run |
| WCL API -> committed repository | Third-party response data becomes permanent, world-readable repository content |
| Recorded fixture -> test suite | Fixture content is trusted input to every engine test written in 02-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `scripts/record-wcl-fixtures.mjs` and the pulled Vercel env file | high | mitigate | The recorder never prints or writes credential material; the pulled env file lands only in the session scratchpad and is removed with `rm -f` in the same task; a negative grep over `lib/__fixtures__/` for credential and token key names is a Task 1 acceptance gate |
| T-02-02 | Information Disclosure | committed fixtures under `lib/__fixtures__/` | medium | mitigate | Recording is restricted to the public demo report `ZjKgNYxVcAqR8pGJ`; the prohibition in `must_haves` plus the Task 1 `human-check` confirm public visibility before commit; a healer source is resolved from that same public report rather than substituted from elsewhere |
| T-02-SC-01 | Tampering | npm / pip / cargo installs | high | accept | This plan installs no packages. RESEARCH.md `## Package Legitimacy Audit` records that no new external package is introduced anywhere in Phase 2 (virtualisation is hand-rolled), so no legitimacy checkpoint is required. Any executor that finds it needs a package must stop and request an audit before installing |
| T-02-SC-02 | Tampering | WCL response data treated as trusted test input | low | accept | Fixtures come from an API the product already depends on at runtime; `fixtures.test.ts` asserts shape rather than trusting it silently, so a malformed or truncated recording fails the suite instead of quietly weakening later engine tests |
</threat_model>

## Artifacts this phase produces

Symbols and files created or extended by **this plan** (new-symbol exclusions for source-grounding):

| Kind | Symbol / path |
|------|---------------|
| new file | `scripts/record-wcl-fixtures.mjs` |
| new file | `lib/__fixtures__/README.md` |
| new files | `lib/__fixtures__/demo-player-dps.json`, `demo-player-healer.json`, `demo-raid-overview.json`, `demo-raid-combatant-info.json`, `demo-raid-death-events.json`, `demo-timeline-casts.json` |
| new file | `lib/__fixtures__/fixtures.test.ts` |
| new export | `TIMELINE_CASTS_QUERY` (`lib/wcl-queries.ts`) |
| new export | `TIMELINE_CASTS_PAGE_QUERY` (`lib/wcl-queries.ts`) |
| new query field | `healingByPlayer` on `PLAYER_FULL_DATA_QUERY_HEALING` and `TOP_PLAYER_DATA_QUERY_HEALING` |
| new type | `WCLCastEvent`, `TimelineRowKind`, `TimelineRow`, `TimelineAbilityCount`, `CastTimelineResult`, `TimelineRequest` (`lib/wcl-types.ts`) |
| new type | `HealerTableRow`, `HealerMetricsComputed`, `HealerComparison` (`lib/wcl-types.ts`) |
| new field | `AnalysisResult.healer`, `TopPlayerFullData.healerRow` (`lib/wcl-types.ts`) |
| new config key | `RATE_LIMITS.timeline` (`lib/constants.ts`) |
| new export | `formatFightTime` (`lib/utils.ts`) |
| newly exported | `JUNK_SPELL_IDS`, `isJunkSpell` (`lib/analysis-engine.ts`, previously module-private) |
| new npm script | `record-fixtures` (`package.json`) |

## Flagged Assumptions (spec-less probe fallback)

Phase 2 has no SPEC.md, so the deterministic edge probe ran over the phase requirement texts and
returned four rows, every one `unclassified -> unresolved`. Under the spec-less fallback protocol an
unresolved row is **never** auto-resolved and **never** silently dropped — it is surfaced as an
explicit planner assumption. This is the canonical table for the phase; each downstream plan repeats
the row it owns.

| Requirement | Probe verdict | Flagged assumption carried into the plan set |
|---|---|---|
| ACC-01 | unclassified -> unresolved | Assumed that "traces to a wago.tools-regenerated data file" is satisfied when every ID-to-name row is emitted by `scripts/regen-game-data.mjs` and every non-derivable row is an explicitly source-noted override marked unverified. Planned in 02-03 and 02-06. |
| ACC-02 | unclassified -> unresolved | Assumed that "tests that fail when analysis output changes" means a per-engine full-output snapshot plus named behavioural assertions, with no coverage-percentage threshold (D-14, D-16). Planned in 02-08. |
| ACC-03 | unclassified -> unresolved | Assumed that "read what that player actually cast, in order" is satisfied by a chronological vertical log of the player's non-junk casts within the recorded page cap, with truncation disclosed. Planned in 02-02 and 02-05. |
| ACC-04 | unclassified -> unresolved | Assumed that "judged on healer-relevant metrics" means effective HPS as the ranking basis plus overheal percent and healing uptime shown against top-healer values, and nothing beyond those three metrics (D-05). Planned in 02-04 and 02-07. |

No-silent-drop equality for this phase: **4 probe-surfaced items == 0 authored into `must_haves` + 4
surfaced as flagged assumptions.**

Prohibition-probe canon referral (not minted): WCL credential handling and secret hygiene are canon
security concerns covered by `/gsd-secure-phase` and the standing `CLAUDE.md` secrets rule; they are
enforced here through `T-02-01` rather than as a minted prohibition.

<verification>
- `npx vitest run lib/__fixtures__/fixtures.test.ts` passes.
- `npm test` (full suite) still passes — the existing seven test files are untouched.
- `npx tsc --noEmit` and `npm run lint` are clean.
- Six non-empty fixtures and a README answering A1-A5 exist under `lib/__fixtures__/`.
- No credential or token material appears anywhere under `lib/__fixtures__/`, and no pulled env file remains on disk.
</verification>

<success_criteria>
Plans 02-02, 02-04, 02-05 and 02-07 can be executed without any further live WCL query: the response
shapes are recorded, the queries that produce them are exported, the TypeScript contracts over them
are declared, and the timeline route's rate-limit bucket exists before the route does.
</success_criteria>

<output>
Create `.planning/phases/02-accuracy-analysis-depth/02-01-SUMMARY.md` when done. Record, verbatim,
the A1-A5 findings — later plans depend on them.
</output>
