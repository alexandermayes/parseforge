---
phase: 04-ads-live
plan: 07
type: execute
wave: 5
depends_on: ["04-01", "04-06"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
  - .planning/research/rpglogs-approval-request-2026-09-19.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MONY-02, MONY-03, R0-1]

user_setup:
  - service: rpglogs-warcraft-logs
    why: "D-12: the production deploy that turns ads on is gated on RPGLogs' written approval, recorded verbatim. The reply arrives in the operator mailbox; only a human can read it and confirm the sending address."
    dashboard_config:
      - task: "Check the operator mailbox for a reply to the approval request sent in 04-01, and paste the reply verbatim into the Thread table"
        location: "info@lootlistplus.com"
  - service: vercel
    why: "The production deploy is a manual CLI action on this project and must be confirmed by the developer each time (CLAUDE.md)."
    env_vars:
      - name: (none — uses the existing personal CLI login)
        source: "~/.vercel-personal; every command needs --global-config ~/.vercel-personal --scope loot-list-plus"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ads are live in production only after RPGLogs' written approval is recorded verbatim in the Thread table; silence was never treated as approval (R0-1, D-12, D-14)."
    - "The production deploy happened under an explicit, recorded developer approval given at the time of the deploy, not inherited from an earlier session (CLAUDE.md)."
    - "After the deploy, the four reserved boxes render on the two whitelisted routes in production, `/ads.txt` serves its declared publisher line, every canonical is still param-free, and `npm run protected-elements` is green against production (MONY-02, SC1, SC2)."
    - "PostHog counts real ad_slot events from live production traffic, not merely event definitions, and the OPS-01 item 7 live-traffic thresholds are scored with counted evidence (SC5)."
    - "Search Console reports no indexability or metadata change on the two ad routes attributable to this deploy (SC5)."
    - "Part 6 records the day-2 and day-7 CWV re-read obligations against the pre-ad baseline, with dates, and REQUIREMENTS.md carries a dated addendum stating exactly which parts of MONY-02 and MONY-03 are met and which remain open (MONY-03, D-11)."
    - statement: "AdSense reports revenue on live traffic — readable only from the AdSense dashboard, and only after enough impressions have accrued."
      verification: backstop
    - statement: "CLS, LCP and INP p75 after 7 days of live ads stay inside the D-10 rollback trigger on all three routes — measurable only after the observation window elapses."
      verification: backstop
  artifacts:
    - "docs/OPS-01-SHIP-GATE.md Part 6: production deploy, post-deploy route contracts, item 7 counted result, Search Console rows, the sign-off row, and the two scheduled re-reads"
    - ".planning/research/rpglogs-approval-request-2026-09-19.md (reply recorded verbatim, or the day-14 escalation recorded)"
    - ".planning/REQUIREMENTS.md (dated Phase 4 addendum)"
  key_links:
    - "The Thread table's reply row is the only thing that unblocks the deploy. If it is empty at the day-14 deadline, the decision checkpoint presents D-14's three options and nothing ships by default."
    - "Part 6's pre-ad baseline table (written in 04-01 before any ad code existed) is the only reference the day-2 and day-7 comparisons are scored against."
    - "OPS-01 item 7's zero-events-is-a-FAIL rule applies to the ad_slot family exactly as it applies to every other event — an event definition's existence is not proof of current ingestion."
  prohibitions:
    - "Ads must not be enabled in production before RPGLogs' written approval is recorded verbatim in the Thread table; non-response must never be read as approval."
    - "The D-10 rollback numbers must not be relaxed, re-negotiated or re-scoped after a breach is observed; a CLS p75 above 0.1 on any route triggers the AdSense pause without waiting for a reply."
    - "No gate may be softened to let this deploy be signed off: an unreadable figure is recorded as no-data or not-evaluable with its re-measure test, never as a pass, and never omitted."
    - "No ad_slot event may carry a report code, player name, character name, server or full URL."
  flagged_assumptions:
    - "Both phase requirement rows came back unclassified from the edge probe and stay unresolved. The deploy-time edges this plan assumes are: (a) the live-traffic window landing on a quiet UTC hour, which this project has hit three times before and which is handled by re-measuring on a busier hour rather than by lowering the threshold; (b) an ad-blocked share of traffic producing ad_slot_blocked events that must not be read as a gate failure; and (c) Search Console having no data yet for a route, which is recorded as no-data and never as a pass."
    - "04-RESEARCH.md Open Question 3: the reply's timing is entirely external and cannot be scheduled around. Every other plan in this phase was built to be complete and independently valuable without it."
    - "Historical: three previous OPS-01 item 7 runs on this project produced FAIL or NOT EVALUABLE rows on quiet UTC hours. Plan the measurement window accordingly and record the window boundaries explicitly."
---

<objective>
Turn ads on in production — but only behind the two gates this phase exists to respect: RPGLogs'
written approval, and the developer's explicit, at-the-time deploy confirmation. Then prove on live
traffic that the analyzer did not get worse, and record honestly what is still open.

**User story (MVP framing):** **As a** ParseForge visitor, **I want to** paste a Warcraft Logs URL and
get the same fast, accurate analysis I always did, **so that** the ads now funding the site are
something I can ignore rather than something I have to work around.

Purpose: `PARSEFORGE-RANKINGS-SPEC.md` §2.1 makes advertising a commercial use of the Warcraft Logs
API that needs prior written approval. Shipping ads without it is a Terms breach against the API the
entire product depends on — which is why this is the one task in the phase that cannot be automated
past, and why D-12 rates it one-way.
Output: a production deploy (or a recorded, reasoned decision not to deploy), the post-deploy OPS-01
evidence, and a dated REQUIREMENTS.md addendum.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ads-live/04-CONTEXT.md
@.planning/phases/04-ads-live/04-01-SUMMARY.md
@.planning/phases/04-ads-live/04-06-SUMMARY.md
@.planning/research/rpglogs-approval-request-2026-09-19.md
@docs/OPS-01-SHIP-GATE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: The one-way gate — does the recorded RPGLogs reply authorize turning ads on?</name>
  <files>.planning/research/rpglogs-approval-request-2026-09-19.md, docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - .planning/research/rpglogs-approval-request-2026-09-19.md — the Thread table as it stands, including the sent row written in 04-01 and any reply rows added since
    - .planning/phases/04-ads-live/04-CONTEXT.md D-12 and D-14 — the deploy is gated on the reply, and decline or fourteen days of silence escalates with options rather than shipping by default
    - .planning/research/PARSEFORGE-RANKINGS-SPEC.md §2.1 — the clause that makes advertising commercial use, and the consequence column for ParseForge
    - docs/OPS-01-SHIP-GATE.md Part 6 `### Observation schedule and escalation clocks` — the dated day-14 deadline recorded in 04-01
    - .planning/phases/04-ads-live/04-06-SUMMARY.md — whether the preview evidence is complete, which is the other precondition for shipping
  </read_first>
  <action>
Before presenting anything, do the readable work: check the operator mailbox for a reply to the
request sent in 04-01, and if one has arrived, paste it into the Thread table verbatim — the exact
text, the sending address and the UTC date — as a new row with Direction `received`. Do not
summarize it in the Verbatim copy column. Then compute today's date against the recorded day-14
deadline and state which side of it we are on.

Then present the decision with the actual text in front of the developer, never a characterization of
it. Do not select an option; this checkpoint exists because the default answer would be wrong to
assume.
  </action>
  <decision>Does the recorded RPGLogs correspondence authorize turning AdSense on in production for parseforge.gg today?</decision>
  <context>
    Everything else in Phase 4 is built, preview-verified and ready: four reserved slots, the
    consent-gated loader, /ads.txt, the CSP entries, the privacy disclosure, the AdSense account
    configuration and the pre-ad CWV baseline. The only thing standing between that and production is
    this reply. The RPGLogs API Terms say use is commercial if it earns money including through
    advertising, and that commercial use needs their approval — so shipping ads without a recorded
    yes is a Terms breach against the API ParseForge entirely depends on. D-12 rates the deploy
    one-way: reversing it means pulling ads down, not un-becoming a commercial API user.
  </context>
  <options>
    <option id="approved">
      <name>Approval recorded — deploy</name>
      <pros>The Terms condition is met with verbatim evidence in the Thread table; the phase completes as designed and the revenue experiment starts collecting the data the D-11 window needs.</pros>
      <cons>One-way: from the moment it is live, ParseForge is a commercial API user. Any attribution or volume condition stated in the reply must be implemented before or with the deploy, not after.</cons>
    </option>
    <option id="conditional">
      <name>Approval recorded with conditions — implement the conditions first, then deploy</name>
      <pros>Honours exactly what RPGLogs asked for (attribution text, a logo, a request or user cap) rather than assuming a bare yes; keeps the relationship intact.</pros>
      <cons>Adds a gap-closure plan before the deploy; the phase does not complete this session.</cons>
    </option>
    <option id="declined">
      <name>Declined — do not deploy</name>
      <pros>No Terms breach. The built work is preserved behind the env flag and costs nothing while off.</pros>
      <cons>No ad revenue. Monetization must be re-planned; CONTEXT.md's deferred non-ad monetization spike becomes the live option.</cons>
    </option>
    <option id="silent-before-14">
      <name>No reply yet and the day-14 deadline has not passed — wait</name>
      <pros>Non-response is not approval; waiting costs nothing and keeps the phase honest.</pros>
      <cons>The phase stays open. Record the current date and the remaining days so the next session knows where the clock is.</cons>
    </option>
    <option id="silent-at-14">
      <name>No reply and the day-14 deadline has passed — escalate per D-14</name>
      <pros>Forces a deliberate choice rather than drift: send a nudge, mark Phase 4 blocked-external and re-plan monetization, or record an explicit risk acceptance with the developer's name on it.</pros>
      <cons>Each sub-option has its own cost; none of them is "ship quietly and see".</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: approved, conditional, declined, silent-before-14, silent-at-14. For `conditional`, quote the condition. For `silent-at-14`, also say which D-14 sub-option to take: nudge, blocked-external, or recorded risk acceptance.</resume-signal>
  <acceptance_criteria>
    - The Thread table contains either a row with Direction `received` carrying the reply verbatim, or an explicit dated row recording that no reply has arrived as of that date.
    - Part 6 records the decision taken, its date, and the developer's exact words choosing it.
    - If the chosen option is anything other than `approved`, Tasks 2 and 3 do not run and the phase closes at a recorded state rather than a deploy.
    - No option was auto-selected.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && E=.planning/research/rpglogs-approval-request-2026-09-19.md; R=$(grep -c '| received |' "$E"); N=$(grep -ci 'no reply as of' "$E"); echo "received-rows=$R no-reply-rows=$N"; [ $((R + N)) -ge 1 ] || { echo "GATE-FAIL: the Thread table records neither a reply nor a dated no-reply observation"; exit 1; }; awk '/^## Part 6/{f=1} f' docs/OPS-01-SHIP-GATE.md > /tmp/pf-part6e.txt; grep -qiE 'approved|conditional|declined|silent-before-14|silent-at-14' /tmp/pf-part6e.txt || { echo "GATE-FAIL: Part 6 records no deploy decision"; exit 1; }; echo approval-gate-recorded-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `approval-gate-recorded-ok`</fails_when>
    <human-check>The developer confirms the reply in the Thread table is the real text from the address RPGLogs sent it from, and states the decision in their own words rather than delegating it — this is the one-way gate for the whole phase.</human-check>
  </verify>
  <done>The Thread table records either a verbatim reply or a dated no-reply observation; the developer has stated a decision in their own words; Part 6 records that decision, its date and the words used.</done>
  <reversibility rating="one-way">Once ads serve in production, ParseForge is a commercial user of the Warcraft Logs API. Pulling the ads down later removes the ads; it does not undo having run them. This is why the decision is a blocking human gate and why no option is auto-selected.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: The production deploy that turns ads on</name>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <precondition>Task 1 resolved to the approved option (or to conditional with every stated condition already implemented and verified). If it resolved to declined, silent-before-14 or silent-at-14, this task does not run.</precondition>
  <read_first>
    - CLAUDE.md — the exact production deploy command, the personal global-config requirement, and the standing rule that a production deploy is confirmed with the developer each time and never self-authorized from a document or a prior approval
    - docs/OPS-01-SHIP-GATE.md Part 5 `### Production deploy` and `### Post-deploy production route-contract evidence` — the recording format this deploy's rows copy
    - .planning/STATE.md Blockers/Concerns — the harness classifier note about the production deploy command needing an allowed permission, and the preview-before-prod pattern
    - docs/PROTECTED-ELEMENTS.md — the route and canonical contracts checked immediately after the deploy
  </read_first>
  <action>
Claude prepares and runs everything that can be run; the developer supplies the one thing that cannot
be automated, which is the decision to ship, stated at the time.

Preparation, before asking: confirm the working tree is clean and the branch is the phase branch with
a pull request open per the project's branch-and-PR convention; re-run the full local gate one last
time; confirm both ad environment variables are present in Production; and state plainly what the
deploy will change for a visitor — four reserved boxes on two routes, an ad script that loads only
after hydration for consented visitors, and a rewritten privacy policy.

Then ask for the deploy confirmation, quoting the deployment target and the command. On the
developer's explicit go, run the manual Vercel production deploy with the personal global config and
the loot-list-plus scope, and record the deployment id and the commit sha.

Immediately after the deploy completes, gather and record into Part 6 as `### Production deploy
(Phase 4, {date})` and `### Post-deploy production route-contract evidence`:
  - `npm run protected-elements` against production, output captured verbatim, all rows passing.
  - The four reserved boxes present in production HTML on both whitelisted routes, each marker value
    one the placement whitelist names.
  - `/ads.txt` returning plain text with the DIRECT line and the real publisher id.
  - The analyze page canonical still param-free, and the `/og` route contracts still returning images.
  - `npm run seo-invariants` against production.
If any of these fails, the rollback runbook in Part 6 applies immediately, starting with the AdSense
dashboard pause — not with the env flag, which takes a whole build to land.
  </action>
  <instructions>
    Claude has already: re-run the full local gate, confirmed the production environment variables
    exist, and prepared the exact deploy command. The one thing needing you is the go-ahead to ship
    to production — the project's standing rule is that a production deploy is confirmed by you at
    the time, never inherited from an earlier session or from an approval recorded in a document.
  </instructions>
  <verification>After the deploy, Claude runs the protected-elements gate, the seo-invariants diff and the route-contract fetches against production and records every result verbatim.</verification>
  <resume-signal>Say "deploy" to ship to production, or "hold" to stop here with everything recorded.</resume-signal>
  <acceptance_criteria>
    - Part 6 contains a `### Production deploy (Phase 4` subsection naming the deployment id and the commit sha.
    - It records the developer's verbatim go-ahead and its timestamp.
    - `npm run protected-elements` output against production is captured with every row passing.
    - Production HTML for both whitelisted routes contains all four reserved boxes; `/ads.txt` returns plain text containing DIRECT.
    - The analyze page canonical in production contains no question mark.
    - `npm run seo-invariants` reports no unexplained diff on the two ad routes or on `/privacy`.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npm run protected-elements</automated>
    <fails_when>non-zero exit, or a trailing summary line reporting any failed check</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && B=https://parseforge.gg; CODE=$(node -e 'const s=require("fs").readFileSync("lib/demo-report.ts","utf8");console.log(s.match(/code:\s*"([^"]+)"/)[1])'); curl -s "$B/tbc-audit" > /tmp/pf-prod-tbc.html; curl -s "$B/analyze/$CODE" > /tmp/pf-prod-analyze.html; ADS=$(curl -s -o /tmp/pf-prod-ads.txt -w '%{http_code}:%{content_type}' "$B/ads.txt"); T=$(grep -c 'data-ad-slot=' /tmp/pf-prod-tbc.html); A=$(grep -c 'data-ad-slot=' /tmp/pf-prod-analyze.html); Q=$(grep -o 'rel="canonical" href="[^"]*"' /tmp/pf-prod-analyze.html | grep -c '?'); echo "tbc-boxes=$T analyze-boxes=$A ads-txt=$ADS canonical-with-query=$Q"; [ "$T" -eq 2 ] || { echo "GATE-FAIL: expected 2 reserved boxes on the production raid-audit page, found $T"; exit 1; }; [ "$A" -ge 1 ] || { echo "GATE-FAIL: no reserved box on the production analyze page"; exit 1; }; case "$ADS" in 200:text/plain*) ;; *) echo "GATE-FAIL: /ads.txt did not serve plain text in production"; exit 1;; esac; grep -q 'DIRECT' /tmp/pf-prod-ads.txt || { echo "GATE-FAIL: the production ads.txt declares no DIRECT relationship"; exit 1; }; [ "$Q" -eq 0 ] || { echo "GATE-FAIL: the analyze canonical carries a query string - the SEO invariant broke"; exit 1; }; for SLOT in $(grep -o 'data-ad-slot="[^"]*"' /tmp/pf-prod-tbc.html /tmp/pf-prod-analyze.html | sed 's/.*data-ad-slot="//;s/"//' | sort -u); do grep -qF "$SLOT" docs/PROTECTED-ELEMENTS.md || { echo "GATE-FAIL: production serves an ad slot the whitelist does not name: $SLOT"; exit 1; }; done; echo prod-contracts-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `prod-contracts-ok`</fails_when>
  </verify>
  <done>Ads are live in production under a recorded, at-the-time developer go-ahead; the deployment id and commit sha are recorded; the placement gate, the reserved boxes, /ads.txt, the param-free canonical and the SEO invariants are all confirmed against production with captured output.</done>
  <reversibility rating="one-way">This is the deploy D-12 rates one-way. The rollback runbook in Part 6 exists precisely because reversing it is a pause-and-pull, not an undo.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Post-deploy OPS-01 evidence, the sign-off row, and the two clocks that stay open</name>
  <files>docs/OPS-01-SHIP-GATE.md, .planning/REQUIREMENTS.md</files>
  <precondition>Task 2 deployed to production; at least sixty minutes of live traffic have accrued since the deploy, so item 7's window can be measured rather than estimated.</precondition>
  <read_first>
    - docs/OPS-01-SHIP-GATE.md Part 1 items 4 to 8 — especially item 7's three live-traffic thresholds, the zero-events-is-a-FAIL rule, and the explicit statement that an event definition's presence is not proof of current ingestion
    - docs/OPS-01-SHIP-GATE.md Part 5 `### Item 7 — counted result (2026-09-16, 03-07 final continuation)` and `### Item 7 — post-deploy live-traffic check: PENDING` — how a counted result and an honestly-open result are each recorded, and the quiet-UTC-hour problem that produced three FAIL rows on this project
    - docs/OPS-01-SHIP-GATE.md Part 6 as written by 04-01 — the pre-ad baseline table and the rollback trigger these readings are scored against
    - .planning/REQUIREMENTS.md — the MONY-01 addendum's format, which the Phase 4 addendum copies
    - .planning/phases/04-ads-live/04-CONTEXT.md D-10, D-11
  </read_first>
  <action>
Four recordings, all appended to Part 6, plus one requirements addendum.

1. **Item 7, the live-traffic check.** Run the three OPS-01 item 7 threshold queries against PostHog
   project 337485 for a full sixty-minute window on the busiest available UTC hour since the deploy —
   this project has produced a FAIL or NOT EVALUABLE row three separate times by measuring a quiet
   hour, and the response is a busier window, never a lower threshold. Record the exact window
   boundaries, the counted numbers and the pass or fail per threshold. Add a fourth, phase-specific
   row: the counted number of `ad_slot_requested`, `ad_slot_filled`, `ad_slot_empty` and
   `ad_slot_blocked` events in the same window, broken down by `consent_gate_path`. Zero ad_slot
   events across the whole window is a FAIL under the same rule that governs every other event here.
   Assert from the same query that no ad_slot event carries any property beyond the three allowed.

2. **Search Console.** URL-inspect `/tbc-audit`, the demo analyze page and `/privacy`, and record
   indexability, canonical and coverage state per route. A route Search Console has no data for is
   recorded as no-data — never as a pass, never dropped. Note explicitly that a ranking read this
   soon after a deploy is not meaningful and that the SC5 ranking-movement claim is a later reading,
   scheduled below.

3. **CSP report-only violations from production.** Collect the distinct blocked hosts now visible on
   real traffic and compare them against the preview harvest from 04-06. Record any Google ad host
   still missing from `next.config.ts` as a follow-up for Phase 7's promotion work; the policy stays
   report-only, so nothing is broken by a missing host today.

4. **The two clocks, and the sign-off row.** Record the dated day-2 and day-7 CWV re-read
   obligations: the same three `vercel metrics` commands 04-01 used, compared against Part 6's own
   baseline rows, scored against the three D-10 numbers, with the pre-agreed automatic AdSense pause
   for a CLS breach restated. Then write the Phase 4 sign-off row in the same shape Parts 2 to 5 use:
   one row per gate item with its counted evidence, and the signature line stating either SIGNED with
   the date or explicitly NOT SIGNED with exactly which rows are open and what closes them. A row
   whose figure could not be read is `no-data` or `not evaluable` with its re-measure test — never a
   softened pass.

Finally, append a dated Phase 4 addendum to `.planning/REQUIREMENTS.md` in the same format as the
existing MONY-01 addendum, stating precisely which half of MONY-02 and MONY-03 is met by counted
evidence and which remains open: the ad-serving and placement half of MONY-02 is closed by this
deploy's evidence, and MONY-03's monitoring half stays open until the day-7 read appends its
confirmation. Do not mark either requirement complete on the strength of the deploy alone.
  </action>
  <acceptance_criteria>
    - Part 6 contains an item 7 subsection with explicit window boundaries, three counted threshold results and a fourth ad_slot event-count row broken down by consent gate path.
    - Every ad_slot event counted carries only the three allowed properties.
    - Part 6 contains a Search Console row for `/tbc-audit`, the demo analyze page and `/privacy`, with `no-data` used where applicable.
    - Part 6 contains a production CSP violation host list, or an explicit statement that none were observed.
    - Part 6 contains dated day-2 and day-7 CWV re-read obligations naming the exact commands and the three D-10 numbers.
    - Part 6 contains a Phase 4 sign-off row that is either SIGNED with a date or NOT SIGNED with the open rows named.
    - `.planning/REQUIREMENTS.md` gained a dated Phase 4 addendum naming what is met and what is open for MONY-02 and MONY-03.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && awk '/^## Part 6/{f=1} f' docs/OPS-01-SHIP-GATE.md > /tmp/pf-part6f.txt; for TOK in 'Item 7' 'consent_gate_path' 'ad_slot_requested' 'Search Console' 'day-2' 'day-7'; do N=$(grep -ciF -- "$TOK" /tmp/pf-part6f.txt); echo "[$TOK]=$N"; [ "$N" -ge 1 ] || { echo "GATE-FAIL: Part 6 is missing $TOK"; exit 1; }; done; S=$(grep -ciE 'SIGNED|NOT SIGNED' /tmp/pf-part6f.txt); echo "sign-off-lines=$S"; [ "$S" -ge 1 ] || { echo "GATE-FAIL: Part 6 has no Phase 4 sign-off row"; exit 1; }; for R in '/tbc-audit' '/privacy'; do grep -qF "$R" /tmp/pf-part6f.txt || { echo "GATE-FAIL: no Search Console row for $R"; exit 1; }; done; A=$(grep -ciE 'Addendum|addendum' .planning/REQUIREMENTS.md); Y=$(date -u +%Y); grep -q "MONY-02" .planning/REQUIREMENTS.md || { echo "GATE-FAIL: MONY-02 vanished from REQUIREMENTS.md"; exit 1; }; echo "requirements-addenda=$A"; [ "$A" -ge 2 ] || { echo "GATE-FAIL: no new dated addendum was appended to REQUIREMENTS.md"; exit 1; }; echo post-deploy-evidence-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `post-deploy-evidence-ok`</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && vercel metrics vercel.speed_insights.cls --aggregation p75 --group-by route --since 1d --project parseforge --prod --global-config ~/.vercel-personal --scope loot-list-plus > /tmp/pf-cls-postship.txt 2>&1 || { echo "GATE-FAIL: the post-ship metrics read did not succeed"; cat /tmp/pf-cls-postship.txt; exit 1; }; cat /tmp/pf-cls-postship.txt; grep -q 'tbc-audit' /tmp/pf-cls-postship.txt || { echo "GATE-FAIL: no /tbc-audit row in the post-ship CLS read - the day-2 comparison cannot be scored from it"; exit 1; }; echo postship-metrics-readable-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `postship-metrics-readable-ok`</fails_when>
    <human-check>The developer reads the item 7 counted numbers and the first post-ship CLS figure and confirms whether the deploy should stand or the AdSense pause should be taken now — with the standing exception that a CLS p75 above 0.1 on any route is paused immediately without waiting for this confirmation.</human-check>
  </verify>
  <done>Part 6 carries a counted item 7 result with an ad_slot breakdown by consent gate path, per-route Search Console rows with no-data used honestly, the production CSP violation list, the dated day-2 and day-7 re-read obligations, and a sign-off row that is either signed with evidence or explicitly unsigned with its open rows named; REQUIREMENTS.md carries a dated Phase 4 addendum that marks nothing complete on the strength of the deploy alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPGLogs reply → the deploy decision | an email is the sole authorization for a one-way change to the product's legal posture |
| local shell → production | a manual CLI deploy changes what every visitor is served |
| PostHog / Search Console / Vercel readings → a committed sign-off | numbers read from third-party dashboards become permanent evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-33 | Spoofing | a paraphrased, forwarded or assumed approval standing in for a real reply | high | mitigate | The Thread table requires the verbatim text and the sending address; the decision checkpoint is `gate="blocking-human"` so no mode can auto-select it; the developer confirms the address first-hand |
| T-04-34 | Elevation of Privilege | a production deploy self-authorized from a document or a prior session | high | mitigate | The deploy is a `checkpoint:human-action` requiring an at-the-time go-ahead recorded verbatim, per CLAUDE.md's standing rule |
| T-04-35 | Repudiation | signing the gate on unreadable or absent figures | high | mitigate | Every row is counted evidence or an explicit no-data / not-evaluable with its re-measure test; the quiet-hour failure mode is answered with a busier window, never a lowered threshold |
| T-04-36 | Information Disclosure | a live ad_slot event carrying a report code or player name to PostHog | high | mitigate | Item 7's phase-specific row asserts from the live query that no ad_slot event carries a property beyond the three allowed — a source grep proved intent pre-deploy, this proves behaviour post-deploy |
| T-04-37 | Denial of Service | a CLS regression degrading the core flow for days before anyone looks | high | mitigate | Dated day-2 and day-7 re-read obligations against the pre-ad baseline, with the pre-agreed automatic AdSense pause for a CLS breach requiring no reply |
| T-04-38 | Tampering | a Google ad host missing from the report-only policy hiding a real violation | low | accept | The policy is report-only; a missing host blocks nothing today and the production harvest feeds Phase 7's promotion work |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks in this plan or this phase (04-RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies). Any later addition must pass `package-legitimacy check` plus a blocking human checkpoint before install; `[SLOP]` packages are forbidden |
</threat_model>

<artifacts_this_phase_produces>
Created by this plan:
- `docs/OPS-01-SHIP-GATE.md` `## Part 6` subsections: `### Deploy decision (Phase 4, {date})`,
  `### Production deploy (Phase 4, {date})`, `### Post-deploy production route-contract evidence`,
  `### Item 7 — counted result (Phase 4)`, `### Search Console (Phase 4)`,
  `### CSP report-only violations (production, Phase 4)`,
  `### Scheduled re-reads (day-2 and day-7 CWV)`, and the Phase 4 sign-off row
- `.planning/research/rpglogs-approval-request-2026-09-19.md` — a `received` Thread row carrying the
  reply verbatim, or a dated no-reply observation
- `.planning/REQUIREMENTS.md` — a dated Phase 4 addendum for MONY-02 and MONY-03
</artifacts_this_phase_produces>

<verification>
- The Thread table records a verbatim reply or a dated no-reply observation; no option was auto-selected at the decision checkpoint.
- If deployed: production serves four reserved boxes on two routes, a valid `/ads.txt`, a param-free analyze canonical, and a green protected-elements run.
- Item 7 is scored with counted numbers over a stated sixty-minute window, including an ad_slot breakdown by consent gate path.
- Part 6's sign-off row is either signed with evidence or explicitly unsigned with its open rows and their closing tests named.
- REQUIREMENTS.md marks nothing complete that the deploy alone does not prove.
</verification>

<success_criteria>
- SC3 and SC5 have production evidence: ads load only for consented visitors (counted by gate path), PostHog ingests the ad_slot family on live traffic, and Search Console shows no indexability change on the two ad routes.
- SC4's second half is scheduled and dated rather than assumed: the day-2 and day-7 reads are recorded as obligations against the pre-ad baseline with the rollback numbers quoted.
- R0-1's gating purpose is honoured: ads are live only because a written approval exists, or they are not live and the reason is recorded.
</success_criteria>

<output>
Create `.planning/phases/04-ads-live/04-07-SUMMARY.md` when done.
</output>
