---
phase: 04-ads-live
plan: 04
type: tdd
wave: 2
depends_on: ["04-02"]
files_modified:
  - lib/wcl-types.ts
  - lib/rankings/parse-lens.ts
  - lib/rankings/parse-lens.test.ts
  - lib/rankings/budget.ts
  - lib/rankings/budget.test.ts
autonomous: true
requirements: [R0-3]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "lib/wcl-types.ts declares a new type for the per-report rankings blob that is distinct from the existing WCLRanking boss-leaderboard interface, and its field names are verified against lib/__fixtures__/rankings-report.json rather than against any document (R0-3)."
    - "lib/rankings/parse-lens.ts turns the recorded rankings blob into per-player parse rows and per-fight badges as a pure function with no I/O and no throwing, in the style of lib/analysis-engine.ts (R0-3)."
    - "A character marked hidden, or listed in the blob's character blacklist, is surfaced as such rather than silently included or silently dropped (R0-3)."
    - "lib/rankings/budget.ts runtime-validates the three rate-limit numbers before trusting them, persists the budget state through the existing lib/kv-cache.ts Redis-with-Map-fallback helpers, and refuses at 90% of the runtime-read hourly limit (R0-3)."
    - "The hourly limit is read from the response at runtime and never hard-coded — the spec records it changing mid-session."
    - "Nothing in this plan is imported by any route, page, component or API handler; `npm run protected-elements` and `npm run seo-invariants` are unchanged because no user-facing surface moved (D-13)."
  artifacts:
    - lib/rankings/parse-lens.ts
    - lib/rankings/parse-lens.test.ts
    - lib/rankings/budget.ts
    - lib/rankings/budget.test.ts
    - "lib/wcl-types.ts (new rankings-blob types)"
  key_links:
    - "lib/__fixtures__/rankings-report.json -> the new types -> parse-lens. If the types are written from PARSEFORGE-RANKINGS-SPEC.md §3.3 instead of the fixture, R1 ships a lens that reads fields WCL does not return."
    - "lib/__fixtures__/rankings-ratelimit.json -> parseRateLimitData -> lib/kv-cache.ts cacheSet. The budget reader must reuse the existing cache helpers, not re-implement a Redis client."
    - "The existing inline rankings type at app/api/analyze/route.ts (which reads only the partition field) must keep working exactly as it does — R0-3 adds types and an engine, it rewires nothing."
  prohibitions:
    - "The new rankings type must not extend or modify WCLRanking / WCLRankingsData — those describe the boss leaderboard and share no shape with the per-report blob."
    - "The parse lens must not synthesize a percentile, a bracket percentage, or any field the recorded fixture does not contain."
    - "The budget reader must not hard-code the hourly point limit, and must not re-implement the Redis transport that lib/kv-cache.ts already owns."
    - "No gate may be softened to let this change pass: `npm test` and `npx tsc --noEmit` must go green because the code is right, never because an assertion was weakened."
  flagged_assumptions:
    - "04-RESEARCH.md Pitfall 1 (VERIFIED): lib/wcl-types.ts's WCLRanking is the characterRankings boss-leaderboard shape, while the per-report blob is typed today only as an inline `{ fightID?, partition? }` at app/api/analyze/route.ts line 40. They do not share a shape. A new type is required; extending the old one is the failure mode."
    - "Observed in the committed Phase 2 fixture this planning session: character rows carry `bracketData` and `bracket` but NO bracket-percentile field, and no `hidden` key was present in that recording. The lens must therefore treat `hidden` as optional-and-honoured-when-present, and must not invent a bracket percentile. Re-confirm against 04-02's freshly recorded rankings-report.json before writing the type."
    - "Open Question 2 (from 04-RESEARCH.md): the per-query point cost is unpublished; 04-02 measures it. The 90% gate fraction is a policy choice, not a measured threshold, and is stated as such in the module header so a later phase can tune it deliberately."
---

<objective>
Type the Warcraft Logs per-report rankings blob correctly and build the two pure pieces R1 will need:
a parse lens and a rate-budget gate. Tests only, wired into nothing (R0-3, D-13).

**User story context:** the user-visible win in this phase is ads that do not slow the analyzer down.
This plan is the part of the phase that buys the *next* user-visible win — "your parse, explained" —
without spending any of this phase's risk budget on it: no route changes, no new WCL calls at
request time, no UI.

Purpose: `PARSEFORGE-RANKINGS-SPEC.md` §7 places R0 before every rankings phase precisely so the two
unknowns that could kill the product (the contract, and the real response shapes) are resolved before
anything is built on them. 04-02 resolved the shapes; this plan turns them into code.
Output: new rankings types in `lib/wcl-types.ts`, `lib/rankings/parse-lens.ts` and
`lib/rankings/budget.ts`, each with a fixture-driven test file.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ads-live/04-CONTEXT.md
@.planning/phases/04-ads-live/04-PATTERNS.md
@.planning/research/PARSEFORGE-RANKINGS-SPEC.md
@lib/wcl-types.ts
@lib/kv-cache.ts
</context>

<feature>
  <name>Rankings blob types + pure parse lens + Redis-backed WCL rate-budget gate (R0-3)</name>
  <files>lib/wcl-types.ts, lib/rankings/parse-lens.ts, lib/rankings/parse-lens.test.ts, lib/rankings/budget.ts, lib/rankings/budget.test.ts</files>
  <behavior>
    The parse lens turns one recorded `report.rankings(fightIDs:)` entry into a flat list of per-player
    parse rows plus one per-fight badge object, honouring hidden and blacklisted characters, and
    inventing nothing the recording does not contain. The budget reader validates the three WCL
    rate-limit numbers at runtime, persists them through the project's existing cache helpers, and
    answers a single question — may an expensive query run right now — with a reason, refusing both
    at the 90% line and when the state is unknown.
  </behavior>
  <implementation>See the two tasks below; RED-GREEN-REFACTOR per task, one atomic commit per phase of the cycle.</implementation>
</feature>

<!-- planner-discipline-allow: WCLRanking -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fixture to types to parse rows — the rankings lens, end to end</name>
  <files>lib/wcl-types.ts, lib/rankings/parse-lens.ts, lib/rankings/parse-lens.test.ts</files>
  <precondition>lib/__fixtures__/rankings-report.json exists and is committed (produced by 04-02 Task 1). Without it the types would be written from a document, which is the exact failure Pitfall 1 describes.</precondition>
  <read_first>
    - lib/__fixtures__/rankings-report.json — the ONLY authority for field names in this task. Read the whole recorded entry before writing a single type member.
    - lib/wcl-types.ts lines ~149-175 — the existing `WCLRanking` / `WCLRankingsData` interfaces (the boss-leaderboard shape) and the Cast Timeline Types section's comment convention ("Field names match the recorded response in lib/__fixtures__/…"), which the new types copy
    - app/api/analyze/route.ts around line 40 — the inline `rankings?: { data?: Array<{ fightID?: number; partition?: number }> }` that is the current under-typed version; do not change this call site's behaviour
    - lib/analysis-engine.ts lines 1-40 — the import ordering, the ASCII section-divider comment style, and the pure-no-throwing discipline this engine matches
    - .planning/research/PARSEFORGE-RANKINGS-SPEC.md §4.3 — why hidden and blacklisted characters matter (the "gaming" row)
  </read_first>
  <behavior>
    - Test: `toParseRows` over the recorded fixture returns one row per character across all three role groups, and the row count equals the sum of the three groups' character counts.
    - Test: each row carries the character id, name, server name and region, class, spec, the role it came from, the raw amount, and `rankPercent` — every value read from the fixture, none computed.
    - Test: `rankPercent` is preserved as null when the recording has no value for it, and is never coerced to 0 (a missing percentile and a zeroth-percentile parse are different facts).
    - Test: a character whose `hidden` field is true is returned with `hidden: true` and is excluded from the default visible list returned by the lens's ranked-view helper — present in the data, absent from the ranking.
    - Test: a character id present in the entry's character blacklist is returned with a blacklisted marker set, using the same include-but-mark rule.
    - Test: the lens is pure — calling it twice on the same input returns equal output, and the input object is not mutated (deep-equality against a structured clone taken before the call).
    - Test: `toFightBadges` returns the fight's kill flag, duration, death count, and the speed and execution objects' rankPercent values, all read from the recording.
    - Edge case: an empty `data` array returns an empty row list and a null badge object, never throws.
    - Edge case: an entry missing the roles object entirely returns an empty row list, never throws.
    - Edge case: a role group present but with an empty characters array contributes zero rows without error.
    - Edge case: the lens must not invent a bracket percentile — assert that no returned row has a field whose name suggests one, and expose the recorded `bracket` and `bracketData` values under their recorded names instead.
  </behavior>
  <action>
RED first: write `lib/rankings/parse-lens.test.ts` against the behaviors above, importing the
committed fixture directly the way `lib/__fixtures__/fixtures.test.ts` already imports its JSON.
Run it, watch it fail against the absent module, commit the failing test.

GREEN: add the types, then the engine.

**Types (`lib/wcl-types.ts`).** Add a new, clearly separated section with an ASCII divider comment in
the file's existing style, headed by a comment stating that these describe
`reportData.report.rankings(fightIDs:)` — the per-report parse blob — that they are deliberately NOT
an extension of `WCLRanking` (which is the `characterRankings` boss-leaderboard shape), and that
every field name was verified against `lib/__fixtures__/rankings-report.json` on the date of writing.
Declare at minimum: a character entry type, a role group type (`{ name, characters }`), a roles
container with the three role keys, a per-fight ranking entry type carrying the fight identifiers,
the kill flag, duration, bracket values, the role container, the character blacklist and the speed
and execution objects, and a top-level blob type wrapping a `data` array of entries. Mark optional
every field the recording does not guarantee — in particular the hidden marker, which was absent from
the Phase 2 recording and must therefore be optional rather than required. Do not remove, rename or
re-purpose any existing export in this file.

**Engine (`lib/rankings/parse-lens.ts`).** New directory, new file. Module header in
`lib/analysis-engine.ts`'s voice: what it does, that it is pure and never throws, and which fixture
its field names were verified against. Exports:
  - `ParseRow` — the per-player row interface described in the behaviors, including a role
    discriminator, an optional hidden flag and a blacklisted flag.
  - `FightBadges` — the per-fight object carrying the kill flag, duration, death count and the speed
    and execution percentiles.
  - `toParseRows(blob)` — flattens the roles container into rows, preserving nulls, marking hidden and
    blacklisted characters, never mutating its argument.
  - `toFightBadges(blob)` — the per-fight object, or null when there is no entry.
  - `visibleParseRows(rows)` — the ranked view: rows that are neither hidden nor blacklisted, sorted
    by `rankPercent` descending with a stable tiebreak on name so the order is deterministic.
Read defensively from every level — the blob comes from a third party and every nested object is
treated as possibly absent, exactly as `lib/consent.ts` treats the CMP payload. Return empty results
rather than throwing on any shape surprise.

REFACTOR only if the engine ended up with duplicated per-role flattening; keep the tests green.

Do not import this module from any route, page, component or API handler. D-13 is explicit: R0-3
ships types and engine only, wired into nothing, until the approval reply lands.
  </action>
  <acceptance_criteria>
    - `lib/rankings/parse-lens.ts` and `lib/rankings/parse-lens.test.ts` exist.
    - `npx vitest run lib/rankings/parse-lens.test.ts` exits 0 with at least 11 passing tests.
    - `lib/rankings/parse-lens.ts` imports its types from `../wcl-types` and imports nothing from `app/`.
    - `lib/rankings/parse-lens.ts` contains no reference to `WCLRanking`.
    - `lib/wcl-types.ts` still exports `WCLRanking` and `WCLRankingsData` with their existing members unchanged.
    - No file under `app/` imports from `lib/rankings/` (grep count is zero).
    - `npx tsc --noEmit` exits 0 and `npx eslint lib/rankings/parse-lens.ts lib/rankings/parse-lens.test.ts lib/wcl-types.ts` reports no new findings.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx vitest run lib/rankings/parse-lens.test.ts</automated>
    <fails_when>non-zero exit, or a `Tests 0 passed` / `no test files found` summary line, or fewer than 11 passing tests in the summary</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npx eslint lib/rankings/parse-lens.ts lib/rankings/parse-lens.test.ts lib/wcl-types.ts && echo gates-ok</automated>
    <fails_when>non-zero exit, or the absence of `gates-ok` on the last line</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && W=$(grep -v '^[[:space:]]*//' lib/rankings/parse-lens.ts | grep -v '^[[:space:]]*\*' | grep -c 'WCLRanking'); echo "leaderboard-type-refs=$W"; [ "$W" -eq 0 ] || { echo "GATE-FAIL: the lens imports the boss-leaderboard type instead of the per-report blob type"; exit 1; }; U=$(grep -rl "lib/rankings" app --include='*.ts' --include='*.tsx' | wc -l | tr -d ' '); echo "app-imports-of-rankings=$U"; [ "$U" -eq 0 ] || { echo "GATE-FAIL: $U files under app/ import the rankings engine - R0-3 ships unwired"; exit 1; }; for SYM in 'export interface WCLRanking ' 'export interface WCLRankingsData '; do grep -q "$SYM" lib/wcl-types.ts || { echo "GATE-FAIL: an existing exported type was removed or renamed: $SYM"; exit 1; }; done; F=$(grep -c 'rankings-report.json' lib/wcl-types.ts); echo "fixture-citations=$F"; [ "$F" -ge 1 ] || { echo "GATE-FAIL: the new types do not cite the fixture they were verified against"; exit 1; }; echo parse-lens-wiring-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `parse-lens-wiring-ok`</fails_when>
  </verify>
  <done>The per-report rankings blob has its own types citing the fixture they were verified against; a pure, non-throwing lens turns a real recorded blob into per-player rows and per-fight badges, honours hidden and blacklisted characters, invents nothing, and is imported by no route or component.</done>
  <reversibility rating="reversible">Additive types and a new directory; nothing existing is rewired.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The WCL rate-budget gate on the existing Redis cache</name>
  <files>lib/rankings/budget.ts, lib/rankings/budget.test.ts</files>
  <precondition>lib/__fixtures__/rankings-ratelimit.json exists with at least three samples (produced by 04-02 Task 1) — the validator's tests read real recorded shapes, not hand-built mocks.</precondition>
  <read_first>
    - lib/__fixtures__/rankings-ratelimit.json — the recorded shape and the three sample labels
    - lib/kv-cache.ts in full — `usingSharedCache`, the exported `cacheGet` / `cacheSet` helpers, the Map fallback, and the "never throw, log and degrade" contract. Import these; do not re-implement the Redis transport.
    - app/components/PostHogProvider.tsx lines 115-122 — the runtime-validate-before-trusting, fail-closed pattern the validator mirrors
    - .planning/research/PARSEFORGE-RANKINGS-SPEC.md §2.2 — the budget facts, and the recorded observation that the hourly limit has been seen changing mid-session
    - .agents/skills/upstash-redis-js/SKILL.md — the project's Redis SDK guidance, for anything beyond what kv-cache.ts already wraps
  </read_first>
  <behavior>
    - Test: `parseRateLimitData` accepts each recorded sample from the fixture and returns the three numbers.
    - Test: `parseRateLimitData` returns null when any of the three values is missing, is a string, is NaN, or is negative — a malformed payload is never partially trusted.
    - Test: `parseRateLimitData` returns null when the hourly limit is zero (a zero denominator would make every spend look infinitely safe or infinitely spent, depending on the division order).
    - Test: `recordRateLimit` with a valid payload writes state through the cache helper and resolves true; with an invalid payload it writes nothing and resolves false.
    - Test: `recordRateLimit` never throws when the cache helper rejects — it resolves false and the caller is unaffected.
    - Test: `hasBudget` returns allowed true with reason ok when spend is below 90% of the runtime-read limit.
    - Test: `hasBudget` returns allowed false with reason at-gate at exactly 90% and above (the boundary is inclusive — 90% is the gate, not the last safe point).
    - Test: `hasBudget` returns allowed false with reason unknown when no state has been recorded, and when the recorded state fails validation on read-back.
    - Test: the hourly limit used in the comparison comes from the recorded state, and a fixture sample with a different limit produces a different gate point — proving the limit is never hard-coded.
    - Edge case: a state whose reset window has elapsed is treated as unknown rather than as a stale allowance.
  </behavior>
  <action>
RED first: write `lib/rankings/budget.test.ts` against the behaviors above, reading the recorded
samples from `lib/__fixtures__/rankings-ratelimit.json` and stubbing the cache helpers with vitest's
module mocking so no network call happens in the suite. Run it, watch it fail, commit the failing test.

GREEN: write `lib/rankings/budget.ts`. Module header in `lib/kv-cache.ts`'s voice, stating three
things explicitly: that the hourly limit is always read from the response and never hard-coded
because it has been observed changing mid-session; that the 90% figure is a deliberate policy choice
rather than a measured threshold, so a later phase can tune it knowingly; and that the gate fails
closed on an unknown state, with the reason exposed so a caller with a genuinely cheap or
already-cached request can make its own decision rather than being silently blocked.

Exports:
  - `WclRateLimitData` — the three numbers.
  - `BUDGET_KEY` — the Redis key namespace, `wcl:budget`.
  - `BUDGET_GATE_FRACTION` — 0.9.
  - `parseRateLimitData(raw: unknown): WclRateLimitData | null` — pure, no I/O, validates that all
    three values are finite non-negative numbers and that the hourly limit is greater than zero.
  - `recordRateLimit(raw: unknown): Promise<boolean>` — validates, then persists a state object
    (the three numbers plus the record timestamp) via the existing cache set helper with a TTL
    derived from the reset-in value, so an expired window cannot masquerade as current state.
    Never throws.
  - `readBudgetState(): Promise<WclBudgetState | null>` — reads through the existing cache get helper
    and re-validates on the way out; a state that fails validation on read-back is treated as absent.
  - `hasBudget(): Promise<{ allowed: boolean; reason: "ok" | "at-gate" | "unknown"; spentFraction: number | null }>`
    — the single question callers ask. Inclusive at the gate fraction.
Import `cacheGet` and `cacheSet` from `../kv-cache`; do not open a Redis connection here and do not
duplicate `usingSharedCache`.

REFACTOR only if validation logic duplicated between the parse and read-back paths; keep tests green.

Do not import this module from any route, page, component or API handler (D-13).
  </action>
  <acceptance_criteria>
    - `lib/rankings/budget.ts` and `lib/rankings/budget.test.ts` exist.
    - `npx vitest run lib/rankings/budget.test.ts` exits 0 with at least 10 passing tests.
    - `lib/rankings/budget.ts` imports `cacheGet` and `cacheSet` from the existing cache module and contains no `fetch(` call of its own.
    - `lib/rankings/budget.ts` contains the literal `0.9` exactly once (the exported gate fraction) and contains no numeric literal standing in for the hourly point limit.
    - No file under `app/` imports from `lib/rankings/`.
    - `npm test` exits 0 for the whole suite and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx vitest run lib/rankings/budget.test.ts</automated>
    <fails_when>non-zero exit, or a `Tests 0 passed` / `no test files found` summary line, or fewer than 10 passing tests in the summary</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && B=lib/rankings/budget.ts; strip() { grep -v '^[[:space:]]*//' "$1" | grep -v '^[[:space:]]*\*'; }; F=$(strip "$B" | grep -c 'fetch('); echo "own-fetch-calls=$F"; [ "$F" -eq 0 ] || { echo "GATE-FAIL: the budget reader opens its own transport instead of reusing lib/kv-cache.ts"; exit 1; }; I=$(grep -c 'from "\.\./kv-cache"' "$B"); echo "kv-cache-imports=$I"; [ "$I" -eq 1 ] || { echo "GATE-FAIL: expected exactly 1 import from the shared cache module, found $I"; exit 1; }; G=$(strip "$B" | grep -c '0\.9'); echo "gate-fraction-literals=$G"; [ "$G" -eq 1 ] || { echo "GATE-FAIL: the gate fraction appears $G times - it must be a single exported constant"; exit 1; }; H=$(strip "$B" | grep -cE '3600|36000|9000|18000'); echo "hardcoded-limit-literals=$H"; [ "$H" -eq 0 ] || { echo "GATE-FAIL: an hourly point limit is hard-coded - it must be read from the response at runtime"; exit 1; }; U=$(grep -rl "lib/rankings" app --include='*.ts' --include='*.tsx' | wc -l | tr -d ' '); echo "app-imports-of-rankings=$U"; [ "$U" -eq 0 ] || { echo "GATE-FAIL: $U files under app/ import the rankings engine - R0-3 ships unwired"; exit 1; }; echo budget-wiring-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `budget-wiring-ok`</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npm test && npm run seo-invariants -- --report && echo suite-ok</automated>
    <fails_when>non-zero exit, or the absence of `suite-ok` on the last line</fails_when>
  </verify>
  <done>The budget gate validates the three rate-limit numbers before trusting them, persists them through the project's existing cache helpers with a reset-derived TTL, refuses inclusively at 90% of a runtime-read limit, refuses on unknown state with the reason exposed, never throws, and is imported by nothing.</done>
  <reversibility rating="reversible">A new unwired module; deleting it changes no behaviour.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WCL GraphQL response → `parseRateLimitData` | untrusted third-party numbers decide whether further API spend is allowed |
| WCL GraphQL response → `toParseRows` | untrusted third-party nested objects, any level of which may be absent |
| `lib/rankings/budget.ts` → Upstash Redis | budget state crosses into shared, cross-instance storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Tampering | malformed `rateLimitData` causing the gate to under- or over-count spend | high | mitigate | `parseRateLimitData` requires three finite non-negative numbers with a positive hourly limit, rejects partial payloads outright, and `hasBudget` fails closed with reason `unknown` rather than allowing — the same runtime-validate-then-fail-closed pattern the geo decision already uses |
| T-04-19 | Denial of Service | a stale budget state allowing spend after the hourly window reset | medium | mitigate | State is written with a TTL derived from the reset-in value and re-validated on read-back; an elapsed window reads as unknown, which refuses |
| T-04-20 | Information Disclosure | third-party player names and server/region entering logs through the lens | medium | mitigate | The lens is pure and logs nothing; no console output is added; the rows stay in memory and are returned to a caller that does not exist yet (D-13) |
| T-04-21 | Tampering | an engine that throws on an unexpected blob shape taking down a request | medium | mitigate | Every level is read defensively and every failure path returns an empty result; the edge-case tests assert no-throw on absent roles, empty data and empty character lists |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks in this plan or this phase (04-RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies). Any later addition must pass `package-legitimacy check` plus a blocking human checkpoint before install; `[SLOP]` packages are forbidden |
</threat_model>

<artifacts_this_phase_produces>
Created by this plan:
- `lib/rankings/parse-lens.ts` — exports `ParseRow`, `FightBadges`, `toParseRows`, `toFightBadges`, `visibleParseRows`
- `lib/rankings/parse-lens.test.ts` — new test file
- `lib/rankings/budget.ts` — exports `WclRateLimitData`, `WclBudgetState`, `BUDGET_KEY`,
  `BUDGET_GATE_FRACTION`, `parseRateLimitData`, `recordRateLimit`, `readBudgetState`, `hasBudget`
- `lib/rankings/budget.test.ts` — new test file
- `lib/wcl-types.ts` — new per-report rankings-blob types (a character entry type, a role group type,
  a roles container, a per-fight ranking entry type and a top-level blob type), distinct from the
  existing `WCLRanking` / `WCLRankingsData`
- New Redis key namespace: `wcl:budget`
- New directory: `lib/rankings/`
</artifacts_this_phase_produces>

<verification>
- `npm test` green for the whole suite; `npx tsc --noEmit` clean; `npm run lint` with no new findings.
- Zero imports of `lib/rankings/` from anywhere under `app/`.
- `npm run seo-invariants` and `npm run protected-elements` unchanged — no user-facing surface moved.
- `lib/wcl-types.ts`'s pre-existing exports are intact.
</verification>

<success_criteria>
- R0-3's verification line from `PARSEFORGE-RANKINGS-SPEC.md` §7 is met exactly: `npm test` green, `npx tsc --noEmit` clean, no UI change, `seo-invariants` / `protected-elements` unchanged.
- R1 can be written against `toParseRows` and `hasBudget` without re-deriving a single WCL field name.
</success_criteria>

<output>
Create `.planning/phases/04-ads-live/04-04-SUMMARY.md` when done.
</output>
