---
phase: 04-ads-live
plan: 05
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - lib/ads.ts
  - app/tbc-audit/page.tsx
  - app/analyze/[reportCode]/AnalyzeClient.tsx
  - docs/PROTECTED-ELEMENTS.md
  - app/privacy/page.tsx
autonomous: false
requirements: [MONY-02]

user_setup:
  - service: google-adsense
    why: "D-05 and D-15: Auto ads must be switched off in the account (no code can stop Google injecting a unit), the four fixed-size units must be created before their ids can be put in lib/ads.ts, the publisher id must exist in Vercel env before any build can serve an ad, and the /privacy URL has been waiting to be pasted into Privacy & messaging since Phase 1."
    env_vars:
      - name: NEXT_PUBLIC_ADSENSE_PUB_ID
        source: "AdSense -> Account -> Account information -> Publisher ID (the digits after the ca-pub- prefix). Added to Vercel Preview AND Production with `vercel env add ... --global-config ~/.vercel-personal --scope loot-list-plus`; bakes at build time, so it only takes effect on the next deploy."
      - name: NEXT_PUBLIC_ADS_ENABLED
        source: "set by hand to the string 1 on Preview and Production; anything else removes the ad markup entirely on the next build (D-08)"
    dashboard_config:
      - task: "Switch Auto ads OFF for parseforge.gg"
        location: "AdSense -> Ads -> By site -> parseforge.gg -> Edit -> Auto ads toggle"
      - task: "Create four fixed-size display units and record their slot ids: 300x250/728x90 for the two in-content positions and 300x250/336x280 for the two end-of-page positions"
        location: "AdSense -> Ads -> By ad unit -> Display ads (fixed size, NOT responsive)"
      - task: "Verify ads.txt for parseforge.gg"
        location: "AdSense -> Sites -> parseforge.gg -> ads.txt status"
      - task: "Paste https://parseforge.gg/privacy into the consent message's site settings (open since Phase 1, plan 01-09)"
        location: "AdSense -> Privacy & messaging -> European regulations -> message -> site settings"

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor on /tbc-audit sees exactly two ad positions — one after the first content section and one at the end — and a visitor on an analyze page sees exactly two — one between the tab bar and the tab body, one at the very end of the page (MONY-02, SC1, D-01, D-02)."
    - "No ad position is inside a table, adjacent to any of the six data-protected elements, or between a share control and the thing it shares — proven by `npm run protected-elements`, not by inspection (SC1, D-02)."
    - "Every position reserves its exact pixel box per breakpoint: 300x250 on phones, 728x90 for the two in-content positions and 336x280 for the two end positions on md and wider (SC2, D-03, D-04)."
    - "No ad slot exists on the homepage, on any guide page, or on any route other than /analyze/* and /tbc-audit (D-01)."
    - "/privacy describes, in the present tense, that Google AdSense serves ads on the analyzer routes, what the ad script may access, that a visitor who rejects consent is never served the ad script, and that report data shown on the site is sourced from the Warcraft Logs API — the disclosure the RPGLogs Terms require before ads ship (R0-1, D-15)."
    - "Auto ads is switched OFF in the AdSense account and the four fixed-size units exist with their ids recorded in lib/ads.ts (D-05)."
    - statement: "The /privacy text is accurate on the day it reaches production — it ships in the same deploy that turns ads on, never ahead of it."
      verification: backstop
  artifacts:
    - "lib/ads.ts (four slot entries with real unit ids)"
    - "app/tbc-audit/page.tsx (two slots)"
    - "app/analyze/[reportCode]/AnalyzeClient.tsx (two slots)"
    - "docs/PROTECTED-ELEMENTS.md (four placement rows)"
    - "app/privacy/page.tsx (advertising and Warcraft Logs data disclosure)"
  key_links:
    - "AnalyzeClient's mid slot must render as part of the report-loaded shell, at the same moment the tab bar and selectors appear — if it mounts later than the shell it pushes the tab body down after paint, which is a layout shift on the phase's most important route."
    - "Both end slots must be the last child of their page's main element, after the guide-link block — that is what makes their collapsible flag safe, because nothing exists below them to shift."
    - "docs/PROTECTED-ELEMENTS.md's placement table, lib/ads.ts's AD_SLOTS and the owner components must all name the same four ids with the same four box sizes; `npm run protected-elements` fails on any disagreement."
  prohibitions:
    - "No ad format that Google can place outside a reserved whitelisted slot may be enabled — no auto-placement, no anchor, no overlay, no interstitial, no auto-sizing unit — and no route beyond /analyze/* and /tbc-audit may carry a slot this phase."
    - "No ad slot may be placed inside app/components/RaidOverview.tsx, app/components/ComparisonSummary.tsx, app/components/AnalysisView.tsx, app/components/CLAView.tsx or any table body."
    - "The /privacy advertising text must not be deployed to production ahead of the deploy that actually turns ads on — a policy claiming ads that are not running is as wrong as one omitting ads that are."
    - "No gate may be softened to let this change pass: protected-elements, seo-invariants, token-audit and theme-parity must be met, not relaxed or waived."
  flagged_assumptions:
    - "The edge-probe row for MONY-02 came back unclassified and stays unresolved. Surfaced rather than dropped: the placement edges this plan assumes are (a) an analyze page whose report never loads, where the shell and therefore the mid slot never render at all, (b) a fight with no result on any tab, where the end slot still renders below the guide links, and (c) the narrowest supported viewport, where a 300px box must not force horizontal scroll inside the max-w-3xl and max-w-7xl wrappers. (a) and (b) are covered by the render gating below; (c) is a human-check on the mobile pass."
    - "04-RESEARCH.md A3: Auto ads may inject units even when only manual code is present. Task 3's dashboard step is the only control for this; no code in this repository can enforce it, and a unit appearing at a position no slot reserved is the signal that it was not done."
    - "04-RESEARCH.md A4: no published AdSense traffic threshold exists. If the account is not approved for parseforge.gg, Task 3 cannot complete and the phase stops at a recorded blocked state rather than shipping ad markup with no account behind it."
---

<objective>
Expand the proven tracer slice to the full D-02 placement map — two positions on each of the two
whitelisted routes — write the advertising and Warcraft Logs disclosure `/privacy` needs before ads
can legally run, and do the account-side configuration no code can do.

**User story (MVP framing):** **As a** ParseForge visitor on either the raid-audit page or a report
analysis page, **I want to** have every share button, awards panel and analysis table exactly where
it was, **so that** the ads funding the site never cost me a click I was about to make.

After this plan a real user can do something they could not after 04-03: load an actual report
analysis on a build with ads on and see the full, final ad layout — four reserved positions across
two routes — with every protected element still where the share loop put it.

Purpose: the tracer proved one slot end to end on the simplest route. This plan is the horizontal
reach of that same slice, and the only genuinely new risk it adds is proximity to the six protected
elements — which is exactly what 04-03 Task 3's machine check was built to police.
Output: four live slot placements, four placement-table rows, the `/privacy` disclosure, and a
configured AdSense account.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ads-live/04-CONTEXT.md
@.planning/phases/04-ads-live/04-PATTERNS.md
@.planning/phases/04-ads-live/04-03-SUMMARY.md
@docs/PROTECTED-ELEMENTS.md
@.planning/research/PARSEFORGE-RANKINGS-SPEC.md
</context>

<!-- planner-discipline-allow: AdSlot -->
<!-- planner-discipline-allow: PENDING -->

<tasks>

<task type="auto">
  <name>Task 1: The remaining three D-02 placements, on both whitelisted routes</name>
  <files>lib/ads.ts, app/tbc-audit/page.tsx, app/analyze/[reportCode]/AnalyzeClient.tsx, docs/PROTECTED-ELEMENTS.md</files>
  <read_first>
    - lib/ads.ts as written by 04-03 — `AdSlotId`, `AdSlotSpec`, `AD_SLOTS` and the one existing entry whose shape the three new ones copy exactly
    - app/components/AdSlot.tsx as written by 04-03 — its props and its render gating, so the new call sites pass only what it takes
    - app/tbc-audit/page.tsx — `<main className="mx-auto max-w-3xl py-16 space-y-14">`, the `<header>` ending with `ReportUrlForm`, then five `<section>` blocks; the first is the "Gear checks" section and the last is the guide-link list that closes just before `</main>`
    - app/analyze/[reportCode]/AnalyzeClient.tsx — the `<main className="py-6 space-y-6">` wrapper; the report header block carrying `data-protected="share-header"`; the error alert; the tab switcher block; the selectors block ending with the Refresh button; the report loading skeleton; then the three tab-content blocks; then the guide-links block gated on `(player.result || raid.result || cla.result)` that closes just before `</main>`
    - docs/PROTECTED-ELEMENTS.md `## Ad slot placements` as created by 04-03 — the five-column row format the three new rows follow
    - .planning/phases/04-ads-live/04-CONTEXT.md D-02, D-03, D-04 — the exact positions and sizes being implemented
  </read_first>
  <action>
**lib/ads.ts.** Widen `AdSlotId` from one member to four: the existing end-of-page raid-audit slot,
plus `tbc-audit-mid`, `analyze-mid` and `analyze-end`. Add three `AD_SLOTS` entries with the same
`AdSlotSpec` shape:
  - `tbc-audit-mid` — route `/tbc-audit`, base 300x250, md 728x90, `collapsible` false (content
    follows it), `unit` left as the placeholder until Task 3 supplies the real id.
  - `analyze-mid` — route `/analyze/[reportCode]`, base 300x250, md 728x90, `collapsible` false.
  - `analyze-end` — route `/analyze/[reportCode]`, base 300x250, md 336x280, `collapsible` true
    (it is the last element on the page).
Each entry's `boxClass` is a whole static Tailwind class string encoding exactly its own four
numbers, never assembled by concatenation — the existing entry is the template. The invariant test
04-03 wrote over `AD_SLOTS` will now cover all four entries automatically; if it does not (for
example because it iterated a hard-coded list), widen it so it iterates the record's own keys.

**app/tbc-audit/page.tsx.** Add one mount of the slot component with id `tbc-audit-mid` between the
closing tag of the first content section (the gear-checks one) and the opening tag of the second.
The existing end-of-page mount from 04-03 stays exactly where it is. Two mounts total on this file.

**app/analyze/[reportCode]/AnalyzeClient.tsx.** Two mounts, and nothing else on this file changes:
  - `analyze-mid` immediately after the report loading-skeleton block and immediately before the
    Player Analysis tab-content block — that is, below the header, the tab switcher and the
    selectors, and above whichever tab body renders. Wrap the mount in the same `{report && ( … )}`
    condition the tab switcher and selectors already use, so the reserved box appears at the same
    moment as the rest of the report shell rather than arriving later and pushing the tab body down
    after paint. Do not place it inside any tab-content block; one mount serves all three tabs.
  - `analyze-end` as the last child of `<main>`, after the guide-links block's closing tag. Render it
    unconditionally so it does not appear and disappear as results load; nothing exists below it to
    shift either way.
Do not touch the header Share button, the tab switcher, the selectors, any tab-content block, the
landing-ref effect, or the guide-links block.

**docs/PROTECTED-ELEMENTS.md.** Add three rows to the `## Ad slot placements` table, one per new
slot, with the same five columns and the same backticked first two cells: slot id, owner file,
position described in terms of the surrounding elements (not line numbers, which drift), and the two
box sizes. Extend the section's prose rules with one sentence naming the analyze page's mid position
explicitly as being *below* the header Share control and *above* the Raid tab's awards panel and the
Player tab's scorecard, so a future reader can tell whether a redesign moved it.
  </action>
  <acceptance_criteria>
    - `lib/ads.ts` declares exactly four slot ids and four `AD_SLOTS` entries.
    - `app/tbc-audit/page.tsx` contains exactly two slot mounts; `app/analyze/[reportCode]/AnalyzeClient.tsx` contains exactly two.
    - The analyze page's end slot mount appears at a higher line number than the guide-links block's closing tag.
    - `docs/PROTECTED-ELEMENTS.md`'s placement table has exactly four data rows.
    - Zero slot mounts exist in `app/page.tsx`, anywhere under `app/guides/`, or in `app/components/RaidOverview.tsx`, `app/components/ComparisonSummary.tsx`, `app/components/AnalysisView.tsx` or `app/components/CLAView.tsx`.
    - `npm run protected-elements` exits 0 with all six `attr:` rows and all `adslot:` rows passing.
    - `npx tsc --noEmit`, `npx eslint` on the three touched source files, `npm run token-audit`, `npm run theme-parity` and `npm run seo-invariants` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npx eslint lib/ads.ts app/tbc-audit/page.tsx 'app/analyze/[reportCode]/AnalyzeClient.tsx' && npm run token-audit && npm run theme-parity && npm test && echo gates-ok</automated>
    <fails_when>non-zero exit, or the absence of `gates-ok` on the last line</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npm run protected-elements</automated>
    <fails_when>non-zero exit, or a trailing summary line reporting any failed check</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && T=$(grep -c 'AdSlot id=' app/tbc-audit/page.tsx); A=$(grep -c 'AdSlot id=' 'app/analyze/[reportCode]/AnalyzeClient.tsx'); echo "tbc-mounts=$T analyze-mounts=$A"; [ "$T" -eq 2 ] || { echo "GATE-FAIL: expected 2 slots on the raid-audit page, found $T"; exit 1; }; [ "$A" -eq 2 ] || { echo "GATE-FAIL: expected 2 slots on the analyze page, found $A"; exit 1; }; for F in app/page.tsx app/components/RaidOverview.tsx app/components/ComparisonSummary.tsx app/components/AnalysisView.tsx app/components/CLAView.tsx; do N=$(grep -c 'AdSlot' "$F"); echo "$F=$N"; [ "$N" -eq 0 ] || { echo "GATE-FAIL: an ad slot reached $F, which is outside the two-route whitelist or owns a protected element"; exit 1; }; done; G=$(grep -rc 'AdSlot' app/guides 2>/dev/null | grep -v ':0$' | wc -l | tr -d ' '); echo "guide-files-with-slots=$G"; [ "$G" -eq 0 ] || { echo "GATE-FAIL: an ad slot reached a guide page"; exit 1; }; ROWS=$(awk '/^## Ad slot placements/{f=1;next} /^## /{f=0} f' docs/PROTECTED-ELEMENTS.md | grep -c '^| `'); echo "placement-rows=$ROWS"; [ "$ROWS" -eq 4 ] || { echo "GATE-FAIL: expected 4 placement rows in the whitelist, found $ROWS"; exit 1; }; IDS=$(grep -c 'collapsible' lib/ads.ts); echo "slot-entries-with-collapsible=$IDS"; [ "$IDS" -ge 4 ] || { echo "GATE-FAIL: fewer than 4 slot entries declare a collapsible flag"; exit 1; }; GL=$(grep -n 'Improve your performance' 'app/analyze/[reportCode]/AnalyzeClient.tsx' | head -1 | cut -d: -f1); EL=$(grep -n 'AdSlot id="analyze-end"' 'app/analyze/[reportCode]/AnalyzeClient.tsx' | head -1 | cut -d: -f1); echo "guide-links-line=$GL end-slot-line=$EL"; [ -n "$EL" ] || { echo "GATE-FAIL: no end slot found on the analyze page"; exit 1; }; [ "$EL" -gt "$GL" ] || { echo "GATE-FAIL: the end slot sits above the guide links, so it is not the last element and its collapse would shift content"; exit 1; }; echo placements-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `placements-ok`</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && (NEXT_PUBLIC_ADS_ENABLED=1 NEXT_PUBLIC_ADSENSE_PUB_ID=1234567890123456 npx next dev -p 3998 >/tmp/pf-dev-3998.log 2>&1 & echo $! > /tmp/pf-dev-3998.pid); for i in $(seq 1 45); do curl -sf -o /dev/null "http://localhost:3998/tbc-audit" && break; sleep 2; done; curl -s "http://localhost:3998/tbc-audit" > /tmp/pf-tbc2.html; kill "$(cat /tmp/pf-dev-3998.pid)" 2>/dev/null; MID=$(grep -c 'data-ad-slot="tbc-audit-mid"' /tmp/pf-tbc2.html); END=$(grep -c 'data-ad-slot="tbc-audit-end"' /tmp/pf-tbc2.html); SCRIPT=$(grep -c 'googlesyndication' /tmp/pf-tbc2.html); echo "mid=$MID end=$END script-refs=$SCRIPT"; [ "$MID" -eq 1 ] && [ "$END" -eq 1 ] || { echo "GATE-FAIL: the raid-audit page did not server-render both reserved boxes"; exit 1; }; [ "$SCRIPT" -eq 0 ] || { echo "GATE-FAIL: an ad script reference is present in the server-rendered HTML"; exit 1; }; echo tbc-render-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `tbc-render-ok`</fails_when>
  </verify>
  <done>Four reserved positions exist across the two whitelisted routes, each with its exact per-breakpoint box; the placement whitelist, lib/ads.ts and the owner components all agree; no slot exists on any other route or in any file owning a protected element; every standing gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: The /privacy advertising and Warcraft Logs data disclosure</name>
  <files>app/privacy/page.tsx</files>
  <read_first>
    - app/privacy/page.tsx in full — in particular the `Advertising and your consent choices` section (which currently says ParseForge does not show any adverts and speaks of AdSense in the future tense), the `Third parties` list (whose Google entry says "in the future, may serve AdSense ads"), the `What ParseForge processes` section describing the Warcraft Logs fetch, and the `Your rights` section's California paragraph (which currently asserts that nothing is shared for cross-context behavioral advertising)
    - .planning/research/PARSEFORGE-RANKINGS-SPEC.md §2.1 — the Terms row requiring a privacy policy "describing what is collected and how it is shared, including for advertising"
    - app/terms/page.tsx — the voice and the operator identity the policy speaks in
    - .planning/phases/04-ads-live/04-CONTEXT.md D-07, D-15
  </read_first>
  <action>
Rewrite the three places that go stale the moment ads run, and add the one disclosure the RPGLogs
Terms require. Keep the page's existing plain, second-person voice and its section structure; add no
new section unless the content genuinely does not belong in an existing one.

1. **Advertising and your consent choices.** Replace the future-tense sentence. State in the present
   tense that Google AdSense serves display ads on the report-analysis pages and the raid-audit page,
   and that the homepage and the guides carry none. State what the ad script can see — the page URL
   and the browser information any third-party script on a page receives — and that ParseForge passes
   it no report code, character name or other analysis content. State the gate plainly: for a visitor
   in the EEA or the UK, the ad script is not loaded at all unless consent has been given through the
   dialog, and a rejection or an unanswered dialog means the script is never requested. Link Google's
   own advertising privacy page for what Google does with the data once the ad loads. Keep the
   existing "you can change your mind at any time" paragraph.

2. **Third parties.** Change the Google entry from a future-tense possibility to the present: it
   provides the consent dialog and serves the display ads. Leave every other entry alone.

3. **Warcraft Logs data disclosure.** In the `What ParseForge processes` section (or immediately
   after it), add a short paragraph saying that the report data the site displays — player names,
   gear, buffs, parses and rankings — comes from the public Warcraft Logs API and belongs to Warcraft
   Logs and the uploader; that ParseForge only fetches reports a visitor asks for by pasting a link;
   that it is cached briefly and shown back, not resold or repackaged; and that the analysis pages
   carrying this data also carry advertising. Name Warcraft Logs and link the report back, matching
   the attribution posture the existing Third parties entry already takes.

4. **California paragraph.** The existing assertion that nothing is shared for cross-context
   behavioral advertising stops being true the moment a personalized AdSense unit serves. Replace it
   with an accurate statement: ParseForge does not sell personal information for money, and
   advertising on the analysis pages may constitute sharing for cross-context behavioral advertising
   under California law, with a pointer to the consent dialog as the control. Do not overstate in
   either direction.

5. Update the page's `Last updated` line (or add one in the existing style if the page has none) to
   today's date.

This text ships to production only in the deploy that turns ads on (04-07) — a policy that claims
ads that are not running is as wrong as one that omits ads that are. Do not deploy it separately.
  </action>
  <acceptance_criteria>
    - `app/privacy/page.tsx` no longer contains the phrase `does not currently show any adverts`.
    - The page contains the words `Warcraft Logs` in a paragraph that also mentions advertising or ads.
    - The page names both whitelisted surfaces (the analysis pages and the raid audit page) as the places ads appear.
    - The page states that the ad script is not loaded for an EEA/UK visitor who has not consented.
    - The page no longer asserts, unqualified, that nothing is shared for cross-context behavioral advertising.
    - The page carries a `Last updated` date equal to the day the change was made.
    - `npx tsc --noEmit`, `npx eslint app/privacy/page.tsx`, `npm run token-audit` and `npm run theme-parity` exit 0, and `npm run seo-invariants` reports no change to the /privacy route's title, canonical, robots or description.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npx eslint app/privacy/page.tsx && npm run token-audit && npm run theme-parity && echo gates-ok</automated>
    <fails_when>non-zero exit, or the absence of `gates-ok` on the last line</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && P=app/privacy/page.tsx; STALE=$(grep -cF 'does not currently show any adverts' "$P"); echo "stale-sentences=$STALE"; [ "$STALE" -eq 0 ] || { echo "GATE-FAIL: the policy still says the site shows no adverts"; exit 1; }; for TOK in 'Warcraft Logs' 'AdSense' 'consent'; do N=$(grep -cF "$TOK" "$P"); echo "[$TOK]=$N"; [ "$N" -ge 1 ] || { echo "GATE-FAIL: the policy never mentions $TOK"; exit 1; }; done; U=$(grep -ci 'last updated' "$P"); echo "last-updated=$U"; [ "$U" -ge 1 ] || { echo "GATE-FAIL: the policy carries no last-updated date"; exit 1; }; D=$(date -u +%Y); grep -q "$D" "$P" || { echo "GATE-FAIL: the last-updated date is not from this year"; exit 1; }; CCPA=$(grep -cF 'we do not share it for' "$P"); echo "unqualified-ccpa-claims=$CCPA"; [ "$CCPA" -eq 0 ] || { echo "GATE-FAIL: the unqualified no-sharing claim is still present and stops being true when an ad serves"; exit 1; }; echo privacy-copy-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `privacy-copy-ok`</fails_when>
    <human-check>The developer reads the rewritten Advertising, Third parties, Warcraft Logs and California paragraphs end to end and confirms each statement is true of what the site will actually do on the day ads go live — this is a legal disclosure, and an automated grep can prove the words are present but not that they are accurate.</human-check>
  </verify>
  <done>/privacy describes advertising in the present tense on the two whitelisted surfaces, names what the ad script sees and what it is not given, states the consent gate, discloses the Warcraft Logs source of the displayed data and that those pages carry ads, corrects the California claim, and carries today's date — reviewed by the developer.</done>
  <reversibility rating="costly">A published privacy policy is a public commitment; correcting it later is possible but is a visible change of position, so the developer reviews the text before it ships.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: AdSense account configuration — Auto ads off, four fixed units, env vars, consent message site</name>
  <files>lib/ads.ts</files>
  <read_first>
    - lib/ads.ts as written by Task 1 — the four `unit` placeholder values that this task replaces with real ids, and `adsConfigured`, which keeps every slot inert until they are real
    - .planning/phases/04-ads-live/04-CONTEXT.md D-05, D-08, D-15
    - .planning/phases/04-ads-live/04-RESEARCH.md "Pitfall 3" — Auto ads can inject units from the mere presence of any AdSense code on the page, so switching it off is a separate, explicit account action and not something any component can enforce
    - .planning/STATE.md "Manual follow-ups" item 1 — the Privacy & messaging site-settings paste has been open since plan 01-09
    - CLAUDE.md — every Vercel command needs `--global-config ~/.vercel-personal --scope loot-list-plus`, and `NEXT_PUBLIC_` values only take effect on a new deploy
  </read_first>
  <action>
Developer-at-keyboard, Claude driving Chrome when a browser connection is available. Five things,
and each one is recorded with what was observed, not just that it was done.

1. **Auto ads OFF.** In the AdSense account, for parseforge.gg, switch Auto ads off. Record the
   toggle state observed after saving. This is the only control that stops Google placing a unit at a
   position no slot reserved — which would land it over a protected element and break the phase's
   first success criterion.

2. **Four fixed-size display units.** Create them as fixed-size display units, never responsive:
   two 728x90 units for the two in-content positions, one 336x280 for the analyze end position, and
   size the phone rendering through the reserved box rather than through a separate unit. Record
   each unit's name and slot id. If AdSense will not issue a fixed-size unit under the current
   account configuration, stop and record that — do not substitute a responsive unit, which would
   void D-04 and make the reserved box a lie.

3. **Put the ids in the code.** Replace the four placeholder `unit` values in `lib/ads.ts` with the
   real slot ids. These are public values that ship in the page HTML; they are not secrets and they
   belong in the repository, not in environment variables. Leave every other field untouched.

4. **Environment.** Add `NEXT_PUBLIC_ADSENSE_PUB_ID` (the publisher id digits) and
   `NEXT_PUBLIC_ADS_ENABLED` set to the string `1` to BOTH the Preview and Production environments of
   the parseforge project, using the personal Vercel config and the loot-list-plus scope. Never print
   the values back. Record only that each variable now exists in each environment, read back with a
   list command that does not decrypt. Note in the summary that neither takes effect until the next
   deploy, because these bake into the client bundle at build time.

5. **Site-level settings.** Verify ads.txt for parseforge.gg in the AdSense Sites view and record the
   status it reports. Then close the follow-up that has been open since Phase 1: paste
   `https://parseforge.gg/privacy` into the European-regulations consent message's site settings and
   record that it saved.

If the AdSense account is not approved for parseforge.gg, or any step cannot be completed, stop and
record exactly which step blocked and what it reported. A half-configured account is recorded as
such — the slots stay inert because `adsConfigured` still sees a placeholder, which is the designed
fail-closed behaviour, not a workaround.
  </action>
  <instructions>
    Claude has already written the four slot entries and the gate that keeps every one of them inert
    until real unit ids exist, and can run every Vercel environment command itself. The things needing
    you are inside the AdSense account, which has no CLI: switch Auto ads off, create the four
    fixed-size display units and read back their slot ids, check the ads.txt status, and paste the
    /privacy URL into the European-regulations message's site settings.
  </instructions>
  <verification>Claude greps lib/ads.ts for remaining placeholder unit ids, lists (never decrypts) both Vercel environments for the two variables, and re-runs the type check and the placement gate.</verification>
  <resume-signal>Say "configured" with the four slot ids, or name the step that blocked.</resume-signal>
  <acceptance_criteria>
    - `lib/ads.ts` contains zero placeholder unit values — every one of the four entries carries a numeric slot id.
    - `vercel env ls preview` and `vercel env ls production` (personal config, loot-list-plus scope) each list `NEXT_PUBLIC_ADSENSE_PUB_ID` and `NEXT_PUBLIC_ADS_ENABLED`; no value is printed or decrypted.
    - The plan summary records: the observed Auto ads toggle state, the four unit names and ids, the ads.txt status string AdSense reported, and confirmation that the /privacy URL saved in the consent message's site settings.
    - `npx tsc --noEmit` and `npm run protected-elements` still exit 0 after the id substitution.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && P=$(grep -c 'PENDING' lib/ads.ts); echo "placeholder-units=$P"; [ "$P" -eq 0 ] || { echo "GATE-FAIL: $P slot entries still carry an unconfigured unit id"; exit 1; }; U=$(grep -cE 'unit: "[0-9]{6,}"' lib/ads.ts); echo "real-unit-ids=$U"; [ "$U" -eq 4 ] || { echo "GATE-FAIL: expected 4 numeric AdSense unit ids, found $U"; exit 1; }; echo unit-ids-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `unit-ids-ok`</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && for ENV in preview production; do OUT=$(vercel env ls "$ENV" --global-config ~/.vercel-personal --scope loot-list-plus 2>&1); for V in NEXT_PUBLIC_ADSENSE_PUB_ID NEXT_PUBLIC_ADS_ENABLED; do printf '%s' "$OUT" | grep -q "$V" || { echo "GATE-FAIL: $V is not set in $ENV"; exit 1; }; echo "$ENV has $V"; done; done; echo env-vars-ok</automated>
    <fails_when>non-zero exit, or any `GATE-FAIL:` line; a passing run ends with `env-vars-ok`</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npm run protected-elements && echo gates-ok</automated>
    <fails_when>non-zero exit, or the absence of `gates-ok` on the last line</fails_when>
    <human-check>The developer confirms, from the AdSense UI itself, that the Auto ads toggle reads off for parseforge.gg and that all four units are fixed-size display units rather than responsive ones — no code in this repository can observe either fact.</human-check>
  </verify>
  <done>Auto ads is off, four fixed-size units exist and their ids are in lib/ads.ts, both env vars exist in Preview and Production without their values being printed, ads.txt status is recorded, and the Phase 1 Privacy & messaging follow-up is closed.</done>
  <reversibility rating="reversible">Every step is a dashboard or environment change that can be undone; the one-way step remains the production deploy in 04-07.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AdSense account configuration → the served page | an account-side setting can place markup this repository never wrote |
| ad slot markup → the six protected share/awards elements | an ad position one element away from a share control is a lost click |
| published privacy policy → visitors and regulators | a public statement about data handling that must be true on the day it ships |
| Vercel environment → the client bundle | `NEXT_PUBLIC_` values bake into shipped JavaScript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering | Auto ads injecting a unit over a protected element | high | mitigate | Task 3 switches Auto ads off as an explicit account action with the toggle state recorded, and the developer confirms it from the UI; an ad appearing at an unreserved position is the detection signal, checked again in 04-06's preview pass |
| T-04-23 | Tampering | an ad slot drifting onto the homepage, a guide page or into a protected-element owner file | high | mitigate | Task 1's gate greps six named files plus the whole guides tree for slot mounts and fails on any hit; `checkNoStraySlots` covers the rest of `app/` |
| T-04-24 | Repudiation | a privacy policy that misdescribes what actually happens | high | mitigate | The disclosure ships in the same deploy as the ads, the developer reads every changed paragraph against actual behaviour as a human check, and the California claim is corrected rather than left unqualified |
| T-04-25 | Information Disclosure | the AdSense publisher id or a unit id being treated as a secret and mishandled | low | accept | Both are public values that ship in page HTML; the unit ids live in the repository deliberately and the publisher id follows the existing public CMP-id env precedent |
| T-04-26 | Information Disclosure | `vercel env` output printing a decrypted value into a transcript | medium | mitigate | Only the listing command is used, never a pull or a decrypt; the gate asserts the variable name is present and never reads its value |
| T-04-27 | Denial of Service | the analyze page's mid slot mounting after the shell and pushing the tab body down | medium | mitigate | The mount is inside the same report-loaded condition as the tab switcher and selectors, so the reserved box appears with the shell; the end slot renders unconditionally as the last child so nothing below it can shift |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks in this plan or this phase (04-RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies). Any later addition must pass `package-legitimacy check` plus a blocking human checkpoint before install; `[SLOP]` packages are forbidden |
</threat_model>

<artifacts_this_phase_produces>
Created or extended by this plan:
- `lib/ads.ts` — `AdSlotId` widened to four members (`tbc-audit-mid`, `tbc-audit-end`,
  `analyze-mid`, `analyze-end`); four `AD_SLOTS` entries with real AdSense unit ids
- `app/tbc-audit/page.tsx` — two `AdSlot` mounts
- `app/analyze/[reportCode]/AnalyzeClient.tsx` — two `AdSlot` mounts
- `docs/PROTECTED-ELEMENTS.md` — four rows in the `## Ad slot placements` table
- `app/privacy/page.tsx` — present-tense advertising disclosure, Warcraft Logs data-source paragraph,
  corrected California statement, `Last updated` date
- Environment variables now set in Vercel Preview and Production: `NEXT_PUBLIC_ADSENSE_PUB_ID`,
  `NEXT_PUBLIC_ADS_ENABLED`
- AdSense account state: Auto ads off; four named fixed-size display units
</artifacts_this_phase_produces>

<verification>
- `npm run protected-elements` green with six `attr:` rows, four `route:` rows and every `adslot:` row passing.
- `npm run seo-invariants`, `npm run token-audit`, `npm run theme-parity`, `npm test`, `npx tsc --noEmit`, `npm run lint` all green.
- Four reserved boxes server-render on the two whitelisted routes with ads configured; zero ad script references in any server-rendered HTML.
- Zero slot mounts outside the two owner files.
</verification>

<success_criteria>
- SC1: every protected element is where the share loop left it, with no ad slot inside a table, adjacent to a share control, or between a share button and the thing it shares — enforced by the gate, not by eye.
- SC2: all four boxes carry exact per-breakpoint pixel dimensions before any script runs.
- The RPGLogs Terms' privacy-policy requirement is satisfied before ads ship, not after.
</success_criteria>

<output>
Create `.planning/phases/04-ads-live/04-05-SUMMARY.md` when done.
</output>
