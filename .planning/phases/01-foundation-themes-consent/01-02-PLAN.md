---
phase: 01-foundation-themes-consent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: false
requirements: [MONY-01]

user_setup:
  - service: google-adsense
    why: "Google Privacy & Messaging is the certified TCF v2.2 CMP (D-01) and is configured inside an AdSense account. The account does not exist yet (D-02) and creation requires the owner's own Google account."
    env_vars:
      - name: NEXT_PUBLIC_GOOGLE_CMP_PUB_ID
        source: "AdSense -> Account -> Settings -> Account information -> Publisher ID (the pub-XXXXXXXXXXXXXXXX value)"
    dashboard_config:
      - task: "Create the AdSense account for parseforge.gg (sign-up only — no ad units, no ad code)"
        location: "https://adsense.google.com/start/"
      - task: "Create and publish a GDPR consent message, geo-targeted to EEA + UK only, using Google's standard full-screen/centre dialog format"
        location: "AdSense -> Privacy & messaging -> European regulations"

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Google AdSense account exists for parseforge.gg and its publisher ID is available to the application as NEXT_PUBLIC_GOOGLE_CMP_PUB_ID (D-02)"
    - "A Google Privacy & Messaging GDPR consent message is created and published for parseforge.gg, geo-targeted to EEA and UK only (D-03), in Google's standard full-screen/centre dialog format (D-04)"
    - "No US-state privacy message is configured, so US visitors and US-IP Googlebot never receive a consent interstitial (D-03)"
    - "The publisher ID is set in the Vercel production environment for the parseforge project and is documented in .env.example"
    - "No ad unit, ad slot or ad script is created in this phase — the account exists solely to host the CMP (MONY-01 before MONY-02)"
  artifacts:
    - path: ".env.example"
      provides: "Documented NEXT_PUBLIC_GOOGLE_CMP_PUB_ID key for local development"
      contains: "NEXT_PUBLIC_GOOGLE_CMP_PUB_ID"
  key_links:
    - from: ".env.example"
      to: "app/layout.tsx"
      via: "NEXT_PUBLIC_GOOGLE_CMP_PUB_ID gates the CMP script tag mounted in plan 01-03"
      pattern: "NEXT_PUBLIC_GOOGLE_CMP_PUB_ID"
  prohibitions:
    - statement: "MUST NOT configure the consent message so that rejecting is harder to reach than accepting, or rely on pre-selected / deceptive consent framing — the published message must offer a reject path at the same level of prominence as accept"
      status: unverified
      verification: judgment
---

<objective>
Stand up the human-only half of MONY-01: create the AdSense account that hosts Google's certified
TCF v2.2 CMP (D-01, D-02), publish an EEA/UK-only GDPR consent message in the full-screen dialog
format (D-03, D-04), and make the resulting publisher ID available to the application.

Purpose: 01-RESEARCH.md Assumption A1 holds that the publisher ID exists immediately after
sign-up, before site-approval review completes — so doing this in wave 1 unblocks the consent
tracer in wave 2 while AdSense's days-to-weeks review runs in the background, which also de-risks
Phase 4.

Output: a live consent message, `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` in Vercel production, and the key
documented in `.env.example`.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-themes-consent/01-CONTEXT.md
@.planning/phases/01-foundation-themes-consent/01-RESEARCH.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|------|---------------|
| env var | `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` (`.env.example`, Vercel production) |
| external resource | Google AdSense account for parseforge.gg |
| external resource | Google Privacy & Messaging GDPR consent message (EEA/UK, full-screen format) |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Create the AdSense account and publish the EEA/UK GDPR consent message</name>
  <reversibility rating="costly">D-01 locks Google Privacy &amp; Messaging as the CMP. Swapping CMPs later means redoing the message configuration, the script loader, the CSP allow-list entries, and Phase 4's ad-integration assumptions that build on this signal. The choice is already made by the developer — recorded, not re-gated.</reversibility>
  <action>Create the AdSense account and publish the EEA/UK GDPR consent message</action>
  <instructions>
    There is no API or CLI for AdSense sign-up or for Privacy &amp; messaging configuration, and both
    require your own Google account — this is the one part of MONY-01 I cannot automate. I have
    prepared everything downstream of it: plan 01-03 wires the consent signal into PostHog and reads
    the publisher ID from `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID`.

    1. Go to https://adsense.google.com/start/ and create an AdSense account for `parseforge.gg`,
       using your Google account. Sign-up only — do NOT create any ad unit and do NOT copy any ad
       code into the site. Ads are Phase 4 (MONY-02); this phase deliberately ships consent first.
    2. Site-approval review can take days to weeks. It runs in the background and does not block the
       rest of this step.
    3. In AdSense -> Account -> Settings -> Account information, copy your Publisher ID. It looks
       like `pub-` followed by 16 digits.
    4. Go to AdSense -> Privacy &amp; messaging -> European regulations, and create a message for
       `parseforge.gg` with:
       - Regions: **EEA and UK only** (D-03). Do NOT add a US-state privacy message — US traffic,
         including US-IP Googlebot, must never see an interstitial.
       - Format: **Google's standard full-screen / centre dialog** (D-04), not a bottom banner.
       - Consent choices: the message must offer a reject path at the same prominence as accept, with
         nothing pre-selected. A message that buries "reject" behind an extra click, or that
         pre-ticks consent, is not acceptable for this project regardless of what the template
         offers.
    5. Publish the message.
    6. Tell me the Publisher ID (`pub-...`) and confirm the message is published with EEA/UK
       targeting and the full-screen format.
  </instructions>
  <verification>
    Once you paste the Publisher ID I will write it to `.env.example` and add it to the Vercel
    production environment via `vercel env add`, then confirm it back with `vercel env ls production`.
  </verification>
  <resume-signal>Paste your Publisher ID (pub-XXXXXXXXXXXXXXXX) and confirm the message is published</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the publisher ID in .env.example and the Vercel production environment</name>
  <precondition>The AdSense publisher ID (pub-XXXXXXXXXXXXXXXX) has been supplied by the developer in the preceding checkpoint</precondition>
  <read_first>
    - `.env.example` — the existing documented env-var list and its comment style
    - `CLAUDE.md` `## Environment / secrets` — the Vercel project (`parseforge`) and team (`loot-list-plus`) the env var belongs to
  </read_first>
  <files>.env.example</files>
  <action>
    Append a `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID=` entry to `.env.example`, matching the surrounding
    comment style, with a one-line comment stating that it is the AdSense publisher ID used to build
    the Google Privacy &amp; Messaging CMP script URL and that it is a public identifier, not a
    secret. Put a placeholder value in the example file — never the real ID (`.env.example` is the
    one env file that IS committed, per `.gitignore`'s `!.env.example` rule).

    Then set the real value in the Vercel production environment:
    `export PATH="$HOME/.local/node20/bin:$PATH"; vercel env add NEXT_PUBLIC_GOOGLE_CMP_PUB_ID production --scope loot-list-plus`
    piping the developer-supplied ID as the value. If the CLI returns an authentication error, that
    is an auth gate, not a failure — raise a `checkpoint:human-action` asking the developer to run
    `vercel login`, then retry.

    Confirm with `vercel env ls production --scope loot-list-plus` and check that
    `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` is listed. Do not print the value itself into the transcript or
    into any file other than the Vercel environment. Note in the SUMMARY that env-var changes only
    take effect on a NEW deploy — plan 01-08 performs that deploy.
  </action>
  <verify>
    <automated>grep -q '^NEXT_PUBLIC_GOOGLE_CMP_PUB_ID=' .env.example && ! grep -Eq '^NEXT_PUBLIC_GOOGLE_CMP_PUB_ID=pub-[0-9]{16}$' .env.example</automated>
    <fails_when>non-zero exit, meaning either the key is absent from .env.example or a real 16-digit publisher ID was committed into the example file instead of a placeholder</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; vercel env ls production --scope loot-list-plus | grep -q 'NEXT_PUBLIC_GOOGLE_CMP_PUB_ID'</automated>
    <fails_when>non-zero exit; the variable name is absent from the `vercel env ls` output, or the command reports "Not authenticated" (an auth gate — raise a checkpoint and retry, do not treat as done)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains a line starting `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID=` with a placeholder, not a real `pub-` identifier
    - `vercel env ls production --scope loot-list-plus` lists `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID`
    - No publisher ID value appears in any committed file
  </acceptance_criteria>
  <done>The publisher ID is available to production builds through `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID`, and the key is documented for local development.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> Vercel environment | A human-supplied identifier is written into the production environment |
| repository -> public git history | `.env.example` is the one env file that is committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | `.env.example`, transcript | low | mitigate | The AdSense publisher ID is a public identifier by design (it appears in the CMP script URL every visitor loads), but the committed example file still carries a placeholder only, and the real value is written solely through `vercel env add`. No live credential is printed or committed |
| T-01-09 | Repudiation | Google Privacy & Messaging message configuration | medium | mitigate | The consent message's regions (EEA/UK only), format (full-screen) and reject-parity are stated explicitly in the checkpoint instructions and confirmed back by the developer, so the configuration that legally gates EEA/UK tracking is an explicit, recorded decision rather than an accepted template default |
| T-01-10 | Elevation of Privilege | AdSense account scope | low | accept | The account is created under the developer's own Google account with no delegated access. Accepted: no shared or service credential is introduced |
</threat_model>

<verification>
- `.env.example` documents the key with a placeholder value
- `vercel env ls production --scope loot-list-plus` lists `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID`
- The developer has confirmed the consent message is published with EEA/UK targeting and the full-screen format
- No ad unit or ad code exists anywhere in the repository (MONY-01 ships before MONY-02)
</verification>

<success_criteria>
- AdSense account exists; publisher ID is known and stored in Vercel production (D-02)
- GDPR consent message published, EEA/UK-targeted (D-03), full-screen format (D-04)
- No US-state privacy message configured
- Zero ad code added to the repository
</success_criteria>

<output>
Create `.planning/phases/01-foundation-themes-consent/01-02-SUMMARY.md` when done
</output>
