---
phase: 01-foundation-themes-consent
plan: 08
type: execute
wave: 6
depends_on: ["01-03", "01-07"]
files_modified:
  - scripts/seo-invariants.mjs
  - package.json
  - docs/OPS-01-SHIP-GATE.md
autonomous: true
requirements: [OPS-01, DSGN-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every shipped route renders readable, correctly styled content in BOTH themes — verified by an explicit manual sweep of all nine routes, because this project has no automated visual-regression harness"
    - "Theme and consent changes introduce no indexing or metadata regression: title, canonical, robots, description and structured-data types match production for every route"
    - "The sitemap pipeline is intact — recordRecentReport, getRecentReports and usingSharedCache are unchanged and still wired from report-meta and sitemap"
    - "The phase's PostHog instrumentation exists at the call sites it claims: theme_changed, consent_resolved and consent_unavailable each have exactly one capture site"
    - "docs/OPS-01-SHIP-GATE.md is a repeatable checklist every later phase runs, not a one-off record of this phase"
    - "Route rows in docs/OPS-01-SHIP-GATE.md are emitted in a stable, specified order — lexicographic by route path — so successive phases produce diffable reports"
    - "A route for which Search Console returns no data (not yet crawled, or zero impressions) is recorded as no-data, never as a pass and never as a silent omission"
    - statement: "Two ship-gate checks that report on the same route do not overwrite each other — docs/OPS-01-SHIP-GATE.md records one row per route carrying both the metadata-diff result and the search-data result"
      verification: backstop
  artifacts:
    - path: "scripts/seo-invariants.mjs"
      provides: "Repeatable local-versus-production head-tag and structured-data diff for every route"
    - path: "docs/OPS-01-SHIP-GATE.md"
      provides: "The OPS-01 ship gate: repeatable checklist plus this phase's recorded evidence"
      contains: "no-data"
    - path: "package.json"
      provides: "seo-invariants npm script"
      contains: "seo-invariants"
  key_links:
    - from: "scripts/seo-invariants.mjs"
      to: "https://parseforge.gg"
      via: "fetches the production head tags to diff the local render against"
      pattern: "parseforge.gg"
    - from: "docs/OPS-01-SHIP-GATE.md"
      to: "scripts/seo-invariants.mjs"
      via: "the checklist names the command that produces the metadata evidence"
      pattern: "seo-invariants"
  prohibitions:
    - statement: "MUST NOT record a ship-gate row as passing without the command output or the Search Console / PostHog evidence that produced it — an unevidenced pass is a claim, not a gate"
      status: unverified
      verification: judgment
---

<objective>
Establish OPS-01 as a repeatable, evidenced gate rather than a habit: verify that this phase's
user-facing changes introduce no indexing or metadata regression, that the PostHog instrumentation
it claims actually exists, and that every route is readable in both themes — then write the
checklist down so every later phase runs the same gate.

Purpose: OPS-01 is a standing invariant across all seven phases, and ParseForge lives on organic
search. 01-UI-SPEC.md flags that there is no automated visual-regression harness (node-environment
vitest only), so the both-theme route sweep has to be an explicitly scheduled manual pass, not an
assumption.

Output: `scripts/seo-invariants.mjs`, and `docs/OPS-01-SHIP-GATE.md` carrying both the reusable
checklist and this phase's evidence.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-themes-consent/01-CONTEXT.md
@.planning/phases/01-foundation-themes-consent/01-UI-SPEC.md
@.planning/phases/01-foundation-themes-consent/01-03-SUMMARY.md
@.planning/phases/01-foundation-themes-consent/01-07-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|------|---------------|
| file | `scripts/seo-invariants.mjs` |
| npm script | `seo-invariants` |
| file | `docs/OPS-01-SHIP-GATE.md` |

<tasks>

<task type="auto">
  <name>Task 1: Run the full local gate and sweep every route in both themes</name>
  <read_first>
    - `package.json` — the `test`, `lint`, `theme-parity` and `token-audit` scripts assembled across this phase
    - `lib/demo-report.ts` — the `DEMO_REPORT` code used for the analyze-route sweep and already listed in the sitemap
    - `.planning/phases/01-foundation-themes-consent/01-UI-SPEC.md` `## UI Considerations` — E3 · zero-one-many, which is why this sweep is manual and enumerated rather than assumed
    - `CLAUDE.md` `## Running node tooling here` — why `npm run build` is left to Vercel and is not part of the local gate
  </read_first>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <action>
    Run the assembled local gate and capture each command's output verbatim for the SUMMARY — the
    prohibition on this plan is that a gate row may only be recorded as passing with the output that
    produced it:
    `export PATH="$HOME/.local/node20/bin:$PATH"`, then `npx tsc --noEmit`, `npm run lint`,
    `npm test`, `npm run theme-parity`, `npm run token-audit`. Do not add `npm run build` — CLAUDE.md
    records that a full Next build prerenders `/` and `/sitemap.xml`, which hit the WCL API and Redis
    and need production environment; that build belongs to Vercel and happens in plan 01-09.

    Start the dev server in the background (`npm run dev`), wait for the ready signal, and confirm it
    responds before presenting anything for review — never hand over a broken verification
    environment.

    Create `docs/OPS-01-SHIP-GATE.md` as a stub in this task with just the route table skeleton, one
    row per route in lexicographic order by path, and a `Theme sweep` column per route filled from
    the sweep below. Task 3 completes the document. Use exactly this route set, which is every
    `page.tsx` under `app/`:
    `/`, `/analyze/{DEMO_REPORT.code}`, `/guides`, `/guides/how-to-analyze-wow-classic-logs`,
    `/guides/improve-dps-wow-classic`, `/guides/raid-preparation-checklist`,
    `/guides/warcraft-logs-vs-parseforge`, `/guides/wow-classic-loot-council-tools`, `/tbc-audit`.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; [ -d node_modules ] || npm ci; npx tsc --noEmit && npm run lint && npm test && npm run theme-parity && npm run token-audit</automated>
    <fails_when>non-zero exit from any of the five; any "error TS" line; any new eslint error; any vitest suite reported as failing; a theme-parity violation line; or an outstanding token-audit finding</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'fetch("http://localhost:3000").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))'</automated>
    <fails_when>non-zero exit — the dev server is not responding on port 3000, so the manual sweep below cannot be performed against a working environment</fails_when>
    <human-check>
      The dev server is running at http://localhost:3000. Visit each of these nine routes and check
      it in BOTH Light and Dark (use the navbar theme control; there is no automated visual-regression
      harness for this project, so this pass is the only coverage):
      1. /
      2. /analyze/{DEMO_REPORT.code} — walk all three tabs (Raid Overview, player analysis, CLA)
      3. /guides
      4. /guides/how-to-analyze-wow-classic-logs
      5. /guides/improve-dps-wow-classic
      6. /guides/raid-preparation-checklist
      7. /guides/warcraft-logs-vs-parseforge
      8. /guides/wow-classic-loot-council-tools
      9. /tbc-audit

      For each, confirm: no unreadable text (nothing near-invisible against its background), no
      unstyled or dark-on-light card, tables and progress bars legible, grade and percentile badges
      readable, class-coloured player names readable, and the navbar and footer correct. Report any
      route and theme combination that fails, naming the element.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run theme-parity` and `npm run token-audit` all exit 0, with output captured in the SUMMARY
    - The dev server responds with 200 on http://localhost:3000
    - `docs/OPS-01-SHIP-GATE.md` exists with one row per route, ordered lexicographically by path
    - Every one of the nine routes has a recorded result for both themes
  </acceptance_criteria>
  <done>The full local gate is green with captured evidence, and every shipped route has been checked in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Build and run the SEO-invariant diff against production, and verify the instrumentation call sites</name>
  <read_first>
    - `app/layout.tsx` — the root `metadata` export, `metadataBase`, canonical and Open Graph configuration
    - `app/analyze/[reportCode]/page.tsx` — `generateMetadata`, the param-free canonical and the noindex/404 branching that CLAUDE.md lists as an SEO invariant
    - `lib/kv-cache.ts` lines 20–30 and 145–185 — `usingSharedCache`, `recordRecentReport`, `getRecentReports`
    - `app/sitemap.ts` and `lib/report-meta.ts` — the two call sites that wire that pipeline together
    - `scripts/token-audit.mjs` — the script structure and mode conventions to mirror
  </read_first>
  <files>scripts/seo-invariants.mjs, package.json</files>
  <action>
    Create `scripts/seo-invariants.mjs`, a plain ESM node script using only `node:` built-ins plus
    global `fetch`. For each route in the nine-route set (the analyze route using `DEMO_REPORT.code`),
    it fetches the local render from a base URL (default `http://localhost:3000`, overridable by a
    `--base` flag) and the corresponding production URL under `https://parseforge.gg`, extracts from
    each the `<title>`, `<link rel="canonical">` href, the `<meta name="robots">` content, the
    `<meta name="description">` content, `og:title`, `og:image` and the sorted list of `@type` values
    from every JSON-LD block, and reports any difference.

    Rules that make it a real gate rather than a formality:
    - Emit **one row per route**, in lexicographic order by route path, so two runs of the gate are
      diffable and a second check on the same route extends that route's row rather than replacing it.
    - A route production returns a non-200 for is recorded as `no-data` — not as a pass and not as a
      silent omission.
    - Exit non-zero when any route shows a difference in canonical, robots or structured-data types.
      A title or description difference is reported but does not fail, because this phase may
      legitimately not change them and a later phase deliberately iterates them (SEO-03).
    - `--report` prints without failing; `--markdown <path>` appends the route table to a file.

    Add `"seo-invariants": "node scripts/seo-invariants.mjs"` to `package.json` `scripts`.

    Run it against the running dev server and record the table.

    Then verify the sitemap pipeline is intact — `usingSharedCache` still exported from
    `lib/kv-cache.ts`, `recordRecentReport` still called from `lib/report-meta.ts`, and
    `getRecentReports` still called from `app/sitemap.ts`. None of this phase's plans touch those
    files, so any change here is an accident.

    Finally verify the instrumentation this phase claims: exactly one `posthog.capture` site each for
    `theme_changed` (in `app/components/ThemeToggle.tsx`), `consent_resolved` and
    `consent_unavailable` (in `app/components/PostHogProvider.tsx`). The live PostHog
    event-definition check needs real traffic and therefore belongs after deploy — record it as a
    post-deploy checklist row in Task 3 rather than claiming it now.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run seo-invariants -- --report | grep -c '^/' | grep -qE '^9$'</automated>
    <fails_when>non-zero exit, or a route-row count other than 9 — the script skipped a route or emitted duplicates, so the table is not one stable row per route</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run seo-invariants</automated>
    <fails_when>non-zero exit — at least one route differs from production in canonical URL, robots directive or structured-data types, which is an indexing regression</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; grep -q 'export const usingSharedCache' lib/kv-cache.ts && grep -q 'export async function recordRecentReport' lib/kv-cache.ts && grep -q 'export async function getRecentReports' lib/kv-cache.ts && grep -q 'recordRecentReport' lib/report-meta.ts && grep -q 'getRecentReports' app/sitemap.ts</automated>
    <fails_when>non-zero exit — one of the three sitemap-pipeline symbols is no longer exported from lib/kv-cache.ts, or one of its two call sites no longer references it, which would silently stop public reports being fed to the sitemap</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; [ "$(grep -rc 'theme_changed' app/components/ThemeToggle.tsx)" = "1" ] && [ "$(grep -rc 'consent_resolved' app/components/PostHogProvider.tsx)" = "1" ] && [ "$(grep -rc 'consent_unavailable' app/components/PostHogProvider.tsx)" = "1" ]</automated>
    <fails_when>non-zero exit — a count other than 1 for any of the three event names, meaning an event this phase promises is missing or is captured from more than one site and would double-count</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/seo-invariants.mjs` exists and `package.json` `scripts` contains `seo-invariants`
    - `npm run seo-invariants -- --report` emits exactly 9 route rows in lexicographic order by path
    - `npm run seo-invariants` exits 0 — no canonical, robots or structured-data difference against production
    - A route production returns non-200 for is reported as `no-data`
    - `usingSharedCache`, `recordRecentReport` and `getRecentReports` are all still exported and still called from `lib/report-meta.ts` and `app/sitemap.ts`
    - `theme_changed`, `consent_resolved` and `consent_unavailable` each appear exactly once at their expected call site
  </acceptance_criteria>
  <done>A repeatable command proves this phase introduces no indexing or metadata regression, the sitemap pipeline is intact, and every promised event has exactly one capture site.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/OPS-01-SHIP-GATE.md as the repeatable gate every later phase runs</name>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` — the stub created in Task 1
    - `.planning/REQUIREMENTS.md` OPS-01 — "enforced as a phase-gate, not a follow-up task"
    - `.planning/ROADMAP.md` — the seven phases that will each re-run this gate
    - `CLAUDE.md` `## Deploying` and `## SEO invariants` — the manual deploy procedure and the invariants the gate protects
  </read_first>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <action>
    Complete `docs/OPS-01-SHIP-GATE.md` with two clearly separated parts.

    **Part 1 — the reusable gate.** A numbered checklist any later phase can run verbatim, each step
    naming the exact command and what its evidence is:
    1. Local gate: `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run theme-parity`,
       `npm run token-audit` — evidence is the captured output.
    2. Both-theme route sweep: every route with a `page.tsx`, checked in Light and Dark — evidence is
       a per-route recorded result.
    3. SEO invariants: `npm run seo-invariants` — evidence is the route table.
    4. PostHog instrumentation: every new user-facing interaction has exactly one capture site, and
       after deploy the event appears in the PostHog project's event definitions — evidence is the
       grep count pre-deploy and the event definition post-deploy.
    5. Search Console: after deploy, URL-inspect the affected routes and confirm indexability and
       metadata are unchanged — evidence is the inspection result per route.
    6. Prod deploy is manual and requires explicit developer confirmation
       (`vercel deploy --prod --scope loot-list-plus --yes`).

    State the three recording rules up front, because they are what make this a gate rather than a
    habit: a row may only be marked passing when the evidence that produced it is recorded alongside
    it; a route Search Console has no data for is recorded as `no-data`, never as a pass and never
    dropped; rows are ordered lexicographically by route path so two runs diff cleanly.

    **Part 2 — Phase 1 evidence.** The completed route table from Tasks 1 and 2: one row per route in
    lexicographic order, carrying both the theme-sweep result and the metadata-diff result in the
    same row (one route, one row — a second check extends the row, it does not replace it). Add the
    local-gate command outputs, the instrumentation grep counts, and a `Pending post-deploy` section
    listing the two rows that genuinely cannot be filled until the change is live: the PostHog event
    definitions and the Search Console inspection. Plan 01-09 fills those.

    Link the report from `docs/TOKEN-AUDIT.md` (or reference it) so the two phase artifacts are
    discoverable together.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; test -s docs/OPS-01-SHIP-GATE.md && grep -qi 'no-data' docs/OPS-01-SHIP-GATE.md && grep -q 'seo-invariants' docs/OPS-01-SHIP-GATE.md && grep -q 'token-audit' docs/OPS-01-SHIP-GATE.md && grep -qi 'Pending post-deploy' docs/OPS-01-SHIP-GATE.md</automated>
    <fails_when>non-zero exit — the file is missing or empty, the no-data recording rule is absent, one of the two audit commands is not named in the checklist, or the post-deploy section that plan 01-09 fills is missing</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'const rows=require("node:fs").readFileSync("docs/OPS-01-SHIP-GATE.md","utf8").split("\n").filter(l=>/^\|\s*\//.test(l)).map(l=>l.split("|")[1].trim()); if(rows.length!==9){console.error("expected 9 route rows, got "+rows.length);process.exit(1)} const sorted=[...rows].sort(); if(JSON.stringify(rows)!==JSON.stringify(sorted)){console.error("route rows are not in lexicographic order");process.exit(1)} if(new Set(rows).size!==rows.length){console.error("duplicate route row");process.exit(1)} console.log("9 route rows, ordered, unique")'</automated>
    <fails_when>non-zero exit; stderr reporting a row count other than 9, rows out of lexicographic order, or a duplicate route row — a duplicate means two checks on the same route produced two rows instead of extending one</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run theme-parity && npm run token-audit && npm run seo-invariants && npx tsc --noEmit && npm test</automated>
    <fails_when>non-zero exit from any of the five — the phase is not in a shippable state and plan 01-09 must not proceed</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` contains a reusable numbered checklist naming `npm run theme-parity`, `npm run token-audit` and `npm run seo-invariants`
    - It states the three recording rules: evidence-with-every-pass, `no-data` for absent search data, lexicographic ordering
    - The Phase 1 evidence table has exactly 9 unique route rows in lexicographic order, each carrying both the theme-sweep and metadata-diff results
    - A `Pending post-deploy` section names the PostHog event-definition check and the Search Console inspection
    - All five gate commands exit 0
  </acceptance_criteria>
  <done>OPS-01 exists as a written, repeatable gate with this phase's evidence recorded against it and the two genuinely-post-deploy rows explicitly deferred rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production site -> local gate | `scripts/seo-invariants.mjs` fetches live HTML from parseforge.gg and parses it |
| gate document -> release decision | `docs/OPS-01-SHIP-GATE.md` is what the deploy decision in plan 01-09 is taken against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23 | Repudiation | `docs/OPS-01-SHIP-GATE.md` | high | mitigate | A gate that can be marked green without evidence protects nothing, and this document is the template six later phases inherit. Mitigated by the evidence-with-every-pass and `no-data` recording rules being stated in the document itself and asserted by the gate commands |
| T-01-24 | Tampering | `scripts/seo-invariants.mjs` HTML parsing | medium | mitigate | The script parses untrusted remote HTML. It only extracts named tag values for comparison, never evaluates fetched content, and writes only to the developer-supplied `--markdown` path — never to a path derived from the fetched HTML |
| T-01-25 | Information Disclosure | dev server + fetches | low | accept | The dev server is bound locally and the production fetches are of public pages. No credential is transmitted or logged. Accepted |
| T-01-26 | Denial of Service | `npm run seo-invariants` production fetches | low | mitigate | Nine sequential GETs of public pages per run — negligible load, and the gate runs once per phase rather than in CI on every commit |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package — the script uses node built-ins and global fetch. 01-RESEARCH.md `## Package Legitimacy Audit` records no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries for this phase |
</threat_model>

<verification>
- `export PATH="$HOME/.local/node20/bin:$PATH"; npx tsc --noEmit && npm run lint && npm test && npm run theme-parity && npm run token-audit && npm run seo-invariants` all exit 0
- The nine-route table in `docs/OPS-01-SHIP-GATE.md` is unique, ordered and complete for both themes
- The sitemap pipeline symbols and their two call sites are unchanged
- Each of the three PostHog events has exactly one capture site
</verification>

<success_criteria>
- Every shipped route is confirmed readable in both themes (DSGN-03 closing check)
- No indexing or metadata regression against production (OPS-01)
- The sitemap pipeline is provably intact
- `docs/OPS-01-SHIP-GATE.md` is a repeatable gate with recorded evidence and explicitly deferred post-deploy rows
</success_criteria>

<output>
Create `.planning/phases/01-foundation-themes-consent/01-08-SUMMARY.md` when done
</output>
