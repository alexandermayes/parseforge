---
phase: 01-foundation-themes-consent
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified:
  - app/components/AbilityPriorityHeatmap.tsx
  - app/components/CLAGearIssues.tsx
  - app/components/ComparisonSummary.tsx
  - app/components/DpsComparison.tsx
  - app/components/GearComparison.tsx
  - app/components/RaidOverview.tsx
  - app/components/TalentComparison.tsx
  - app/analyze/[reportCode]/AnalyzeClient.tsx
  - app/guides/warcraft-logs-vs-parseforge/page.tsx
  - components/ui/animated-shiny-text.tsx
  - components/ui/meteors.tsx
  - scripts/token-audit.mjs
  - docs/TOKEN-AUDIT.md
autonomous: true
requirements: [DSGN-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every shipped route resolves colour, spacing, motion and elevation from Tailwind v4 @theme tokens in BOTH themes — the DSGN-01 token audit report demonstrates it"
    - "No component under app/ or components/ carries a hardcoded Tailwind palette class: the audit command reports zero non-allowlisted findings (D-11, full consolidation, no shortcut)"
    - "The token audit report is generated by the audit command rather than hand-written, so it cannot drift from the codebase it describes"
    - "The audit report lists every allowlisted exception with its reason, so 'no known exceptions' is a claim about a written-down set"
    - "The SEO-critical guide page changes class names only — its headings, copy, internal links and canonical remain byte-identical"
  artifacts:
    - path: "docs/TOKEN-AUDIT.md"
      provides: "Generated DSGN-01 token audit report: @theme category inventory, zero outstanding findings, reasoned allowlist"
      contains: "allowlist"
  key_links:
    - from: "docs/TOKEN-AUDIT.md"
      to: "scripts/token-audit.mjs"
      via: "generated by npm run token-audit -- --markdown"
      pattern: "token-audit"
    - from: "app/components/DpsComparison.tsx"
      to: "lib/constants.ts"
      via: "consumes percentileColor/percentileBg, which now return tier token classes"
      pattern: "percentileColor|percentileBg"
  prohibitions: []
---

<objective>
Close the second half of DSGN-01: drive every remaining hardcoded Tailwind palette class in the
shipped UI to zero and generate the token audit report that demonstrates it.

Purpose: D-11 explicitly rejects a "fix only what breaks in light mode" shortcut — the audit report
must show no known exceptions. This is the last plan that touches component source, and it is what
makes the audit command green for the first time.

Output: eleven components migrated to semantic tokens, and `docs/TOKEN-AUDIT.md` generated with zero
outstanding findings and a reasoned allowlist.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-themes-consent/01-CONTEXT.md
@.planning/phases/01-foundation-themes-consent/01-UI-SPEC.md
@.planning/phases/01-foundation-themes-consent/01-06-SUMMARY.md
@app/globals.css
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|------|---------------|
| file | `docs/TOKEN-AUDIT.md` (generated) |

<tasks>

<task type="auto">
  <name>Task 1: Migrate the seven analysis components to semantic tokens</name>
  <read_first>
    - The `--report` worklist recorded in the plan 01-06 SUMMARY — the authoritative per-file, per-line finding list
    - `app/globals.css` `@theme inline` — the full set of available semantic token utilities: `status-good`, `status-warn`, `status-bad`, `status-info`, `gold-from`, `gold-to`, `arcane-from`, `arcane-to`, `surface-0` through `surface-3`, `tier-artifact` through `tier-common`, plus the shadcn semantics (`foreground`, `muted-foreground`, `background`, `card`, `border`, `primary`, `destructive`)
    - `app/components/TalentComparison.tsx` — the densest file in the worklist; read it before editing to understand what each colour is signalling
    - `.planning/phases/01-foundation-themes-consent/01-CONTEXT.md` D-11 — full consolidation, no shortcut
  </read_first>
  <files>app/components/AbilityPriorityHeatmap.tsx, app/components/CLAGearIssues.tsx, app/components/ComparisonSummary.tsx, app/components/DpsComparison.tsx, app/components/GearComparison.tsx, app/components/RaidOverview.tsx, app/components/TalentComparison.tsx</files>
  <action>
    Replace each palette utility with the semantic token utility whose *meaning* matches the usage —
    do not translate by hue alone. Use this mapping as the default, and deviate only where reading
    the surrounding code shows the colour means something else:

    | Palette family used | Meaning in this codebase | Semantic token utility |
    |---|---|---|
    | emerald / green | a good or passing state | `status-good` |
    | red / rose | a bad or failing state | `status-bad` |
    | blue / sky | informational, neutral highlight | `status-info` |
    | amber / yellow | a warning, or a caution state | `status-warn` |
    | amber / yellow used as brand accent | the gold identity colour | `gold-from` |
    | purple / violet | a performance tier, or the arcane accent | `tier-epic` when it ranks performance, `arcane-from` when it is decorative |
    | indigo | decorative secondary accent | `arcane-from` |
    | zinc / gray / slate / neutral / stone | de-emphasised text or an inert bar fill | `muted-foreground` for text, `surface-3` for a fill |

    Opacity and state suffixes carry over unchanged — a background at 20 percent stays at 20 percent,
    a `hover:` or `dark:` prefix stays on the utility it was on. Where a pair of light/dark variants
    was used to fake theming (a base utility plus a `dark:` override of the same role), collapse it
    to the single semantic token, which already resolves per theme — that is the point of the token.

    Change class names only. Do not restructure JSX, rename props, alter conditional logic, or touch
    anything the worklist does not name. `app/components/RaidOverview.tsx` was already edited in plan
    01-05 for its inline `style` colour reads; this task touches only its `className` strings.

    Run `npm run token-audit -- --report` after each file and confirm that file's findings drop to
    zero before moving to the next.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; [ -d node_modules ] || npm ci; [ "$(npm run token-audit -- --report | grep -E 'app/components/(AbilityPriorityHeatmap|CLAGearIssues|ComparisonSummary|DpsComparison|GearComparison|RaidOverview|TalentComparison)\.tsx' | grep -c 'palette-class')" = "0" ]</automated>
    <fails_when>non-zero exit — a non-zero count, meaning at least one of the seven components still carries a hardcoded palette utility</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npx tsc --noEmit && npm run lint && npm test</automated>
    <fails_when>non-zero exit; any "error TS" line; any new eslint error in the seven touched files (pre-existing findings in components/ui/meteors.tsx and lib/analysis-engine.ts are expected); or any vitest suite reported as failing</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run token-audit -- --report` reports zero `palette-class` findings for all seven files
    - No JSX structure, prop name or conditional expression changed in any of the seven files — the diff is class-name strings only
    - `npx tsc --noEmit`, `npm run lint` and `npm test` all exit 0
  </acceptance_criteria>
  <done>The seven analysis components resolve every colour from a semantic token and therefore render correctly in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the analyze shell, the SEO guide page and the two vendored UI components</name>
  <read_first>
    - The `--report` worklist from the plan 01-06 SUMMARY
    - `app/guides/warcraft-logs-vs-parseforge/page.tsx` — an indexed SEO content page; read it before editing so the diff can be confined to class names
    - `CLAUDE.md` `## SEO invariants` — why this guide page's headings, copy, internal links and canonical must not move
    - `components/ui/meteors.tsx` and `components/ui/animated-shiny-text.tsx` — vendored magicui-style components; `CLAUDE.md` records pre-existing lint debt in the former
  </read_first>
  <files>app/analyze/[reportCode]/AnalyzeClient.tsx, app/guides/warcraft-logs-vs-parseforge/page.tsx, components/ui/animated-shiny-text.tsx, components/ui/meteors.tsx</files>
  <action>
    Apply the same meaning-first mapping table from Task 1 to the four remaining files.

    `app/guides/warcraft-logs-vs-parseforge/page.tsx` is the highest-consequence file in this plan:
    it is an indexed SEO content page and this project lives on organic search. Change class-name
    strings ONLY. Do not touch a heading level, a heading string, body copy, an internal link, the
    metadata export or the canonical. Its findings are all one role — a positive/good state in a
    comparison table — so they map to the same `status-good` token.

    `components/ui/animated-shiny-text.tsx` currently pairs a light-mode and a dark-mode neutral text
    utility to fake theming. Collapse that pair to the single `muted-foreground` token, which
    resolves per theme by itself.

    `components/ui/meteors.tsx`: change only the two palette utilities to the `muted-foreground`
    token. Do NOT fix, reformat or refactor anything else in this file — CLAUDE.md records
    pre-existing lint debt here that is explicitly to be left alone. The token change must not add a
    new eslint finding.

    After the last file, `npm run token-audit` (default mode) must exit 0 for the first time in the
    phase. If it does not, the remaining findings are the honest worklist — close them or, if one is
    a genuine renderer limitation like the Satori case, add an allowlist entry with a written reason
    in `scripts/token-audit.mjs`. Never silence a finding without a reason.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run token-audit</automated>
    <fails_when>non-zero exit — at least one non-allowlisted palette-class or raw-hex finding remains anywhere in app/, components/ or lib/, or one of the four required @theme categories is reported absent</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; git diff --unified=0 -- app/guides/warcraft-logs-vs-parseforge/page.tsx | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -cvE 'className=|class=' | grep -qE '^0$'</automated>
    <fails_when>non-zero exit, or a count above 0 — a changed line in the SEO guide page that does not carry a class attribute, meaning copy, a heading, a link or metadata moved when only class names should have</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npx tsc --noEmit && npm run lint && npm test</automated>
    <fails_when>non-zero exit; any "error TS" line; a new eslint error in components/ui/meteors.tsx beyond the pre-existing findings recorded in CLAUDE.md; or any vitest suite reported as failing</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run token-audit` exits 0 for the whole repository
    - Every changed line in `app/guides/warcraft-logs-vs-parseforge/page.tsx` carries a class attribute
    - `components/ui/meteors.tsx` differs only in its two palette utilities; its pre-existing lint findings are unchanged in count
    - `npx tsc --noEmit`, `npm run lint` and `npm test` all exit 0
  </acceptance_criteria>
  <done>The whole shipped codebase resolves colour from tokens; the audit command is green for the first time and the SEO guide page's indexable content is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the DSGN-01 token audit report</name>
  <read_first>
    - `scripts/token-audit.mjs` — the `--markdown` mode created in plan 01-06 and the shape of its output
    - `.planning/phases/01-foundation-themes-consent/01-CONTEXT.md` D-11 — the "audit report should show no known exceptions" bar this artifact has to meet
  </read_first>
  <files>docs/TOKEN-AUDIT.md</files>
  <action>
    Generate the report with
    `npm run token-audit -- --markdown docs/TOKEN-AUDIT.md`, creating `docs/` in the process. The
    report is generated, never hand-written, so it cannot drift from the codebase it describes.

    Confirm the generated file contains: the `@theme` category inventory showing colour, spacing,
    motion and elevation all present (the four DSGN-01 requires); a findings section reporting zero
    outstanding items; and the allowlist section naming the Open Graph route and the Satori-only hex
    maps, each with its reason.

    If the generator does not already emit a short header explaining what the report is and how to
    regenerate it, add that to `scripts/token-audit.mjs`'s markdown output rather than editing the
    generated file — a hand-edit to a generated artifact is exactly the drift this task avoids.
    (Note that would make `scripts/token-audit.mjs` part of this plan's diff; that is acceptable, and
    no other plan in a later wave touches it.)

    Do not commit any local `.env` file or print any credential while running the generator.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run token-audit -- --markdown docs/TOKEN-AUDIT.md && test -s docs/TOKEN-AUDIT.md</automated>
    <fails_when>non-zero exit; the generator reporting outstanding findings; or docs/TOKEN-AUDIT.md being absent or zero bytes</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; grep -qi 'spacing' docs/TOKEN-AUDIT.md && grep -qi 'motion' docs/TOKEN-AUDIT.md && grep -qi 'elevation' docs/TOKEN-AUDIT.md && grep -qi 'color' docs/TOKEN-AUDIT.md && grep -qi 'allowlist' docs/TOKEN-AUDIT.md && grep -q 'og/route' docs/TOKEN-AUDIT.md</automated>
    <fails_when>non-zero exit — the report omits one of the four required @theme categories, omits the allowlist section, or fails to name the Open Graph route as a recorded exception</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; npm run theme-parity && npm run token-audit && npx tsc --noEmit</automated>
    <fails_when>non-zero exit from any of the three — a theme-parity violation, an outstanding audit finding, or a TypeScript error</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/TOKEN-AUDIT.md` exists, is non-empty, and was produced by `npm run token-audit -- --markdown`
    - The report names all four required `@theme` categories (colour, spacing, motion, elevation) as present
    - The report shows zero outstanding findings and lists the allowlist with a reason per entry
    - `npm run token-audit` and `npm run theme-parity` both exit 0
  </acceptance_criteria>
  <done>A generated, regenerable token audit report demonstrates that the shipped UI resolves colour, spacing, motion and elevation from `@theme` tokens, with the exceptions written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source diff -> indexed SEO page | An edit to an indexed guide page can silently change what Google sees |
| generated report -> compliance claim | `docs/TOKEN-AUDIT.md` is the artifact the phase's success criterion is judged against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Tampering | `app/guides/warcraft-logs-vs-parseforge/page.tsx` | high | mitigate | An accidental copy, heading or link change on an indexed page is an SEO regression on this project's only acquisition channel. Mitigated by a diff gate asserting that every changed line in that file carries a class attribute, plus the metadata regression check in plan 01-08 |
| T-01-21 | Repudiation | `docs/TOKEN-AUDIT.md` | medium | mitigate | A hand-written audit report can claim a state the code does not have. Mitigated by generating the report from the audit command and asserting the same command exits 0 in the same gate |
| T-01-22 | Tampering | `components/ui/meteors.tsx` | low | accept | This vendored file carries pre-existing lint debt CLAUDE.md says to leave alone; the task changes only two class names. Accepted: the lint finding count is asserted unchanged rather than fixed |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. 01-RESEARCH.md `## Package Legitimacy Audit` records no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries for this phase |
</threat_model>

<verification>
- `export PATH="$HOME/.local/node20/bin:$PATH"; npm run token-audit` exits 0 for the whole repository
- `export PATH="$HOME/.local/node20/bin:$PATH"; npm run theme-parity` exits 0
- `export PATH="$HOME/.local/node20/bin:$PATH"; npx tsc --noEmit && npm run lint && npm test` all pass
- Every changed line in the indexed guide page carries a class attribute
- `docs/TOKEN-AUDIT.md` exists, is generated, and reports zero outstanding findings
</verification>

<success_criteria>
- Zero non-allowlisted hardcoded palette classes or raw hex colours remain in `app/`, `components/` or `lib/` (D-11)
- The token audit report demonstrates colour, spacing, motion and elevation all resolving from `@theme` tokens (DSGN-01)
- Every exception is recorded with a reason
- The indexed guide page's content is provably unchanged
</success_criteria>

<output>
Create `.planning/phases/01-foundation-themes-consent/01-07-SUMMARY.md` when done
</output>
