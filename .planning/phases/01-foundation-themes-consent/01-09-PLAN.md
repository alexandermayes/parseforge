---
phase: 01-foundation-themes-consent
plan: 09
type: execute
wave: 7
depends_on: ["01-08"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
autonomous: false
requirements: [OPS-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The phase ships to production only after an explicit developer approval — deploys on this project are manual and outward-facing, never self-authorised from a prior approval or a document"
    - "The consent layer is live: the CMP script tag is present in the production HTML, proving the publisher ID env var took effect on a new deploy"
    - "A Search Console pass confirms no indexing or metadata regression on the live site, closing the OPS-01 gate for this phase"
    - "A route Search Console has no data for is recorded as no-data in the ship-gate document, never as a pass and never dropped from the table"
    - "The PostHog project shows event definitions for theme_changed and consent_resolved after live traffic, confirming the instrumentation reaches the destination and not just the call site"
    - "The post-deploy rows extend their existing route rows in docs/OPS-01-SHIP-GATE.md rather than adding a second row for the same route"
  artifacts:
    - path: "docs/OPS-01-SHIP-GATE.md"
      provides: "Completed OPS-01 gate including the post-deploy Search Console and PostHog evidence"
      contains: "Pending post-deploy"
  key_links:
    - from: "docs/OPS-01-SHIP-GATE.md"
      to: "https://parseforge.gg"
      via: "post-deploy route inspection results recorded per route"
      pattern: "parseforge.gg"
  prohibitions: []
---

<objective>
Ship the phase and close the OPS-01 gate: get explicit developer approval for a production deploy,
deploy through the Vercel CLI, then fill the two gate rows that genuinely required live traffic —
the Search Console indexing pass and the PostHog event definitions.

Purpose: OPS-01 is defined as a phase gate rather than a follow-up task, and "a GSC pass confirms no
indexing/metadata regression" is a success criterion of this phase. Both need the change to be live.
The deploy itself is outward-facing and hard to reverse, so CLAUDE.md requires the developer to
confirm it — that confirmation is the first task here, not an assumption carried over from earlier.

Output: the change live on parseforge.gg, and `docs/OPS-01-SHIP-GATE.md` with no rows left pending.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-themes-consent/01-08-SUMMARY.md
@docs/OPS-01-SHIP-GATE.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|------|---------------|
| deployment | production release of parseforge.gg carrying the theme toggle, light palette, token system and consent layer |
| document section | `docs/OPS-01-SHIP-GATE.md` post-deploy evidence rows |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve or hold the Phase 1 production deploy</name>
  <decision>Deploy Phase 1 to production now, or hold?</decision>
  <context>
    Everything in the phase is built and the local gate is green — typecheck, lint, tests,
    theme-parity, token-audit and the SEO-invariant diff against production all pass, and every route
    has been checked in both themes (see `docs/OPS-01-SHIP-GATE.md`).

    Two things still need a live deploy before OPS-01 can close: the Search Console indexing pass and
    the PostHog event definitions. Also note that `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` was added to the
    Vercel production environment in plan 01-02, and Vercel environment changes only take effect on a
    NEW deploy — so the consent dialog does not exist for EEA/UK visitors until this deploy lands.

    Deploys on this project are manual and are not triggered by git. CLAUDE.md requires explicit
    confirmation before a production deploy, and I will not self-authorise one from a plan or a prior
    approval. What ships is user-visible: a theme toggle in the navbar, a redesigned light palette on
    every route, and a consent dialog for EEA/UK visitors.
  </context>
  <options>
    <option id="deploy-now">
      <name>Deploy to production now</name>
      <pros>Closes the OPS-01 gate this session; activates the consent layer, which also closes the pre-existing EU-consent debt in CONCERNS.md; starts the Search Console clock so any indexing effect is visible sooner; de-risks Phase 4 by getting the CMP live early</pros>
      <cons>Ships a visible redesign of light mode to all traffic at once; if a route reads badly in light mode in a way the manual sweep missed, real visitors see it before you do</cons>
    </option>
    <option id="hold">
      <name>Hold — do not deploy yet</name>
      <pros>Lets you review the diff or run your own preview first; you keep control of the timing relative to traffic patterns</pros>
      <cons>OPS-01 cannot close for this phase — the Search Console and PostHog rows stay pending; the consent layer stays inactive, so the EU-consent debt remains open going into later phases</cons>
    </option>
  </options>
  <resume-signal>Reply "deploy-now" to approve the production deploy, or "hold" to stop here</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy to production and confirm the live site</name>
  <precondition>The developer selected "deploy-now" at the preceding decision checkpoint</precondition>
  <read_first>
    - `CLAUDE.md` `## Deploying` — the exact command, project (`parseforge`) and team (`loot-list-plus`)
    - `docs/OPS-01-SHIP-GATE.md` — the route table whose post-deploy rows this and Task 3 fill
    - `lib/demo-report.ts` — the `DEMO_REPORT` code used for the analyze route
  </read_first>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <action>
    Deploy with
    `export PATH="$HOME/.local/node20/bin:$PATH"; vercel deploy --prod --scope loot-list-plus --yes`
    and capture the deployment URL from the output. If the CLI returns an authentication error, that
    is an auth gate rather than a failure — raise a `checkpoint:human-action` asking the developer to
    run `vercel login`, then retry the same command. Never print or echo any environment value the
    CLI reports.

    Once the deploy reports ready, confirm the live site:
    - Every one of the nine routes returns 200 on `https://parseforge.gg`.
    - The production HTML for `/` contains the CMP script host, which proves
      `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` was picked up by this build. If it is absent, the env var did
      not reach the build — stop and report rather than continuing to the Search Console pass.
    - `https://parseforge.gg/sitemap.xml` returns 200 and still lists report URLs, confirming the
      `getRecentReports` pipeline survived the change.
    - `https://parseforge.gg/robots.txt` returns 200.

    Record the deployment URL and each of these results into the corresponding route rows of
    `docs/OPS-01-SHIP-GATE.md` — extend the existing row for each route, do not append a second row
    for a route that already has one.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'const u="https://parseforge.gg";Promise.all(["/","/guides","/tbc-audit","/sitemap.xml","/robots.txt"].map(p=>fetch(u+p).then(r=>({p,s:r.status})))).then(rs=>{const bad=rs.filter(r=>r.s!==200);if(bad.length){console.error("non-200: "+JSON.stringify(bad));process.exit(1)}console.log("all 200")}).catch(e=>{console.error(e.message);process.exit(1)})'</automated>
    <fails_when>non-zero exit, or a "non-200:" line naming a path — the live site is not serving one of the core routes, the sitemap or robots after the deploy</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'fetch("https://parseforge.gg").then(r=>r.text()).then(h=>{if(!h.includes("fundingchoicesmessages.google.com")){console.error("CMP script host absent from production HTML");process.exit(1)}console.log("CMP script present")}).catch(e=>{console.error(e.message);process.exit(1)})'</automated>
    <fails_when>non-zero exit, or the "CMP script host absent" line — the publisher ID env var did not reach this build, so the consent layer is not actually live and MONY-01 is not satisfied</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'fetch("https://parseforge.gg/sitemap.xml").then(r=>r.text()).then(x=>{const n=(x.match(/<loc>/g)||[]).length;if(n<5){console.error("sitemap has only "+n+" entries");process.exit(1)}console.log(n+" sitemap entries")}).catch(e=>{console.error(e.message);process.exit(1)})'</automated>
    <fails_when>non-zero exit, or a "sitemap has only N entries" line with N below 5 — the self-populating sitemap pipeline collapsed, which is the SEO invariant CLAUDE.md flags as never-break</fails_when>
  </verify>
  <acceptance_criteria>
    - `vercel deploy --prod --scope loot-list-plus --yes` completed and the deployment URL is recorded
    - All nine routes plus `/sitemap.xml` and `/robots.txt` return 200 on the live domain
    - Production HTML for `/` contains the CMP script host
    - `/sitemap.xml` still lists at least five entries
    - `docs/OPS-01-SHIP-GATE.md` route rows are extended in place, not duplicated
  </acceptance_criteria>
  <done>The phase is live on parseforge.gg, the consent layer is active, and the sitemap pipeline is confirmed intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Search Console and PostHog post-deploy checks and close the gate</name>
  <precondition>The production deploy from the preceding task is live and serving the nine routes</precondition>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` — the `Pending post-deploy` section and the three recording rules stated at the top of the document
    - `.planning/phases/01-foundation-themes-consent/01-08-SUMMARY.md` — the pre-deploy metadata baseline each route row is being compared against
  </read_first>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <action>
    **Search Console.** Using the `gscServer` MCP connection, URL-inspect each of the nine routes on
    `https://parseforge.gg` and record, per route: indexability verdict, the canonical Google
    selected, and the crawl/coverage state. Compare each against the pre-deploy baseline recorded in
    plan 01-08. A route Search Console returns no data for — not yet crawled since the deploy, or
    zero impressions — is recorded as `no-data`. That is the honest result and it is neither a pass
    nor a drop: it stays in the table with that value, and the document's recording rules say so.
    If the MCP connection is unavailable, record the rows as `no-data` with the reason and say so
    plainly in the SUMMARY; do not substitute an assumption.

    **PostHog.** Using the PostHog MCP `exec` tool with the `event-definition` command, check whether
    `theme_changed` and `consent_resolved` now appear as event definitions in the project. Newly
    deployed events only materialise once real traffic fires them, so an absent definition shortly
    after deploy is `no-data`, not a failure — record it as such together with the timestamp, so the
    row can be revisited rather than silently assumed. `consent_unavailable` will only appear if a
    visitor's CMP was actually blocked, so record it as `no-data` unless it is present.

    **Close the gate.** Fold both result sets into the existing per-route rows — one route, one row,
    extended in place — remove the `Pending post-deploy` heading only once both its rows carry a
    value (including `no-data`), keep the lexicographic ordering, and add a dated sign-off line
    stating which gate steps passed, which are `no-data`, and the evidence for each. Do not mark any
    row as passing without the inspection output or event-definition result that produced it.
  </action>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; node -e 'const t=require("node:fs").readFileSync("docs/OPS-01-SHIP-GATE.md","utf8"); const rows=t.split("\n").filter(l=>/^\|\s*\//.test(l)).map(l=>l.split("|")[1].trim()); if(rows.length!==9){console.error("expected 9 route rows, got "+rows.length);process.exit(1)} if(new Set(rows).size!==rows.length){console.error("duplicate route row - a post-deploy check added a row instead of extending one");process.exit(1)} const sorted=[...rows].sort(); if(JSON.stringify(rows)!==JSON.stringify(sorted)){console.error("route rows are not in lexicographic order");process.exit(1)} if(/Pending post-deploy/.test(t)){console.error("Pending post-deploy section still present");process.exit(1)} console.log("gate closed: 9 unique ordered route rows, no pending section")'</automated>
    <fails_when>non-zero exit; stderr reporting a row count other than 9, a duplicate route row, rows out of lexicographic order, or the "Pending post-deploy" section still being present — any of which means the gate was left open or a second check overwrote rather than extended a route's row</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; grep -qiE 'no-data|indexable|coverage' docs/OPS-01-SHIP-GATE.md && grep -q 'theme_changed' docs/OPS-01-SHIP-GATE.md && grep -q 'consent_resolved' docs/OPS-01-SHIP-GATE.md</automated>
    <fails_when>non-zero exit — the document carries no Search Console verdict vocabulary at all, or one of the two events this phase promises is not named in the evidence, meaning a required row was dropped rather than recorded</fails_when>
    <human-check>
      On the live site (https://parseforge.gg), confirm the shipped result:
      1. The navbar theme control is present and switching Light / Dark / System works and persists.
      2. From an EEA/UK IP (VPN), a fresh profile shows Google's consent dialog; accepting and
         rejecting both leave the site usable.
      3. From a US IP, no dialog appears.
      4. Spot-check two guide routes and the analyze page in light mode on a phone.
      Report anything that looks wrong; a rollback is `vercel rollback` against the previous
      production deployment.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` has 9 unique route rows in lexicographic order, each carrying its theme-sweep, metadata-diff and Search Console results
    - No `Pending post-deploy` section remains
    - Every row is either an evidenced pass or an explicit `no-data`, with no blank cells
    - `theme_changed` and `consent_resolved` both appear in the evidence with a recorded result
    - A dated sign-off line states which steps passed and which are `no-data`
  </acceptance_criteria>
  <done>OPS-01 is closed for Phase 1 with recorded evidence, and the gate document is the template every later phase re-runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine -> production | The Vercel CLI publishes a build to the live domain |
| MCP connections -> gate document | Search Console and PostHog results become the evidence a release decision is recorded against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27 | Elevation of Privilege | production deploy | high | mitigate | A production deploy is outward-facing and hard to reverse. Gated behind a `blocking-human` decision checkpoint that is never auto-approved in any mode, so an unattended run cannot ship on its own authority. The rollback command is stated in the human check |
| T-01-28 | Repudiation | post-deploy gate rows | high | mitigate | An unavailable Search Console or PostHog result is recorded as `no-data` with a reason, never as a pass and never dropped. Asserted by a gate that fails on a blank or missing row and on the pending section surviving |
| T-01-29 | Denial of Service | live consent layer | medium | mitigate | The deploy activates a third-party script for all traffic. Verified by asserting the CMP host is present in production HTML and that all nine routes plus the sitemap and robots still return 200; the report-only CSP means a mis-scoped policy surfaces as a violation report rather than blocking the page |
| T-01-30 | Information Disclosure | Vercel CLI output | medium | mitigate | Deploy output can echo environment configuration. The task explicitly forbids printing environment values, and no `.env` file is created or committed |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. 01-RESEARCH.md `## Package Legitimacy Audit` records no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries for this phase |
</threat_model>

<verification>
- The production deploy completed and all nine routes plus `/sitemap.xml` and `/robots.txt` return 200
- The CMP script host is present in production HTML, proving the publisher ID reached the build
- `docs/OPS-01-SHIP-GATE.md` has 9 unique, ordered route rows with no blank cells and no pending section
- Every row is an evidenced pass or an explicit `no-data`
- The `<human-check>` confirms the theme control, the EEA/UK dialog and the US no-dialog behaviour on the live site
</verification>

<success_criteria>
- The phase is live on parseforge.gg, deployed only after explicit developer approval
- The consent layer is active for EEA/UK visitors (MONY-01)
- A Search Console pass confirms no indexing or metadata regression, or records `no-data` honestly (OPS-01)
- PostHog shows the phase's events, or records `no-data` with a timestamp
- The OPS-01 ship gate document is complete and reusable by every later phase
</success_criteria>

<output>
Create `.planning/phases/01-foundation-themes-consent/01-09-SUMMARY.md` when done
</output>
