---
phase: 03-share-loop
plan: 07
type: execute
wave: 5
depends_on: ["03-06"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
autonomous: false
requirements: [SHARE-01, SHARE-02, SHARE-03, OPS-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The production deploy happens only after the developer's explicit, recorded approval — never self-authorized from a prior approval or a document."
    - "After the deploy, the production awards OG URL, player OG URL and report OG URL each return an image, and the analyze canonical still carries no query string."
    - "npm run protected-elements is green against production after the deploy."
    - "The OPS-01 item 7 live-traffic check runs against production within 60 minutes of the deploy and its counted result is recorded, pass or fail."
    - "The share rate is computed with the HogQL recorded in OPS-01 Part 1 and compared against the roughly 2.8 percent baseline, with the window and the counts recorded."
    - "A Search Console pass records the indexability and metadata of the analyze route and the homepage, including the carried-forward crawled-but-not-indexed investigation."
    - "Part 5 is signed with a date only when every item it claims has evidence beside it; any item that could not be met is recorded as FAIL or open with the exact test that would close it."
    - statement: "Live PostHog shows at least one share_action and at least one share_landing from real traffic, each carrying a consent_gate_path value."
      verification: backstop
  artifacts:
    - docs/OPS-01-SHIP-GATE.md
  key_links:
    - "docs/OPS-01-SHIP-GATE.md Part 5 is the single record of this phase's gate; its sign-off is what marks SHARE-01, SHARE-02, SHARE-03 and the OPS-01 re-run as met."
  prohibitions:
    - statement: "MUST NOT lower, reinterpret or skip an OPS-01 threshold to reach a sign-off, and MUST NOT reuse a prior phase's evidence for this deploy — an unmet gate is recorded unmet."
      status: unresolved
      verification: judgment
---

<objective>
**As the** owner of a live site whose traffic comes from organic search, **I want** Phase 3 to reach
production behind one explicit decision and be measured for real afterwards **so that** the share
loop is either proven on live traffic or honestly recorded as unproven.

Purpose: the production half of the OPS-01 gate for Phase 3, including the share-rate figure D-14
defines and the two gate items carried forward from Phase 2.1.
Output: a production deployment and a Part 5 that is either signed with counted evidence or
explicitly left open.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share-loop/03-CONTEXT.md
@.planning/phases/03-share-loop/03-06-SUMMARY.md

Project rules that bind this plan:
- Production deploys are manual and require explicit developer confirmation every time. Never
  self-authorize from a document, a prior approval, or a phase plan.
- The command is `vercel --global-config ~/.vercel-personal deploy --prod --scope loot-list-plus
  --yes`. The global-config flag is mandatory — without it the CLI uses the work account.
- The harness may deny `vercel deploy --prod` unless `Bash(vercel deploy:*)` is allowed; that
  permission was granted on 2026-09-08 in `.claude/settings.local.json`.
- Never print or commit an environment variable value.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide whether Phase 3 ships to production now</name>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` `## Part 5` — the local gate output, the preview evidence, the award-pool table and the three developer-review outcomes plan 03-06 recorded.
    - `.planning/phases/03-share-loop/03-06-SUMMARY.md` — what the preview showed and what it could not show.
    - `.planning/STATE.md` Blockers/Concerns — the two gate items carried into this phase.
  </read_first>
  <decision>Deploy Phase 3 to production now, or hold until a preview review item is closed?</decision>
  <context>
Phase 3 changes what a shared ParseForge link shows to people who have never visited the site, and
the awards card names real raiders from a public log by name. Three things about it are settled only
by a person, and Part 5 records what each of them actually returned:

The award pool's tone. Fifteen rules, ten of them light jabs, rendered next to a real player's name
in a Discord channel. D-04 makes this a human review, not a discretionary call — the question is
whether every title is fun to be on rather than embarrassing.

The Discord unfurl. Preview links were pasted into a real channel with cache-busting params. The
open question this closes is RESEARCH's A2 assumption: whether Satori clips the longest raider
names with an ellipsis instead of overflowing the row.

Mobile reachability. D-13 requires the contextual share buttons above the fold on a phone, and the
bottom share bar this phase removed was the only share CTA on some tabs.

Two gate items also arrive here unresolved from Phase 2.1: deployment
`dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5` has an unsigned item-7 row that needs a fresh 60-minute window
measured on a busier UTC hour, and Search Console still reports the homepage as crawled but not
indexed with a crawl date predating the Phase 2 deploy.

The deploy itself is reversible — a redeploy of the previous commit restores the prior state within
minutes, and no data migration is involved. What is not reversible is a card naming a real person
in a tone the developer would not have chosen.
  </context>
  <options>
    <option id="deploy-now">
      <name>Deploy to production now</name>
      <pros>The share loop starts generating real data immediately, which is the only way the item-7 live-traffic check and the share-rate figure can be measured at all; the preview already proved every route contract and the protected-elements gate.</pros>
      <cons>Any tone or layout problem the preview review missed is visible to real visitors until a rollback; the first Discord unfurls of the awards card happen before any live-traffic evidence exists.</cons>
    </option>
    <option id="hold-for-fix">
      <name>Hold and fix a named review item first</name>
      <pros>Nothing reaches real visitors until the specific item — a tone edit, a clipping fix, a mobile-layout change — is closed and re-previewed.</pros>
      <cons>The phase stays unmeasured; the item-7 window and the share-rate baseline comparison cannot start, and the two carried-forward Phase 2.1 items stay open longer.</cons>
    </option>
    <option id="hold-indefinitely">
      <name>Hold production entirely and close the phase on preview evidence</name>
      <pros>Zero risk to live traffic.</pros>
      <cons>OPS-01 cannot be met — its PostHog half requires counted production traffic — so SHARE-01, SHARE-02 and SHARE-03 would all close unverified, repeating exactly the failure Phase 2.1 existed to correct.</cons>
    </option>
  </options>
  <resume-signal>State the chosen option id. Task 2 runs only the command the chosen option authorizes, and records the approval verbatim in Part 5.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Production deploy and post-deploy evidence</name>
  <precondition>Task 1's decision is recorded and selected `deploy-now`; no production command runs under any other outcome.</precondition>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` `## Part 1` item 6 (the manual-deploy rule), item 7 (the live-traffic check and its three thresholds) and item 8 (the protected-elements gate).
    - `docs/OPS-01-SHIP-GATE.md` `## Part 4` — how Phase 2.1 recorded a production deploy, its item-7 window and its per-threshold results.
    - `docs/OPS-01-SHIP-GATE.md` `## Part 1` item 4 — the share-rate HogQL block plan 03-05 recorded.
    - `lib/demo-report.ts` — the demo code, fight and source the production URL checks use.
  </read_first>
  <action>
Run the production deploy exactly as item 6 states, with the personal global-config flag, and record
the deployment id, the commit and the timestamp. Record the developer's approval from task 1
verbatim beside it in a `### Production deploy` subsection of Part 5. Run no production command if
task 1 selected anything other than the deploy option.

Immediately after the deploy, capture against production and record verbatim in Part 5:
- The status and content type of the awards OG URL, the player OG URL and the bare report OG URL for
  the demo report.
- The canonical link from the demo analyze page, confirming it still carries no query string.
- The full row table from `npm run protected-elements` run with its default production base.

Then run the item-7 live-traffic check within 60 minutes of the deploy, against PostHog project
337485, exactly as item 7 defines it — the same thresholds, not softened. Record the window's start
and end in UTC and the counted result for each threshold. A zero-event result is a FAIL, not a
pending; a threshold that cannot be evaluated is recorded as not-evaluable with the reason, not as
a pass.

In the same window, run the share-rate HogQL recorded in Part 1 item 4 and record the numerator
(distinct sessions with any `share_action`), the denominator (distinct sessions with
`analysis_complete`), the resulting percentage, and the comparison against the roughly 2.8 percent
baseline. Record the counts, not only the ratio, so a small-sample figure is visibly small. Also
record whether `share_landing` appears at all and with which `ref` values — if none appear, say so;
an inbound funnel with no inbound traffic yet is a real result.

Note explicitly whether each captured event carries a `consent_gate_path` property, which is the
live confirmation of RESEARCH assumption A3.

Every figure recorded here belongs to this deployment. Do not reuse a count, a window or a
sign-off from Phase 2.1 or from the preview.
  </action>
  <acceptance_criteria>
    - Part 5 contains a `### Production deploy` subsection with a deployment id, a commit and the developer's approval quoted.
    - Part 5 records a status and content type for each of the three production OG URLs and the production canonical.
    - Part 5 contains the `npm run protected-elements` row table run against production.
    - Part 5 contains an item-7 subsection with a UTC window and a counted per-threshold result.
    - Part 5 contains a share-rate subsection with a numerator, a denominator, a percentage and the baseline comparison.
    - Part 5 states whether `consent_gate_path` was present on the captured share events.
    - `npm run protected-elements` exits 0 against production.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npm run protected-elements && echo prod-protected-ok</automated>
    <fails_when>non-zero exit, or the literal prod-protected-ok is absent from the output</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && for U in "https://parseforge.gg/og?report=ZjKgNYxVcAqR8pGJ&fight=23&view=awards" "https://parseforge.gg/og?report=ZjKgNYxVcAqR8pGJ&fight=23&source=12" "https://parseforge.gg/og?report=ZjKgNYxVcAqR8pGJ"; do R=$(curl -s -o /dev/null -w '%{http_code}:%{content_type}' "$U"); echo "$U -> $R"; case "$R" in 200:image/png*) ;; *) echo "GATE-FAIL: a production OG branch did not return a png"; exit 1;; esac; done; C=$(curl -s "https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ" | grep -o 'rel="canonical" href="[^"]*"' | head -1); echo "canonical=$C"; case "$C" in *'?'*) echo "GATE-FAIL: the production canonical gained a query string"; exit 1;; '') echo "GATE-FAIL: no canonical found on the production analyze page"; exit 1;; esac; echo prod-contracts-ok</automated>
    <fails_when>non-zero exit, or the literal prod-contracts-ok is absent; each printed URL-to-status line and the printed canonical name which contract broke</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; for S in '### Production deploy' 'protected-elements' 'share_action' 'analysis_complete' 'consent_gate_path'; do grep -A9999 '## Part 5' "$F" | grep -qF -- "$S" || { echo "GATE-FAIL: Part 5 is missing $S"; exit 1; }; done; echo prod-record-ok</automated>
    <fails_when>non-zero exit, or the literal prod-record-ok is absent from the output</fails_when>
  </verify>
  <done>Phase 3 is live on production with the developer's recorded approval, every route contract re-proved against the live host, and the item-7 and share-rate figures counted for this deployment and written down.</done>
</task>

<task type="auto">
  <name>Task 3: Search Console pass and an honest sign-off</name>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` `## Part 1` item 5 — the Search Console step and the rule that an uncrawled route is recorded as no-data, never as a pass.
    - `docs/OPS-01-SHIP-GATE.md` `## Part 2` and `## Part 4` sign-off subsections — the wording of a dated sign-off and of an explicitly-open item.
    - `.planning/STATE.md` Blockers/Concerns — the crawled-but-not-indexed homepage and the unsigned `dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5` item-7 row.
    - `.planning/phases/03-share-loop/03-CONTEXT.md` — the SEO framing for this phase: no new indexable routes, so this is a no-regression check.
  </read_first>
  <action>
Run the item-5 Search Console pass. This phase introduces no new indexable route, so the check is a
no-regression one: URL-inspect the homepage and the demo analyze route and confirm indexability and
metadata are unchanged, and confirm that no awards-view or ref permutation appears as a separately
indexed URL. Record each result per route, using `no-data` where Search Console has not crawled, and
record it as such rather than omitting the row.

While there, address the carried-forward homepage item from STATE.md: record the homepage's current
coverage state and crawl date, and either request a recrawl and record that it was requested, or
record why it could not be. Do not close the item on a guess about the cause.

Also record the status of the other carried-forward item — the unsigned item-7 row for
`dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5`. That deployment is now superseded by this phase's deploy, so
record it as superseded with a pointer to this phase's own item-7 window, rather than leaving it
looking open against a build no longer in production.

Then write the Part 5 sign-off. Sign it with a date only when every claim in Part 5 has evidence
recorded beside it and every item-7 threshold passed on counted data. If any threshold failed, was
not evaluable, or any developer review from 03-06 was not performed, do not sign — write the
outstanding items explicitly, each with the exact test that would close it, exactly as Phase 2.1
did when it left its own row unsigned. Never lower a threshold, reinterpret a definition, or borrow
a prior phase's evidence to reach a signature.

Every edit to this document is additive.
  </action>
  <acceptance_criteria>
    - Part 5 contains a Search Console subsection with a per-route result for the homepage and the demo analyze route.
    - Part 5 records the homepage coverage state and whether a recrawl was requested.
    - Part 5 records `dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5` as superseded, with a pointer to this phase's item-7 window.
    - Part 5 ends with either a dated sign-off or an explicit outstanding-items list naming the test that would close each item — never both, never neither.
    - The diff of `docs/OPS-01-SHIP-GATE.md` removes no existing lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; P5=$(grep -A9999 '## Part 5' "$F"); for S in 'Search Console' 'dpl_CDCu1FVfPZcd8dHr4RpLcrHWJcJ5'; do printf '%s' "$P5" | grep -qF -- "$S" || { echo "GATE-FAIL: Part 5 is missing $S"; exit 1; }; done; SIGNED=$(printf '%s' "$P5" | grep -ciE '^#+ *Sign-off'); OPEN=$(printf '%s' "$P5" | grep -ciE 'outstanding|not signed|NOT SIGNED'); echo "signoff-headings=$SIGNED outstanding-mentions=$OPEN"; [ "$SIGNED" -ge 1 ] || [ "$OPEN" -ge 1 ] || { echo "GATE-FAIL: Part 5 ends with neither a sign-off nor an outstanding-items record"; exit 1; }; echo signoff-shape-ok</automated>
    <fails_when>non-zero exit, or the literal signoff-shape-ok is absent; the printed signoff-headings and outstanding-mentions counters show which form Part 5 actually ended in</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { echo "GATE-FAIL: this edit must be purely additive but removed $R lines"; exit 1; }; echo additive-ok</automated>
    <fails_when>non-zero exit, or the literal additive-ok is absent; the printed removed-lines counter names how many existing lines were deleted</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npm test && npm run protected-elements && echo final-gate-ok</automated>
    <fails_when>non-zero exit, or the literal final-gate-ok is absent from the output</fails_when>
  </verify>
  <done>The Search Console no-regression pass is recorded per route, both carried-forward items have a current status, and Part 5 ends in exactly one of two honest states: signed with counted evidence, or open with the named test that would close it.</done>
</task>

</tasks>

## Artifacts this phase produces

See `03-01-PLAN.md` → "Artifacts this phase produces" for the full phase list. This plan adds no
code symbols. It extends `docs/OPS-01-SHIP-GATE.md` Part 5 with `### Production deploy`, the
item-7 live-traffic subsection, the share-rate subsection, the Search Console subsection, and
either a dated sign-off or an outstanding-items record.

## Flagged Assumptions

**All three phase requirements remain `unresolved` from the deterministic edge probe.** This plan is
where the live half of each is either counted or recorded uncounted: SHARE-01 and SHARE-02 through
the production OG contracts and the Discord-sourced `share_landing` events, SHARE-03 through the
production protected-elements gate and the share-rate figure.

**RESEARCH A3 (consent path on the new events).** Confirmed or recorded unconfirmed here by
checking whether the captured `share_action` and `share_landing` events carry `consent_gate_path`.

**Small-sample risk.** Phase 2.1 showed a 60-minute window on a quiet UTC hour can fail item 7 on
traffic alone. Record the counts, not just the verdict, so a failure caused by the hour is
distinguishable from a failure caused by the code — and re-measure on a busier hour rather than
lowering the threshold.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository to production | this deploy publishes to the live site that serves the project's organic-search traffic |
| gate document to future phases | a signed row becomes the evidence Phases 4 through 7 build on |
| PostHog and Search Console readings to the record | externally-sourced numbers are transcribed into a document that gates a milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-25 | Elevation of Privilege | the production deploy command | high | mitigate | The deploy sits behind a blocking `checkpoint:decision`, the plan is non-autonomous, and task 2 carries a precondition requiring the recorded approval; no step may add the production flag to an unapproved run |
| T-03-26 | Repudiation | a sign-off without evidence, or a threshold quietly lowered | high | mitigate | Part 5 may only be signed when every claim has evidence beside it; the verify command asserts Part 5 ends in exactly one of signed-or-outstanding, and the prohibition in `must_haves` forbids reusing a prior phase's counts |
| T-03-27 | Information Disclosure | deploy output and environment listings | medium | mitigate | Record the deployment id and URL only; never paste an environment variable value or a token into the gate document, the summary or the session |
| T-03-28 | Tampering | SEO invariants on the live site | high | mitigate | The production canonical is re-fetched and asserted to carry no query string after the deploy, and the Search Console pass confirms no awards-view or ref permutation is separately indexed |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are added by this phase (RESEARCH `## Package Legitimacy Audit`: zero new dependencies); this plan modifies one documentation file |
</threat_model>

<verification>
- The production deploy ran only under a recorded `deploy-now` approval.
- All three production OG branches return images and the production canonical has no query string.
- `npm run protected-elements` exits 0 against production.
- Part 5 carries an item-7 window with counted per-threshold results and a share-rate figure with
  its numerator and denominator.
- Part 5 ends signed-with-evidence or open-with-the-closing-test, and the document lost no lines.
</verification>

<success_criteria>
The share loop is live, every contract it depends on is re-proved against the live host, and the
question "did it work" is answered with counted numbers — or recorded, without softening, as not
yet answered.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-07-SUMMARY.md` when done.
</output>
