---
phase: 03-share-loop
plan: 10
type: execute
wave: 8
depends_on: ["03-08", "03-09"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [SHARE-01, SHARE-02, SHARE-03, OPS-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/OPS-01-SHIP-GATE.md Part 5 carries a dated `### Part 5 close-out` subsection restating all nine closing-table rows, each with its counted outcome and a pointer to the dated subsection that carries its evidence."
    - "Part 5 ends in exactly one state: `### Phase 3 Sign-off — SIGNED (<date>, gap closure 03-10)`, written only when every one of the nine rows is a counted PASS or an explicit, accepted, dated FAIL or re-deferral with its closing test named; or `### Phase 3 Sign-off — LEFT OPEN (<date>, gap closure 03-10)` naming the single remaining test. Never both, never neither."
    - "The share-rate row records either a re-run whose query timestamp is on or after 2026-09-23T09:15Z with its numerator, denominator, percentage and the ~2.8% baseline comparison (D-14), or the developer's recorded decision to sign with the first reading of 0/19 standing and the re-read scheduled."
    - ".planning/REQUIREMENTS.md carries `### OPS-01 Addendum 4 (<date>, Phase 3 gap closure 03-10)` superseding Addendum 3, with Addenda 1, 2 and 3 and the text `Phase 1 gate closed 2026-09-06` left intact."
    - ".planning/REQUIREMENTS.md carries a dated second SHARE-01/SHARE-02/SHARE-03 addendum superseding the 2026-09-16 one, stating which backstops are now closed and which remain — appended, never rewritten in place."
    - ".planning/WINDOWS.md entries #9, #10 and #11 each read either closed in the ledger with a pointer to the subsection carrying their closing evidence, or open with a dated disposition written into the Part 5 close-out."
    - "Every edit to docs/OPS-01-SHIP-GATE.md and .planning/REQUIREMENTS.md is additive: `git diff -U0` on each removes zero lines. The `.planning/WINDOWS.md` status mutation is exempt — `gsd-tools windows fixed <id>` rewrites the status cell by design."
    - statement: "The D-14 share-rate HogQL is re-run against PostHog project 337485 on or after 2026-09-23T09:15Z, once a full 7 days of live share_action exposure exists, and its counted numerator, denominator and percentage are compared against the ~2.8% baseline."
      verification: backstop
  artifacts:
    - docs/OPS-01-SHIP-GATE.md
    - .planning/REQUIREMENTS.md
    - .planning/WINDOWS.md
  key_links:
    - "docs/OPS-01-SHIP-GATE.md Part 5's dated sign-off is what marks SHARE-01, SHARE-02, SHARE-03 and the OPS-01 Phase 3 re-run as met; `.planning/REQUIREMENTS.md`'s Addendum 4 and the second SHARE addendum are what carry that status into the requirements ledger, and `.planning/WINDOWS.md` #9/#10/#11 are what keep any still-open follow-up visible across phases."
  prohibitions:
    - statement: "MUST NOT write a sign-off while any of the nine rows lacks counted evidence or an explicit, accepted, dated FAIL or re-deferral naming its closing test."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT run the share-rate HogQL before 2026-09-23T09:15Z, and MUST NOT present a pre-date reading as the scheduled re-run."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT borrow, lower, reinterpret or re-date a count, window, threshold or verdict from another deployment, window, session or phase in order to reach a signature."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT replace, reword or delete existing REQUIREMENTS.md addenda or traceability rows; a correction is a new dated addendum that supersedes the old one in place."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT mark a WINDOWS.md entry closed whose closing evidence was not counted, and MUST NOT waive one in place of closing it."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT print, copy, move or paste the value of any credential — Vercel tokens, PostHog keys, .env* contents."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT change any application code, and MUST NOT run a production deploy; a belief that a successor deployment is needed is a blocking checkpoint:decision, never a self-authorized command."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT fold the 03-REVIEW.md findings WR-01, WR-02, WR-03 or IN-01 into this closure — they belong to /gsd-code-review --fix."
      status: unresolved
      verification: judgment
---

<objective>
**As** the owner of a live site, **I want to** close the OPS-01 Phase 3 gate with a signature that
means something, **so that** SHARE-01, SHARE-02 and SHARE-03 stop being checkboxes ahead of their
own evidence and Phase 4 inherits a true record rather than an optimistic one.

Purpose: plans 03-08 and 03-09 drove eight of the nine Part 5 rows to a dated outcome. This plan
settles the ninth — the D-14 share rate, whose meaningful re-read is gated to on/after
2026-09-23T09:15Z — then writes the close-out, the signature (or an honest open state), the
REQUIREMENTS addenda, and the WINDOWS ledger dispositions.

The share-rate date gate is why this plan is separate: nothing in 03-08 or 03-09 should wait a week
for it, and nothing here may fake it by re-reading a window with no exposure in it.

Output: one close-out subsection plus one sign-off heading in Part 5, two new dated addenda in
`.planning/REQUIREMENTS.md`, and the `.planning/WINDOWS.md` dispositions.

**No application code changes in this plan.** No deploy. No package installs.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-share-loop/03-VERIFICATION.md
@.planning/phases/03-share-loop/03-08-SUMMARY.md
@.planning/phases/03-share-loop/03-09-SUMMARY.md

Project rules that bind this plan:
- `node`/`npm`/`npx` are not on the default PATH. Prepend `$HOME/.local/node20/bin` before any of
  them.
- Never print or commit an environment variable or credential value.
- `docs/OPS-01-SHIP-GATE.md` and `.planning/REQUIREMENTS.md` are edited by appending dated
  sections, never by rewriting existing text. `.planning/WINDOWS.md` is the one exception in this
  plan: its status cell is mutated through the ledger tool, which rewrites that cell by design.
</context>

<who_holds_which_tool>
- **The gsd-executor running this plan has** `Read, Write, Edit, Bash, Grep, Glob, Skill,
  mcp__context7__*`. It cannot run HogQL.
- **The execute-phase orchestrator session has** `mcp__posthog__exec` against project `337485`.
- **The developer owns the honesty call** in Task 1: whether Part 5 may be signed before the
  share-rate re-read exists, or whether the signature waits for it. The developer already faced
  this once and chose "Record now, leave open" at 2026-09-16T16:47Z; that precedent is context, not
  a standing authorization — the call is theirs again here.

The share-rate evidence arrives in the dispatch as a `<counted_evidence>` block and is transcribed
verbatim.
</who_holds_which_tool>

<out_of_scope_recorded_deferral>
`03-REVIEW.md`'s WR-01, WR-02, WR-03 and IN-01 remain deferred to `/gsd-code-review --fix` — see
`03-08-PLAN.md` for the full statement. A signature here does not imply those findings are fixed,
and the close-out should not claim they are.
</out_of_scope_recorded_deferral>
<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide whether Part 5 may be signed before the share-rate re-read exists</name>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2279-2301 (`### Share-rate figure — counted result`) — the first reading (0 share sessions / 19 analysis sessions = 0.0% against a ~2.8% baseline, ~7.5h of exposure inside a 7-day window) and the recorded re-run test dated on/after 2026-09-23T09:15Z.
    - `docs/OPS-01-SHIP-GATE.md` lines 2177-2204 (`### Share-rate figure (D-14)`) — the two HogQL statements to run verbatim: the numerator/denominator/percentage query and the `share_landing` by `properties.ref` breakdown.
    - `docs/OPS-01-SHIP-GATE.md` lines 2400-2429 — the nine-row closing table and the `Sign-off: LEFT OPEN — not signed` close this plan supersedes.
    - The `### Search Console pass — counted result`, `### Developer review backstops — counted result` and `### Item 7 — re-measured counted result` subsections appended by plans 03-08 and 03-09 — the current state of rows 1 and 3 through 9, which is what makes this decision answerable.
    - `.planning/WINDOWS.md` entries #9, #10 and #11 — the three cross-phase follow-ups this closure disposes of.
    - `.planning/phases/03-share-loop/03-CONTEXT.md` D-14 — the share-rate definition and the dual-emit rule that keeps the ~2.8% baseline series intact.
  </read_first>
  <decision>Sign Part 5 now with the share-rate row standing at its first reading and the re-read scheduled, or hold the signature until the D-14 HogQL has been re-run on or after 2026-09-23T09:15Z?</decision>
  <context>
Eight of nine rows now carry a dated outcome. The ninth is the D-14 share rate, and it is the one
row that cannot be honestly improved today: the first reading was 0 share sessions across 19
analysis sessions, taken when the instrumentation had been live for about 7.5 hours inside a 7-day
window. Re-running the same query before 2026-09-23T09:15Z would produce a number that looks like
an answer and is not one — the exposure window would still be mostly pre-deploy.

This is the same honesty question the developer already answered once for the item-7 row, choosing
"Record now, leave open" at 2026-09-16T16:47Z. It is being asked again rather than inherited,
because a signature is a stronger claim than a status note, and because the rows around it have
changed since that choice was made.

What a signature means in this project: `docs/OPS-01-SHIP-GATE.md` Part 5's sign-off is the thing
that marks SHARE-01, SHARE-02, SHARE-03 and the OPS-01 Phase 3 re-run as met. REQUIREMENTS.md
already carries an addendum saying those three checkboxes are ahead of their evidence; a signature
resolves that, and a signature written over an uncounted row would make the ledger less honest
rather than more.

Signing is effectively one-way. An append can correct a wrong number, but a signature that later
proves unearned cannot be un-said — it can only be superseded, which is exactly the pattern
Addenda 1 through 3 exist to perform, and exactly the outcome worth avoiding.
  </context>
  <options>
    <option id="sign-now">
      <name>Sign now, with the share-rate row recorded as a first reading and the re-read scheduled</name>
      <pros>Closes the gate on the eight rows that are genuinely counted; stops SHARE-01/02/03 sitting checked-but-unproven in the ledger; the share-rate re-read stays visible in WINDOWS.md #11 with its exact date and query, so nothing is lost.</pros>
      <cons>The signature covers a row whose figure is statistically meaningless (0/19 over ~7.5h of exposure), which is a weaker claim than every other row in the table and must be labelled as such in the close-out.</cons>
    </option>
    <option id="hold-for-rerun">
      <name>Hold the signature until the re-run on or after 2026-09-23T09:15Z</name>
      <pros>Every row in the signed table rests on a meaningful measurement, including the one that answers the phase's actual question — did the share loop change the share rate.</pros>
      <cons>Phase 3 stays formally open for about a week; Phase 4 planning inherits an unsigned gate; this plan pauses at this checkpoint until the date passes.</cons>
    </option>
    <option id="sign-now-conditional">
      <name>Sign now, and require the re-read to append a dated confirmation or correction to Part 5</name>
      <pros>Closes the gate while binding the project to write the real figure into the same document when it exists; the signature's scope is explicitly limited to what was counted.</pros>
      <cons>Adds an obligation that a future session must honor; if it is skipped, the record ends up signed with a row that was never meaningfully measured — the failure this whole closure exists to correct.</cons>
    </option>
  </options>
  <resume-signal>
State the chosen option id. If `hold-for-rerun`, resume this task only on or after
2026-09-23T09:15Z and supply a `<counted_evidence>` block carrying the re-run: the numerator
(distinct sessions with any `share_action`), the denominator (distinct sessions with
`analysis_complete`), the resulting percentage, the `share_landing` breakdown by
`properties.ref`, whether the captured events carry `consent_gate_path`, and the UTC timestamp the
queries ran at. No share-rate query may be run before that instant. If `sign-now` or
`sign-now-conditional`, state the decision verbatim for the record and supply no query result —
Task 2 records the first reading as standing, not as a re-run.
  </resume-signal>
  <acceptance_criteria>
    - A chosen option id is recorded verbatim, with the UTC timestamp of the decision.
    - Under `hold-for-rerun`, the resume message contains a `<counted_evidence>` block whose recorded query timestamp is on or after 2026-09-23T09:15Z, with a numerator, a denominator, a percentage and a `share_landing` ref breakdown.
    - Under `sign-now` or `sign-now-conditional`, the resume message contains no share-rate query result and no query was run.
    - No credential value appears anywhere in the returned evidence.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the Part 5 close-out and exactly one sign-off heading</name>
  <precondition>Task 1's chosen option id is recorded; under `hold-for-rerun` a `<counted_evidence>` block is present whose recorded query timestamp is on or after 2026-09-23T09:15Z. Halt rather than sign under any other state.</precondition>
  <reversibility rating="one-way">A dated sign-off is a claim the project's own ledger treats as settled; it can only be superseded by a later dated correction, never withdrawn. Governed by Task 1's blocking decision immediately above.</reversibility>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2400-2429 — the nine-row closing table to restate, and the wording of the existing unsigned close.
    - `docs/OPS-01-SHIP-GATE.md` lines 383-397 (`### Sign-off — Phase 1 (2026-09-06)`) and the Phase 2.1 sign-off heading (`### Phase 2.1 Sign-off — SIGNED (2026-09-15, gap-closure 02.1-05)`) — the two precedents for what a signed gate looks like in this document, including the Phase 1 rule that nothing is marked passing without the output that produced it.
    - The three counted-result subsections appended by 03-08 and 03-09 — the evidence each row's pointer must reference.
    - `docs/OPS-01-SHIP-GATE.md` lines 2279-2301 — the share-rate first reading, for the row-2 wording under `sign-now`.
  </read_first>
  <action>
Append to the end of `docs/OPS-01-SHIP-GATE.md` a new subsection headed exactly
`### Part 5 close-out (<YYYY-MM-DD>, gap closure 03-10)` using today's UTC date.

Open it by quoting Task 1's decision verbatim with its UTC timestamp, the same way
`### Item 7 — counted result` quotes the 2026-09-16T16:47Z decision it was governed by.

Then restate the nine-row closing table with the columns `# | Item | Status | Evidence / closing
test`, keyed `| 1 |` through `| 9 |` in the same order as the table at line 2400, so the two tables
can be read side by side:

- Row 1 — item-7 live-traffic thresholds. Status from `### Item 7 — re-measured counted result`.
- Row 2 — share-rate figure (D-14). Under `hold-for-rerun`, the counted re-run: numerator,
  denominator, percentage, the comparison against the ~2.8% baseline, the `share_landing` ref
  breakdown and the query timestamp. Under `sign-now` or `sign-now-conditional`, the literal
  standing state: first reading 0/19 (0.0%) below the ~2.8% baseline, with the scheduled re-read
  dated 2026-09-23T09:15Z and its tracker named as `.planning/WINDOWS.md #11`.
- Row 3 — RESEARCH A3 `consent_gate_path` on `share_action`/`share_landing`. Status from whichever
  subsection captured a real event, or still-open with its closing test.
- Rows 4, 5, 6 — the three Search Console rows, pointing at
  `### Search Console pass — counted result`.
- Rows 7, 8, 9 — D-04 tone, real Discord unfurl, D-13 phone-width, pointing at
  `### Developer review backstops — counted result`.

Every row's Status cell must read a counted PASS, a FAIL with what was counted or seen, or an
explicit `NOT OBTAINED (<YYYY-MM-DD>)` / re-deferral with its closing test named in the Evidence
column. A row with no evidence pointer is not finished.

Then state the disposition of each cross-phase follow-up on its own line, using the literal labels
`WINDOWS.md #9`, `WINDOWS.md #10` and `WINDOWS.md #11` — for each, either closed with a pointer to
the subsection carrying its closing evidence, or open with the dated reason and the test that would
close it. Task 3 mutates the ledger to match these lines; these lines are the record of why.

Finally write exactly one closing heading, and only the one the evidence supports:

- `### Phase 3 Sign-off — SIGNED (<YYYY-MM-DD>, gap closure 03-10)` — permitted only when all nine
  rows are counted PASSes or explicit, accepted, dated FAILs / re-deferrals with their closing
  tests named. Under it, state plainly which rows are passes, which are accepted failures or
  re-deferrals, and that no threshold was lowered or reinterpreted and no count or window was
  borrowed from another deployment or phase. Name what the signature does and does not cover — in
  particular that it does not cover the `03-REVIEW.md` findings, which are deferred to
  `/gsd-code-review --fix`.
- `### Phase 3 Sign-off — LEFT OPEN (<YYYY-MM-DD>, gap closure 03-10)` — when any row still lacks
  both. Name the remaining rows, their exact closing tests, and the next action for each.

Under `sign-now-conditional`, the signed form additionally records the obligation: the re-read on or
after 2026-09-23T09:15Z must append a dated confirmation or correction to this Part, and
`.planning/WINDOWS.md #11` stays open until it does.

Do not edit the existing `### Part 5 status update (2026-09-16, 03-07 final continuation)` section
or its `Sign-off: LEFT OPEN — not signed` line. That line is an accurate record of where 03-07
ended; this close-out supersedes it by standing after it, exactly as Addendum 2 supersedes
Addendum 1 in the requirements ledger.
  </action>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` contains a heading matching `^### Part 5 close-out \(20`.
    - That subsection quotes Task 1's decision verbatim with a UTC timestamp.
    - Its table contains nine rows keyed `| 1 |` through `| 9 |`, each with a status and an evidence pointer or a named closing test.
    - That subsection contains the literals `WINDOWS.md #9`, `WINDOWS.md #10` and `WINDOWS.md #11`, each with a disposition.
    - `docs/OPS-01-SHIP-GATE.md` contains exactly one heading matching `^### Phase 3 Sign-off — (SIGNED|LEFT OPEN) \(`.
    - Under `sign-now` or `sign-now-conditional`, row 2's status names the first reading of 0/19 and the scheduled 2026-09-23T09:15Z re-read; under `hold-for-rerun`, row 2 carries a numerator, a denominator, a percentage and a query timestamp on or after that instant.
    - `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
    - `npm run protected-elements -- --report` exits 0 against production, so the signature is not written over a regressed protected element.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; C=$(sed -n '/^### Part 5 close-out (/,$p' "$F"); [ -n "$C" ] || { echo "GATE-FAIL: no dated Part 5 close-out subsection was appended"; exit 1; }; for K in '| 1 |' '| 2 |' '| 3 |' '| 4 |' '| 5 |' '| 6 |' '| 7 |' '| 8 |' '| 9 |'; do printf '%s\n' "$C" | grep -qF -- "$K" || { echo "GATE-FAIL: the close-out table is missing row $K"; exit 1; }; done; for S in 'WINDOWS.md #9' 'WINDOWS.md #10' 'WINDOWS.md #11'; do printf '%s\n' "$C" | grep -qF -- "$S" || { echo "GATE-FAIL: the close-out states no disposition for $S"; exit 1; }; done; N=$(grep -cE '^### Phase 3 Sign-off — (SIGNED|LEFT OPEN) \(' "$F"); echo "phase3-signoff-headings=$N"; [ "$N" -eq 1 ] || { echo "GATE-FAIL: Part 5 must end in exactly one Phase 3 sign-off heading, signed or left-open"; exit 1; }; echo part5-closeout-ok</automated>
    <fails_when>non-zero exit, or the literal part5-closeout-ok absent from the output; the printed phase3-signoff-headings counter shows whether Part 5 ended in zero or several sign-off headings, and the GATE-FAIL line names the missing table row or ledger disposition</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -E '^-[^-]' | head -20; echo "GATE-FAIL: the close-out must be purely additive"; exit 1; }; echo closeout-additive-ok</automated>
    <fails_when>non-zero exit, or the literal closeout-additive-ok absent from the output; the printed removed-lines counter names how many existing lines were deleted</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && { [ -d node_modules ] || npm ci; } && npm run protected-elements -- --report && echo prod-protected-ok</automated>
    <fails_when>non-zero exit, or the literal prod-protected-ok absent from the output; the report table names which protected element regressed against production</fails_when>
  </verify>
  <done>Part 5 carries a dated close-out restating all nine rows with their counted outcomes and evidence pointers, states the disposition of WINDOWS.md #9, #10 and #11, and ends in exactly one sign-off heading whose form the evidence actually supports.</done>
</task>

<task type="auto">
  <name>Task 3: Carry the outcome into the requirements ledger and the windows ledger</name>
  <precondition>Task 2 has committed the Part 5 close-out and its single sign-off heading; the ledger edits must match what that close-out states, not what this plan hoped for.</precondition>
  <reversibility rating="costly">Appended dated addenda become the ledger's record; a mistake is corrected by a further superseding addendum rather than a deletion, so the wording matters more than usual.</reversibility>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 125-160 (`### OPS-01 Addendum (2026-09-14, Phase 2.1 Plan 04)` and `### OPS-01 Addendum 2 (2026-09-15, Phase 2.1 gap closure 02.1-05)`) — the exact supersession pattern to follow: the new addendum states which one it supersedes and that the superseded text stays in place unedited.
    - `.planning/REQUIREMENTS.md` lines 194-217 (`### OPS-01 Addendum 3 (2026-09-16, Phase 3 gate 03-07)`) — the addendum this one supersedes, including its closing sentence naming exactly what would close it.
    - `.planning/REQUIREMENTS.md` lines 218-250 (`### SHARE-01 / SHARE-02 / SHARE-03 Addendum (2026-09-16, Phase 3 gate 03-07)`) — the capability-proven / backstops-open split this plan updates by appending a superseding addendum.
    - `.planning/REQUIREMENTS.md` line 52 (the OPS-01 checklist line) and line 112 (its traceability row) — the pre-existing text including `Phase 1 gate closed 2026-09-06`, which must remain intact.
    - `.planning/WINDOWS.md` — the table's own column layout and the JSON block beneath it, plus entries #9, #10 and #11.
    - The `### Part 5 close-out` subsection Task 2 wrote — the source of truth for every claim made here.
  </read_first>
  <action>
Append to `.planning/REQUIREMENTS.md`, after the last existing addendum, a new section headed exactly
`### OPS-01 Addendum 4 (<YYYY-MM-DD>, Phase 3 gap closure 03-10)`. Follow Addendum 2's pattern: open
by naming that it supersedes `### OPS-01 Addendum 3 (2026-09-16, Phase 3 gate 03-07)`, which stays
in place unedited, and that it does not touch Addendum 2's Phase 2.1 sign-off.

State in it: which of the nine Part 5 rows are now counted and what they counted; the item-7
per-threshold outcome with its window; the share-rate row's state exactly as the close-out records
it; the Search Console outcome per route including the recrawl disposition; the three developer
backstop verdicts; and then one unambiguous sentence on OPS-01's Phase 3 re-run — met on `<date>`
against deployment `dpl_6Pj5Lz5Q1tSJYSCtUu3YTvtRx3mx` (or the named successor) with
`docs/OPS-01-SHIP-GATE.md` Part 5 signed accordingly, or still not met with the named remaining
test. Do not write "met" if Task 2 wrote the left-open form of the sign-off heading.

Then append a second new section headed
`### SHARE-01 / SHARE-02 / SHARE-03 Addendum 2 (<YYYY-MM-DD>, Phase 3 gap closure 03-10)`, naming
that it supersedes the 2026-09-16 SHARE addendum, which stays in place unedited. Update the
open-backstop statement: name which of the three backstops that addendum listed as open are now
closed and with what evidence (D-04 tone verdict, real Discord unfurl, D-13 phone-width
reachability, and the live `share_action`/`share_landing` with `consent_gate_path`), and which
remain open with their closing tests. If all are closed, say so plainly and state that the three
checkboxes at lines 20-22 now rest on their own evidence rather than on a sibling plan's premature
marking.

Do not edit the checkbox lines, the traceability rows, or any existing addendum. Both additions are
appends. `Phase 1 gate closed 2026-09-06` and every prior addendum heading must survive this edit
unchanged.

Then set the `.planning/WINDOWS.md` dispositions to match the close-out, using the ledger tool
rather than hand-editing the table, so the markdown table, the JSON block and the header counts
stay consistent: run `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed <id>` for each of #9,
#10 and #11 that the close-out records as closed. #9 covers the three developer backstops, #10 the
item-7 re-measure, #11 the share-rate re-run. Close only the ones whose closing evidence was
actually counted — if the share-rate re-run was not performed, #11 stays open, and the close-out
line already says why. Do not waive an entry in place of closing it; a waiver records a decision to
tolerate, which is not what happened here.

Note that this ledger mutation rewrites the status cell in place, so a removed line in
`.planning/WINDOWS.md` is expected and correct — the additive-only invariant applies to the gate
document and the requirements ledger, not to this one.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `### OPS-01 Addendum 4` and still contains `### OPS-01 Addendum 3`, `### OPS-01 Addendum 2` and `Phase 1 gate closed 2026-09-06`.
    - `.planning/REQUIREMENTS.md` contains a heading matching `^### SHARE-01 / SHARE-02 / SHARE-03 Addendum 2 \(20` and still contains the 2026-09-16 SHARE addendum heading.
    - Addendum 4 states OPS-01's Phase 3 re-run as met only if `docs/OPS-01-SHIP-GATE.md` carries the `SIGNED` form of the Phase 3 sign-off heading.
    - `git diff -U0 -- .planning/REQUIREMENTS.md | grep -cE '^-[^-]'` prints 0.
    - For each of `.planning/WINDOWS.md` entries #9, #10 and #11: either the ledger row's status cell no longer reads `open`, or the Part 5 close-out names that entry with a dated disposition.
    - `node .claude/gsd-core/bin/gsd-tools.cjs windows status` parses and still contains entries #9, #10 and #11.
    - `git status --porcelain` shows no change to any application source file.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- .planning/REQUIREMENTS.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { git diff -U0 -- .planning/REQUIREMENTS.md | grep -E '^-[^-]' | head -20; echo "GATE-FAIL: the requirements ledger must be extended, never replaced"; exit 1; }; for T in '### OPS-01 Addendum 4' '### OPS-01 Addendum 3' '### OPS-01 Addendum 2' 'Phase 1 gate closed 2026-09-06' '### SHARE-01 / SHARE-02 / SHARE-03 Addendum (2026-09-16, Phase 3 gate 03-07)'; do grep -qF -- "$T" .planning/REQUIREMENTS.md || { echo "GATE-FAIL: .planning/REQUIREMENTS.md is missing $T"; exit 1; }; done; grep -qE '^### SHARE-01 / SHARE-02 / SHARE-03 Addendum 2 \(20' .planning/REQUIREMENTS.md || { echo "GATE-FAIL: no dated superseding SHARE addendum was appended"; exit 1; }; echo addendum4-ok</automated>
    <fails_when>non-zero exit, or the literal addendum4-ok absent from the output; the printed removed-lines counter names how many existing ledger lines were deleted and the GATE-FAIL line names the missing heading</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && C=$(sed -n '/^### Part 5 close-out (/,$p' docs/OPS-01-SHIP-GATE.md); for ID in 9 10 11; do ROW=$(grep -E "^\| $ID \| 03 \|" .planning/WINDOWS.md | head -1); [ -n "$ROW" ] || { echo "GATE-FAIL: no ledger row for window #$ID"; exit 1; }; ST=$(printf '%s' "$ROW" | awk -F'|' '{gsub(/ /,"",$8); print $8}'); NAMED=$(printf '%s\n' "$C" | grep -cF "WINDOWS.md #$ID"); printf 'window #%s status=%s named-in-closeout=%s\n' "$ID" "$ST" "$NAMED"; [ "$ST" != "open" ] || [ "$NAMED" -ge 1 ] || { echo "GATE-FAIL: window #$ID is still open in the ledger and carries no dated disposition in the Part 5 close-out"; exit 1; }; done; echo windows-disposition-ok</automated>
    <fails_when>non-zero exit, or the literal windows-disposition-ok absent from the output; each printed window line shows that entry's ledger status and whether the close-out named it</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && node .claude/gsd-core/bin/gsd-tools.cjs windows status > /dev/null && { [ -d node_modules ] || npm ci; } && npx tsc --noEmit && npx vitest run && echo final-gate-ok</automated>
    <fails_when>non-zero exit, or the literal final-gate-ok absent from the output — a non-parsing windows ledger, a red typecheck or a failing test all mean something outside this documentation closure was changed</fails_when>
  </verify>
  <done>The requirements ledger carries a dated Addendum 4 and a dated superseding SHARE addendum that say exactly what Part 5's close-out says, every prior addendum is intact, and each of WINDOWS.md #9, #10 and #11 is either closed against counted evidence or open with a written dated reason.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan adds no code symbols. It produces:

| Artifact | Kind | Where |
|---|---|---|
| `### Part 5 close-out (<date>, gap closure 03-10)` | new dated subsection restating all nine rows | `docs/OPS-01-SHIP-GATE.md`, appended at the end of Part 5 |
| `### Phase 3 Sign-off — SIGNED (<date>, gap closure 03-10)` or `### Phase 3 Sign-off — LEFT OPEN (<date>, gap closure 03-10)` | exactly one closing heading | `docs/OPS-01-SHIP-GATE.md`, immediately after the close-out |
| `### OPS-01 Addendum 4 (<date>, Phase 3 gap closure 03-10)` | new dated addendum superseding Addendum 3 | `.planning/REQUIREMENTS.md`, appended after the last addendum |
| `### SHARE-01 / SHARE-02 / SHARE-03 Addendum 2 (<date>, Phase 3 gap closure 03-10)` | new dated addendum superseding the 2026-09-16 one | `.planning/REQUIREMENTS.md`, appended after Addendum 4 |
| `.planning/WINDOWS.md` #9, #10, #11 status | ledger status mutation via `gsd-tools windows fixed <id>` | `.planning/WINDOWS.md` table, JSON block and header counts |

No helper script is created — none is needed, and adding one would be new untested code in a
documentation closure. The API-coverage detector returned `detected: false` for this phase's scope,
so no `COVERAGE.md` is produced.

## Flagged Assumptions

**All three phase requirements came back `unclassified` / `unresolved` from the deterministic edge
probe. This plan is where each is either declared met on its own evidence or recorded still open.**

- **SHARE-01 (unresolved).** Met only if the D-04 tone verdict and the awards-link Discord unfurl
  both came back PASS in 03-08. Otherwise the second SHARE addendum must say which one is open.
- **SHARE-02 (unresolved).** Met only if the player-link Discord unfurl came back PASS.
- **SHARE-03 (unresolved).** Met only if D-13 phone-width reachability came back PASS; the
  protected-elements half is already verified live at 10/10.

**The share-rate row may be signed over honestly, but only labelled as what it is.** A signature
that presents 0/19 across ~7.5h of exposure as a measured share rate would be the same failure mode
this closure exists to correct. Under `sign-now`, row 2's cell must name the exposure window and the
scheduled re-read, not just the percentage.

**WINDOWS.md #11 may legitimately stay open after this plan.** Under `sign-now` it does, by
construction, and that is not a gap — it is the scheduled re-read carrying its own date. The
close-out line naming it is what keeps that honest rather than lost.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this closure to every later phase | a signed Part 5 is the evidence Phases 4 through 7 build their own gates on |
| gate document to the requirements ledger | Addendum 4 restates the gate's outcome; a divergence between them makes the ledger lie |
| cross-phase windows ledger | a closed entry stops being surfaced by `/gsd-ship`, so closing one early hides a real follow-up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-37 | Repudiation | a dated sign-off written over an uncounted row | critical | mitigate | The signature is gated behind Task 1's blocking `checkpoint:decision`, Task 2 carries a precondition and is rated one-way, the action permits a signature only when all nine rows are counted PASSes or accepted dated FAILs / re-deferrals, and the verify command asserts exactly one sign-off heading exists so neither a silent omission nor a double-state can pass |
| T-03-38 | Tampering | existing REQUIREMENTS.md addenda and traceability rows | high | mitigate | Both requirements edits are appends that name what they supersede; the verify command asserts zero removed lines and re-asserts the presence of Addenda 2 and 3, the 2026-09-16 SHARE addendum and the `Phase 1 gate closed 2026-09-06` text |
| T-03-39 | Repudiation | a WINDOWS.md entry closed without its closing evidence | high | mitigate | Closure runs through `gsd-tools windows fixed <id>` only for entries the close-out records as closed; the verify command requires every one of #9, #10, #11 to be either non-open in the ledger or named with a dated disposition in the close-out, so an entry cannot quietly disappear or quietly persist |
| T-03-40 | Information Disclosure | PostHog credentials reached during the share-rate re-run | medium | mitigate | The re-run is performed by the orchestrator and arrives as transcribed counts; the executor reads no credential, and the acceptance criteria require the returned evidence to contain no credential value |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new dependencies (`03-RESEARCH.md` `## Package Legitimacy Audit`: "No external packages are introduced by this phase"); this plan modifies three documentation and planning files, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- Part 5 carries a dated close-out whose table has all nine rows, each with a status and an evidence
  pointer or a named closing test.
- Part 5 contains exactly one `### Phase 3 Sign-off —` heading, in the form the evidence supports.
- The close-out names the disposition of `WINDOWS.md #9`, `#10` and `#11`.
- `.planning/REQUIREMENTS.md` carries Addendum 4 and a dated superseding SHARE addendum, with every
  prior addendum and the Phase 1 closure text intact, and removes zero lines.
- Each of WINDOWS.md #9, #10, #11 is non-open in the ledger or named with a dated disposition.
- `npm run protected-elements -- --report` exits 0 against production; `npx tsc --noEmit` and
  `npx vitest run` are green.
</verification>

<success_criteria>
The OPS-01 Phase 3 gate ends in a state a stranger could audit: every one of the nine rows has a
number or a verdict beside it, the signature (or its absence) matches that evidence exactly, the
requirements ledger says the same thing the gate document says, and any follow-up that genuinely
remains is still visible in the windows ledger with its own date.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-10-SUMMARY.md` when done. Record in it: Task 1's chosen
option id verbatim with its timestamp, the final sign-off form, the per-row outcomes, which
WINDOWS.md entries were closed and which stayed open with why, and the continuing deferral of
`03-REVIEW.md`'s WR-01, WR-02, WR-03 and IN-01 to `/gsd-code-review --fix`.
</output>

