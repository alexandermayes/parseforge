---
phase: 03-share-loop
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/awards-engine.ts
  - lib/awards-engine.test.ts
autonomous: true
requirements: [SHARE-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "AWARD_POOL holds 15 rules with unique ids and a unique, contiguous priority from 1 to 15 (D-02)."
    - "Every rule in AWARD_POOL fires only when its own trigger condition holds against that fight's RaidOverviewResult — no rule fires unconditionally (D-02)."
    - "Every fired award row carries a non-empty stat string and at least one named winner (D-04)."
    - "The card never shows more than MAX_AWARDS_SHOWN rows and never shows a row with zero winners — there are no empty nobody-died rows (D-02)."
    - "Adding the ten expansion rules does not change the relative order of the five tracer rules for a fight where both sets fire."
    - "Every jab-toned award reads as guild banter about a measurable fact, not as a verdict on a person; no rule designates a single worst player (D-01)."
    - statement: "A human reader of the full 15-rule pool confirms every title is fun to be on rather than embarrassing, per the D-01 tone bar."
      verification: backstop
  artifacts:
    - lib/awards-engine.ts
    - lib/awards-engine.test.ts
  key_links:
    - "AWARD_POOL is the only place a rule's trigger, priority, icon, tone and stat formatter are defined — app/og/route.tsx and app/components/RaidOverview.tsx both read the result of computeAwards and neither re-derives a rule."
    - "Every expansion rule reads only RaidOverviewResult fields (players, deathTimeline, healerMetrics) — no new WCL query and no /api/analyze fan-out (D-03)."
  prohibitions:
    - statement: "MUST NOT use insulting, humiliating or personally-derogatory framing about a named real raider, and MUST NOT designate a single worst-player headline — the card names people from a public log and has to be fun to be on."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT display an award whose stat is not derived from that fight's own measured RaidOverviewResult data — no invented, estimated or placeholder figures."
      status: unresolved
      verification: test
---

<objective>
**As a** guild that just wiped, **I want to** see a card full of awards that actually describe
*this* pull **so that** the roast lands and nobody has to argue about whether it is true.

The tracer proved the architecture with five rules. This plan fills the pool out to the 15 rules
D-02 specifies, ranks them, and locks the pool-wide invariants in tests so a future rule cannot
quietly fire on a fight it does not apply to.

Purpose: a conditional slate deep enough that every card looks full, with every row stat-backed.
Output: `AWARD_POOL` at 15 rules, per-rule tests, and pool-wide invariant tests.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share-loop/03-CONTEXT.md
@.planning/phases/03-share-loop/03-RESEARCH.md
@.planning/phases/03-share-loop/03-PATTERNS.md
@.planning/phases/03-share-loop/03-01-SUMMARY.md

Project rules: node tooling needs `export PATH="$HOME/.local/node20/bin:$PATH"`; run
`npx tsc --noEmit` before claiming done; add no new lint findings.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill the award pool to fifteen rules</name>
  <files>lib/awards-engine.ts, lib/awards-engine.test.ts</files>
  <read_first>
    - `lib/awards-engine.ts` — the pool shape, `evaluate` contract, winner sorting and truncation written by 03-01.
    - `lib/awards-engine.test.ts` — the fixture-plus-synthetic idiom the new cases extend.
    - `lib/wcl-types.ts` lines 441-531 — every field an expansion rule may read: `deaths`, `deathDetails`, `avoidableDamage`, `activityPercent`, `consumables`, `missingEnchants`, `avgItemLevel`, `throughput`, `role`, and the `healerMetrics` entries with `hps`, `totalHealing`, `overhealPercent`, `activityPercent`.
    - `app/components/RaidOverview.tsx` lines 39-49 — the existing colour thresholds for deaths and missing enchants, so a rule's threshold does not contradict what the raid table already calls bad.
    - `lib/raid-overview-engine.ts` around line 309-340 — how `activityPercent` is derived for healers versus damage dealers, so the GCD Tourist rule does not judge a healer on a damage-shaped number.
  </read_first>
  <behavior>
    - `graveyard-shift` fires only when some player has 2 or more deaths, and names every player tied at that maximum.
    - `gcd-tourist` considers only players whose `role` is not `Healer` and whose `throughput` is above 0, fires only when the minimum `activityPercent` among them is below 80, and never names a healer.
    - `fire-dancer` fires only when the maximum `avoidableDamage` is above 0 and is at least 1.5 times the median across all players.
    - `naked-slots` fires only when some player has 3 or more `missingEnchants`, and ranks winners by `missingEnchants` descending.
    - `skipped-breakfast` fires only when some player has `consumables.food` false.
    - `dull-blade` considers only players whose `role` is `Physical` or `Tank`, and fires only when one of them has `consumables.weaponEnhancement` false — a caster with no weapon enhancement never wins it.
    - `iron-man` fires only when the raid had at least one death AND some player has zero deaths; on a wipe where everyone died it does not fire.
    - `watering-the-garden` fires only for a healer with `totalHealing` above 0 and `overhealPercent` at or above 50.
    - `kept-them-breathing` fires only for a healer with `totalHealing` above 0 and `activityPercent` at or above 85.
    - `punching-up` fires only when at least two non-healers have an `avgItemLevel` above 0, the spread between the highest and lowest is at least 10, and the lowest-ilvl player's `throughput` is at or above the non-healer median.
    - On a fight where all fifteen rules could fire, `computeAwards` returns exactly `MAX_AWARDS_SHOWN` rows and they are the six lowest priorities.
  </behavior>
  <action>
Append ten rules to `AWARD_POOL` in `lib/awards-engine.ts` at priorities 6 through 15, leaving the
five tracer rules and their priorities 1 through 5 untouched. Each new rule reads only
`RaidOverviewResult` fields — no new fetch, no `/api/analyze` fan-out, no cast-timeline data (D-03).
Each returns `null` when its condition does not hold.

| priority | id | title | icon | tone | fires when | winners | stat |
|---|---|---|---|---|---|---|---|
| 6 | graveyard-shift | Graveyard Shift | coffin emoji | jab | some player with `deaths` of 2 or more | every player tied at the max `deaths` | the death count followed by the word deaths |
| 7 | gcd-tourist | GCD Tourist | mantelpiece-clock emoji | jab | among players whose `role` is not Healer and whose `throughput` is above 0, the minimum `activityPercent` is below 80 | that player | the `activityPercent` to one decimal followed by the word active |
| 8 | fire-dancer | Standing in the Fire | fire emoji | jab | the max `avoidableDamage` is above 0 and at least 1.5 times the median across all players | that player | the K/M-formatted `avoidableDamage` followed by the word taken |
| 9 | naked-slots | Enchants? Never Heard of Her | wrench emoji | jab | some player with `missingEnchants` of 3 or more | every such player, ranked by `missingEnchants` descending | the count followed by the words missing enchants |
| 10 | skipped-breakfast | Skipped Breakfast | meat-on-bone emoji | jab | some player whose `consumables.food` is false | every such player | the literal `no food buff` |
| 11 | dull-blade | Dull Blade | dagger emoji | jab | some player whose `role` is Physical or Tank and whose `consumables.weaponEnhancement` is false | every such player | the literal `no weapon enhancement` |
| 12 | iron-man | Iron Man | rock emoji | praise | `deathTimeline` is non-empty AND some player has `deaths` equal to 0 | among the zero-death players, the one with the highest `avoidableDamage` | the literal `0 deaths` then a middot then the K/M-formatted damage taken |
| 13 | watering-the-garden | Watering the Garden | droplet emoji | jab | some `healerMetrics` entry with `totalHealing` above 0 and `overhealPercent` at or above 50 | the max-`overhealPercent` healer | the rounded percent followed by the word overheal |
| 14 | kept-them-breathing | Kept Them Breathing | green-heart emoji | praise | some `healerMetrics` entry with `totalHealing` above 0 and `activityPercent` at or above 85 | the max-`activityPercent` healer | the rounded percent followed by the words healing uptime |
| 15 | punching-up | Punching Up | direct-hit emoji | praise | at least two non-healers have `avgItemLevel` above 0, the spread between the highest and lowest is at least 10, and the lowest-ilvl player's `throughput` is at or above the non-healer median | that player | the word ilvl then the value then a middot then the K/M-formatted throughput and the unit dps |

Tone bar for every jab title (D-01): the joke is about a measurable fact the row already proves,
never about the person. No rule may name a single worst player, and no rule may produce a headline
that ranks someone last overall. `dull-blade` is role-gated for exactly this reason — a caster has
no weapon enhancement to miss, and naming them would be a wrong recommendation, which this project
treats as worse than none.

Extend `lib/awards-engine.test.ts` with one describe block per new rule covering the fires and
does-not-fire halves of its `<behavior>` bullet, using small synthetic `RaidOverviewResult` object
literals in the same style the existing death-dependent cases already use, plus one case that
builds a fight where all fifteen rules fire and asserts the returned rows are exactly the six
lowest priorities in ascending order.
  </action>
  <acceptance_criteria>
    - `AWARD_POOL` has exactly 15 entries.
    - The 15 ids are unique and the 15 priorities are exactly the integers 1 through 15 with no gaps or repeats.
    - The five ids from 03-01 still carry priorities 1 through 5.
    - `lib/awards-engine.ts` still contains no call to `fetch(` and no reference to `posthog`.
    - `npx vitest run lib/awards-engine.test.ts` passes with at least 28 tests.
    - Every expansion rule has both a fires case and a does-not-fire case asserted.
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx vitest run lib/awards-engine.test.ts</automated>
    <fails_when>non-zero exit, or a "Tests" summary line reporting fewer than 28 passed</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npx eslint lib/awards-engine.ts lib/awards-engine.test.ts && echo gates-ok</automated>
    <fails_when>non-zero exit, or the literal gates-ok is absent from the output</fails_when>
  </verify>
  <done>The pool holds fifteen stat-backed, conditionally-triggered awards ranked by a fixed priority order, each covered by a fires and a does-not-fire test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the pool-wide invariants and clear the tone review</name>
  <files>lib/awards-engine.test.ts</files>
  <read_first>
    - `lib/awards-engine.ts` — the completed 15-rule pool, its constants and its winner-truncation code.
    - `lib/awards-engine.test.ts` — the per-rule blocks task 1 wrote.
    - `.planning/phases/03-share-loop/03-CONTEXT.md` `<decisions>` D-01, D-02 and D-04 — the tone bar and the pool contract these invariants encode.
    - `lib/healer-metrics.test.ts` — the cross-surface parity test style from Phase 2.
  </read_first>
  <behavior>
    - Iterating `AWARD_POOL` asserts: ids are unique, priorities are the contiguous range 1 to 15, every `title` is non-empty, every `icon` is non-empty, and every `tone` is one of the two allowed values.
    - Running every rule's `evaluate` against a minimal all-zero `RaidOverviewResult` (one player, no deaths, all consumables true, no missing enchants, zero healing) returns `null` for every rule except the ones whose trigger genuinely holds on that input — no rule fires unconditionally.
    - For every fired row from every synthetic fixture in the suite, `stat` is a non-empty string and `winners.length` is at least 1 and at most `MAX_WINNER_NAMES`.
    - `computeAwards` never returns more than `MAX_AWARDS_SHOWN` rows for any input in the suite.
    - Sorting is stable: computing awards twice on the same input returns identical id order.
    - `computeAwards` does not mutate the `RaidOverviewResult` it is given — a deep-equality check against a snapshot taken before the call still holds after it.
  </behavior>
  <action>
Add a `describe` block named for the pool-wide invariants to `lib/awards-engine.test.ts`, covering
every bullet in `<behavior>`. These are the guardrails that make a future sixteenth rule safe to
add: they assert the contract of the pool rather than the behaviour of any one rule.

Write the no-unconditional-firing test as a loop over `AWARD_POOL` against a single minimal
fixture, listing the ids that are permitted to fire on it as an explicit allowlist inside the test,
so adding a rule that fires on an empty fight fails the suite rather than silently shipping an
award nobody earned.

Write the non-mutation test by structured-cloning the input before the call and deep-comparing
after it.

Then perform the D-04 tone review: read every title, icon, tone and stat formatter in
`AWARD_POOL` end to end as a single list and confirm against the D-01 bar — praise sits alongside
light jabs, every jab is about a measurable fact the row proves, nothing is insulting, and no rule
designates a worst player. Record the full fifteen-row list verbatim in the plan summary so the
developer reviews the exact shipped pool at the 03-06 preview gate before anything reaches
production.
  </action>
  <acceptance_criteria>
    - `lib/awards-engine.test.ts` contains a pool-invariants describe block asserting id uniqueness, contiguous priorities, non-empty titles and icons, and a valid tone on every entry.
    - The no-unconditional-firing test loops over `AWARD_POOL` rather than naming rules one by one.
    - A non-mutation assertion exists for `computeAwards`.
    - `npx vitest run lib/awards-engine.test.ts` passes with at least 34 tests.
    - `npx vitest run` (the full suite) is green — no existing engine test regressed.
    - The plan summary lists all 15 rules with id, title, icon, tone, trigger and stat, ready for the developer's D-04 review at 03-06.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx vitest run lib/awards-engine.test.ts</automated>
    <fails_when>non-zero exit, or a "Tests" summary line reporting fewer than 34 passed</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx vitest run</automated>
    <fails_when>non-zero exit, or a "Test Files" summary line reporting any failed file</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && P=$(grep -c "priority:" lib/awards-engine.ts); echo "pool-priorities=$P"; [ "$P" -eq 15 ] || { echo "GATE-FAIL: expected 15 priority entries in the pool, found $P"; exit 1; }; echo pool-size-ok</automated>
    <fails_when>non-zero exit, or the literal pool-size-ok is absent; the printed pool-priorities counter names the actual pool size found</fails_when>
    <human-check>Read the full fifteen-row award pool recorded in the plan summary. Confirm against D-01: praise sits alongside light jabs, every jab is about a measurable fact the row proves, nothing is insulting about a named real raider, and no rule designates a single worst player. This is the D-04 review; it is resolved at the 03-06 preview gate before any production deploy.</human-check>
  </verify>
  <done>The pool's contract is enforced by tests rather than by convention, and the exact fifteen rules that will ship are written down for the developer's tone review.</done>
</task>

</tasks>

## Artifacts this phase produces

See `03-01-PLAN.md` → "Artifacts this phase produces" for the full phase list. This plan adds no
new files and no new exported symbols — it extends the existing `AWARD_POOL` array in
`lib/awards-engine.ts` with ten entries whose ids are `graveyard-shift`, `gcd-tourist`,
`fire-dancer`, `naked-slots`, `skipped-breakfast`, `dull-blade`, `iron-man`,
`watering-the-garden`, `kept-them-breathing` and `punching-up`.

## Flagged Assumptions

**SHARE-01 — unresolved (`unclassified`).** The deterministic edge probe produced no edge set for
this requirement. Assumed edges covered by this plan's tests: a fight where no rule fires, a fight
where all fifteen fire, a wipe where every raider died (so `iron-man` must not fire), a healer
metric evaluated against a damage-shaped threshold (excluded by role gating), and a tie needing
the `+N` truncation. Surfaced here rather than dropped.

**A2 (carried from RESEARCH).** Long award titles and multi-name winner lists are capped in the
engine, but Satori's ellipsis behaviour for the resulting spans is confirmed only by eye at the
03-06 preview gate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WCL report data to the award pool | third-party, user-supplied player names and metrics reach a rule's winner list and stat string |
| award content to a named real person | a public log names real raiders; a badly-framed award is a product-trust failure, not a technical one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | a rule's threshold arithmetic on degenerate metrics (zero duration, negative values, NaN) | low | mitigate | `computeAwards` guards a non-positive `fightDuration` and an empty roster before any rule runs; each rule's trigger is a comparison that yields false on NaN, so a degenerate fight fires nothing rather than an incorrect award |
| T-03-08 | Repudiation | an award naming a real raider with no verifiable basis | medium | mitigate | Every row carries the stat that earned it, computed from that fight's own data (D-04); the no-unconditional-firing loop test prevents a rule from producing a winner on an empty fight |
| T-03-09 | Information Disclosure | player names in an award row | low | accept | Only publicly-viewable WCL reports resolve; the same names already appear in the raid table on the same page. No new disclosure |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are added by this phase (RESEARCH `## Package Legitimacy Audit`: zero new dependencies). Any install would require a package-legitimacy gate plus a blocking human checkpoint first |
</threat_model>

<verification>
- `npx vitest run` is green for the whole suite.
- `npx tsc --noEmit` is clean; `npm run lint` shows no new findings.
- `AWARD_POOL` holds 15 rules with contiguous priorities.
- The full pool list is recorded in the summary for the D-04 tone review at 03-06.
</verification>

<success_criteria>
Every fight produces a full card of conditionally-triggered, stat-backed awards ranked by one
fixed priority order, and the pool's contract is defended by tests rather than by hope.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-02-SUMMARY.md` when done.
</output>