---
phase: 03-share-loop
plan: 09
type: execute
wave: 7
depends_on: ["03-08"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
autonomous: false
gap_closure: true
requirements: [SHARE-01, SHARE-02, SHARE-03, OPS-01]

user_setup:
  - service: vercel-web-analytics
    why: "Item 7 threshold 3 needs a window-granularity page-view figure for the same 60 minutes. Part 5 recorded it NOT EVALUABLE because the personal Vercel CLI token exposes no Web Analytics endpoint; mcp__vercel__get_web_analytics may now make it readable, and if it does not, only a dashboard read by the developer can."
    dashboard_config:
      - task: "If the MCP read returns no window-granularity figure, read the production page-view count for the exact 60-minute window off the dashboard"
        location: "Vercel dashboard -> team loot-list-plus -> project parseforge -> Analytics (Production filter, window-scoped range)"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/OPS-01-SHIP-GATE.md Part 5 carries a dated `### Item 7 — re-measured counted result` subsection recording a fully elapsed 60-minute UTC window, the UTC timestamp the queries were run at, and a counted per-threshold result for all three of item 7's own thresholds, unmodified."
    - "That subsection records the threshold 3 outcome either way: a counted Vercel Web Analytics figure with the ratio scored against it, or NOT EVALUABLE with the specific reason the figure could not be read and the named route that would produce it."
    - "That subsection records the RESEARCH A3 disposition — whether any captured `share_action` or `share_landing` event in the window carries a `consent_gate_path` value (D-14 makes `consent_gate_path` a standing super property on both events), or that zero of either event exist in the window to inspect."
    - "The recorded window is 60 minutes long and its end instant is earlier than the recorded query timestamp; a partial window is never scored."
    - "Every edit to docs/OPS-01-SHIP-GATE.md is additive: `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0."
    - statement: "PostHog project 337485 returns a counted result for a fully elapsed 60-minute production window on a busier UTC hour, run against deployment dpl_6Pj5Lz5Q1tSJYSCtUu3YTvtRx3mx or a successor carrying the same code."
      verification: backstop
    - statement: "A window-granularity Vercel Web Analytics page-view figure for the same 60 minutes is either read and recorded, or recorded as unreadable with the specific reason."
      verification: backstop
  artifacts:
    - docs/OPS-01-SHIP-GATE.md
  key_links:
    - "docs/OPS-01-SHIP-GATE.md Part 5 closing-table row 1 (item-7 thresholds) and row 3 (RESEARCH A3 consent_gate_path on the new share events) are the rows this plan drives; row 3 closes automatically only if this window captures a real share_action or share_landing event."
  prohibitions:
    - statement: "MUST NOT score a partial window, and MUST NOT run the queries before the window's end instant has passed."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT borrow, lower, reinterpret or re-date a count, window or threshold from another deployment, window, session or phase; item 7's three thresholds are applied exactly as Part 1 writes them."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT record a zero-event result as no-data or pending; item 7's own rule is that zero events is a FAIL."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT print, copy, move or paste the value of any credential — Vercel tokens, PostHog keys, .env* contents. Name a credential path when a task needs one; never read it out."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT change any application code, and MUST NOT run a production deploy; a belief that a successor deployment is needed is a blocking checkpoint:decision, never a self-authorized command."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT fold the 03-REVIEW.md findings WR-01, WR-02, WR-03 or IN-01 into this closure — they belong to /gsd-code-review --fix."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT remove or rewrite an existing line of docs/OPS-01-SHIP-GATE.md; a superseding status is recorded as a new dated subsection appended at the end of Part 5."
      status: unresolved
      verification: judgment
---

<objective>
**As** the owner of a live site, **I want to** re-measure the OPS-01 item-7 live-traffic check on a
UTC hour that actually has traffic, **so that** the Phase 3 gate's one FAILED threshold is either a
real pass on counted data or a real failure of the code rather than an artifact of a quiet hour.

Purpose: Part 5 closing-table row 1 scored threshold 1 FAIL (7 pageviews against a bar of 20),
threshold 2 PASS (2 countries) and threshold 3 NOT EVALUABLE on the deployment's own
`09:14:57Z`-`10:14:57Z` window. The gate document itself records the closing test: a full 60-minute
window on a busier UTC hour plus a window-granularity Vercel Web Analytics read. Row 3 (RESEARCH A3)
closes automatically if this window captures a real `share_action` or `share_landing` event carrying
`consent_gate_path`.

Output: one new dated subsection appended at the end of Part 5, purely additive.

**No application code changes in this plan.** No deploy. No package installs.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share-loop/03-CONTEXT.md
@.planning/phases/03-share-loop/03-VERIFICATION.md
@.planning/phases/03-share-loop/03-08-SUMMARY.md

Project rules that bind this plan:
- `node`/`npm`/`npx` are not on the default PATH. Prepend `$HOME/.local/node20/bin` before any of
  them.
- Vercel commands need `--global-config ~/.vercel-personal`; without it the CLI uses the work
  account and cannot see the `loot-list-plus` team. This plan runs no deploy.
- Never print or commit an environment variable or credential value.
- Edits to `docs/OPS-01-SHIP-GATE.md` are additive appends (`03-07-SUMMARY.md`, Deviations #2).
</context>

<who_holds_which_tool>
- **The gsd-executor running this plan has** `Read, Write, Edit, Bash, Grep, Glob, Skill,
  mcp__context7__*`. It has no PostHog tool and no `POSTHOG_PERSONAL_API_KEY` — 03-07 confirmed
  this with `env | grep -ci posthog` returning 0. It cannot run HogQL and cannot read Vercel Web
  Analytics.
- **The execute-phase orchestrator session has** `mcp__posthog__exec` (HogQL against project
  `337485` — the same project 03-07's counted figures came from) and
  `mcp__vercel__get_web_analytics` / `get_deployment` / `list_deployments` (team `loot-list-plus`,
  project `parseforge`).
- **The developer holds** the Vercel dashboard, which is the fallback for threshold 3 if the MCP
  read returns nothing window-granular.

Evidence arrives in the dispatch as a `<counted_evidence>` block and is transcribed verbatim. This
is exactly what 03-07 did: "the counted item-7 and share-rate figures were supplied by the
orchestrator … Recorded verbatim, not re-derived or estimated."
</who_holds_which_tool>

<out_of_scope_recorded_deferral>
`03-REVIEW.md`'s WR-01, WR-02, WR-03 and IN-01 remain deferred to `/gsd-code-review --fix` — see
`03-08-PLAN.md` for the full statement of that deferral. Do not fix them here and do not let a
review finding become the reason a threshold is left open.
</out_of_scope_recorded_deferral>
<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Acquire the counted item-7 figures for a fully elapsed 60-minute window</name>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2118-2176 (`### Item 7 — post-deploy live-traffic check`) — the three HogQL statements to run verbatim, the note that the connector's default project is `LootList+ App` so `switch-project 337485` comes first, and the three thresholds exactly as Part 1 writes them.
    - `docs/OPS-01-SHIP-GATE.md` lines 2205-2278 (`### Item 7 — counted result`) — the first measurement, its per-threshold table, the recorded closing test, and the hourly `$pageview` table that makes "a busier hour" concrete (09:00 to 14:00Z on 2026-09-16; the previous build showed 24 pageviews at 22:00Z on 2026-09-14).
    - `docs/OPS-01-SHIP-GATE.md` lines 2400-2429 — the nine-row closing table; this plan closes rows 1 and 3.
    - `.planning/WINDOWS.md` entry #10 — the durable record of this exact re-measure, including the threshold-3 limitation.
    - `.planning/phases/03-share-loop/03-CONTEXT.md` D-14 — the `share_action` shape and the standing `consent_gate_path` super property whose presence is RESEARCH A3.
  </read_first>
  <action>
This executor cannot run HogQL and cannot read Vercel Web Analytics. Stop here and hand the
orchestrator the measurement below. Do not approximate a count, do not reuse the
`09:14:57Z`-`10:14:57Z` figures, and do not record the row as unavailable-this-session.
  </action>
  <instructions>
**Already done by the executor:** nothing automatable remains — no PostHog credential or MCP tool
exists in the executor's environment.

**Choose the window and state it as an input, not a guess.** The window must be exactly 60 minutes,
in UTC, and **fully elapsed before the queries run**. The recorded candidate is the ~22:00Z hour
(24 pageviews on 2026-09-14 on the previous build); the earliest complete instance of it is
`2026-09-16T22:00:00Z`-`2026-09-16T23:00:00Z`, queryable from `23:00Z` onward. If execution happens
later, pick a busier hour off the hourly table at gate doc lines 2262-2277 or a fresh hourly read,
and state which hour was chosen and why. The window must sit against deployment
`dpl_6Pj5Lz5Q1tSJYSCtUu3YTvtRx3mx` or a successor deployment carrying the same code — if the
production deployment has changed, say which one the window belongs to.

**Run the three HogQL statements verbatim** from gate doc lines 2126-2160 against PostHog project
`337485`, substituting only the window's start and end timestamps: query 1 is `$pageview` grouped by
`properties.$geoip_country_code`; query 2 is all events grouped by `properties.consent_gate_path`;
query 3 is the share-event slice restricted to `share_action`, `share_landing`, `share_link_copied`
and `discord_copied`, grouped by event and `consent_gate_path`. Bring back each result table as
rows, not as a summary.

**Attempt threshold 3.** Try `mcp__vercel__get_web_analytics` for team `loot-list-plus`, project
`parseforge`, production, scoped to the same 60 minutes. Part 5 recorded this as NOT EVALUABLE
because the personal CLI token exposes no Web Analytics endpoint — the MCP route may now make it
readable, so try it and record the outcome either way. If it returns only an hour-rounded aggregate,
record the figure and say it is an upper bound, exactly as Part 4 did on 2026-09-15. If nothing
window-granular is available, either the developer reads the dashboard figure or the row is recorded
NOT EVALUABLE with the specific reason — not folded into a pass and not silently dropped.

**Do not score the thresholds in this checkpoint.** Bring back the counts; Task 2 scores them
against the thresholds as written and records the result.

Also state explicitly, from query 3, whether any `share_action` or `share_landing` row exists and
whether it carries a `consent_gate_path` value. Zero rows is a real answer for RESEARCH A3 and must
be reported as zero rows, not as absence of information.
  </instructions>
  <verification>
After resume, the executor checks that the `<counted_evidence>` block names a 60-minute UTC window,
a query timestamp later than that window's end, the deployment the window belongs to, three result
tables, and a threshold-3 figure or an explicit reason it is unreadable. A block whose query
timestamp precedes the window end is an unmet precondition for Task 2.
  </verification>
  <acceptance_criteria>
    - The resume message contains a `<counted_evidence>` block naming `window_start`, `window_end` and the query-run timestamp, all in UTC, with `window_end` earlier than the query-run timestamp and exactly 60 minutes after `window_start`.
    - It names the deployment id the window belongs to.
    - It contains the three query result tables as rows, including a total pageview count and a distinct-country count.
    - It contains either a Vercel Web Analytics figure for the same window (flagged as exact or upper bound) or an explicit reason it could not be read.
    - It states whether any `share_action` or `share_landing` row exists in the window and whether `consent_gate_path` is present on it.
    - No credential value appears anywhere in the returned evidence.
  </acceptance_criteria>
  <resume-signal>Reply with a `<counted_evidence>` block carrying the window, the query timestamp, the three result tables and the threshold-3 outcome. Task 2 transcribes and scores it; it runs no query of its own.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the re-measured item-7 result and the RESEARCH A3 disposition</name>
  <precondition>Task 1's `<counted_evidence>` block is present in this task's dispatch, its window is exactly 60 minutes, and its query-run timestamp is later than its `window_end`; halt rather than score a partial window.</precondition>
  <reversibility rating="reversible">An additive dated subsection; a later dated append can supersede it without rewriting it.</reversibility>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2205-2278 — the exact shape of a counted item-7 record: a Source paragraph, a Window line, the three query tables, then a four-column threshold table with Rule / Counted / Result.
    - `docs/OPS-01-SHIP-GATE.md` lines 2160-2176 — the three thresholds in their own words, including "exactly 20 passes", "exactly 2 passes", "exactly 50% passes" and "Zero events is a FAIL, never no-data".
    - `docs/OPS-01-SHIP-GATE.md` — the end of the file, so the append lands after the last existing line inside Part 5.
    - `.planning/phases/03-share-loop/03-08-SUMMARY.md` — what plan 03-08 closed, so the roll-up line in this subsection states the correct remaining rows.
  </read_first>
  <action>
Append to the end of `docs/OPS-01-SHIP-GATE.md` a new subsection headed exactly
`### Item 7 — re-measured counted result (<YYYY-MM-DD>, gap closure 03-09)` using today's UTC date.
Mirror the structure of `### Item 7 — counted result` at lines 2205-2278 so the two records read as
the same kind of evidence.

Write, in this order, using these literal labels so the record is greppable:

- A Source paragraph naming PostHog project `337485`, who ran the queries, and the statement that
  every figure is transcribed verbatim from the supplied evidence — not rounded, not extrapolated,
  and not combined with any other deployment's numbers.
- `**Window (UTC):**` the start and end instants, and why this hour was chosen over the original
  `09:14:57Z`-`10:14:57Z` window.
- `**Query run (UTC):**` the timestamp the queries actually ran at, which must be later than the
  window end — this is what proves a full window was scored rather than a partial one.
- `**Deployment:**` the deployment id the window belongs to.
- The three query result tables, as rows.
- A threshold table with the columns Threshold / Rule / Counted / Result and exactly three rows
  keyed `| 1 |`, `| 2 |`, `| 3 |`. Score each against the rule as written: threshold 1 needs at
  least 20 `$pageview` events, threshold 2 needs at least 2 distinct non-consent-region
  `$geoip_country_code` values, threshold 3 needs the PostHog `$pageview` count to be at least 50%
  of the Vercel Web Analytics figure for the same window. Exactly at the bar passes. A zero-event
  count is FAIL, never no-data. A threshold whose input could not be read is
  `NOT EVALUABLE — <specific reason>`, never a pass.
- `**Row 1 verdict:**` one line: PASS only if all three thresholds pass; otherwise FAIL or
  PARTIAL naming which thresholds passed and, for any that did not, the exact test that would close
  it (another busier window, or the named route to the Web Analytics figure).
- `**Row 3 verdict (RESEARCH A3):**` whether a real `share_action` or `share_landing` event was
  captured in this window with a `consent_gate_path` value — quoting the value — or that zero of
  either event exist in the window, in which case row 3 stays open and its closing test is the
  share-rate re-run in plan 03-10.
- A closing roll-up line naming which of the nine closing-table rows are now counted and which
  remain, so plan 03-10 can read the state off one line.

If threshold 1 fails again on a genuinely busier hour, that is a real and useful finding: record it
as a FAIL with the counts, and name the next window to try. Do not lower the bar, do not average
two windows together, and do not reinterpret "non-consent-region country" to reach threshold 2.

Do not modify `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md` in this plan — plan 03-10 owns
the ledger propagation once every row is counted.
  </action>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` contains a heading matching `^### Item 7 — re-measured counted result \(20`.
    - That subsection contains the literals `**Window (UTC):**`, `**Query run (UTC):**`, `**Deployment:**`, `**Row 1 verdict:**` and `**Row 3 verdict (RESEARCH A3):**`.
    - That subsection names PostHog project `337485`.
    - Its threshold table contains three rows keyed `| 1 |`, `| 2 |` and `| 3 |`, each with a counted value and a result of PASS, FAIL or `NOT EVALUABLE`.
    - The recorded query-run timestamp is later than the recorded window end.
    - `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
    - `git status --porcelain` shows no change to any file outside `docs/OPS-01-SHIP-GATE.md` and this phase's planning artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; S=$(sed -n '/^### Item 7 — re-measured counted result (/,$p' "$F"); [ -n "$S" ] || { echo "GATE-FAIL: no dated item-7 re-measured subsection was appended"; exit 1; }; for T in '337485' '**Window (UTC):**' '**Query run (UTC):**' '**Deployment:**' '**Row 1 verdict:**' '**Row 3 verdict (RESEARCH A3):**'; do printf '%s\n' "$S" | grep -qF -- "$T" || { echo "GATE-FAIL: the re-measured subsection is missing the field $T"; exit 1; }; done; for K in '| 1 |' '| 2 |' '| 3 |'; do printf '%s\n' "$S" | grep -qF -- "$K" || { echo "GATE-FAIL: the threshold table is missing row $K"; exit 1; }; done; echo item7-remeasure-ok</automated>
    <fails_when>non-zero exit, or the literal item7-remeasure-ok absent from the output; the printed GATE-FAIL line names either the absent subsection, the missing field label, or the missing threshold row</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -E '^-[^-]' | head -20; echo "GATE-FAIL: the item-7 re-measure record must be purely additive"; exit 1; }; echo remeasure-additive-ok</automated>
    <fails_when>non-zero exit, or the literal remeasure-additive-ok absent from the output; the printed removed-lines counter names how many existing lines were deleted</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && { [ -d node_modules ] || npm ci; } && npx tsc --noEmit && npx vitest run && echo local-gate-ok</automated>
    <fails_when>non-zero exit, or the literal local-gate-ok absent from the output — this plan changes no code, so a red typecheck or a failing test means something outside its scope was touched</fails_when>
  </verify>
  <done>Part 5 carries a dated item-7 re-measurement against a fully elapsed 60-minute window on a chosen busier hour, with all three thresholds scored on counted data (or explicitly NOT EVALUABLE with the reason), and the RESEARCH A3 consent-path question answered from real query rows.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan adds no code symbols. It appends one subsection to `docs/OPS-01-SHIP-GATE.md` Part 5:

| Artifact | Kind | Where |
|---|---|---|
| `### Item 7 — re-measured counted result (<date>, gap closure 03-09)` | new dated subsection (closing-table rows 1 and 3) | `docs/OPS-01-SHIP-GATE.md`, appended at the end of Part 5 |

Owned by other plans: the two Search Console / developer-backstop subsections (03-08),
`### Part 5 close-out` plus the sign-off heading (03-10), `### OPS-01 Addendum 4` in
`.planning/REQUIREMENTS.md` and the `.planning/WINDOWS.md` status changes (03-10). No helper script
is created. The API-coverage detector returned `detected: false` for this phase's scope, so no
`COVERAGE.md` is produced.

## Flagged Assumptions

**All three phase requirements came back `unclassified` / `unresolved` from the deterministic edge
probe, and none is auto-resolved here.**

- **SHARE-01, SHARE-02, SHARE-03 (all unresolved).** Their shared live-traffic backstop — at least
  one real `share_action` and one real `share_landing` carrying `consent_gate_path` — is row 3 of
  this plan. If this window captures zero of either event, all three requirements' backstop stays
  open, and the only remaining route to it is plan 03-10's share-rate re-run. Do not let a
  zero-event window be read as a mechanism failure either: 03-07 already recorded that the legacy
  events also read zero in the post-deploy hour, which is a small-exposure reading, not a
  regression signal.

**A busier hour may still fail threshold 1.** Phase 2.1 and Phase 3 both hit this. The recorded
counts, not just the verdict, are what let a later reader tell a quiet-hour failure from a code
failure — which is why the threshold table records Counted alongside Result.

**Threshold 3 may still be unreadable.** The personal Vercel CLI token exposed no Web Analytics
endpoint in two prior sessions. `mcp__vercel__get_web_analytics` is a new route worth trying, and
an hour-rounded aggregate is an upper bound, not a window figure — Part 4's 2026-09-15 precedent
shows how to record that honestly, including the case where the PASS is invariant to the
imprecision.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostHog and Vercel readings to the gate record | externally-sourced counts are transcribed into a document that gates a milestone |
| gate document to future phases | a threshold recorded as passed becomes the evidence Phases 4 through 7 build on |
| production traffic to the measurement | the window is real user traffic; its size is not under the project's control, which is what makes threshold-lowering tempting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-33 | Repudiation | a threshold scored on a partial or borrowed window | high | mitigate | Task 2 carries a precondition requiring a 60-minute window whose end precedes the query timestamp, both are recorded as named fields, and the acceptance criteria assert the ordering; the `must_haves` prohibitions forbid borrowing or averaging windows |
| T-03-34 | Tampering | item 7's three thresholds | high | mitigate | The thresholds are quoted from Part 1 into the task's `read_first` and re-stated in the action as written; the threshold table must record Counted beside Result so a lowered bar is visible in the diff |
| T-03-35 | Tampering | the committed gate document's existing evidence | high | mitigate | Append-only; the proven additive-only check (`git diff -U0 \| grep -cE '^-[^-]'` equals 0) runs in the task's verify block |
| T-03-36 | Information Disclosure | PostHog and Vercel credentials | high | mitigate | No task reads a credential; the PostHog project id and the Vercel team/project names are the only identifiers recorded, and the acceptance criteria require the returned evidence to contain no credential value |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new dependencies (`03-RESEARCH.md` `## Package Legitimacy Audit`: "No external packages are introduced by this phase"); this plan modifies one documentation file, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- Part 5 carries a dated `### Item 7 — re-measured counted result` subsection with a named window,
  a named query timestamp later than the window end, a named deployment, three query tables and a
  three-row threshold table.
- Each threshold row reads PASS, FAIL or NOT EVALUABLE with its counted input beside it.
- The RESEARCH A3 row states either a captured event with its `consent_gate_path` value, or zero
  rows.
- `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
- `npx tsc --noEmit` and `npx vitest run` are green — this plan changed no code.
</verification>

<success_criteria>
The item-7 row is answered with real numbers from a real, fully elapsed hour — pass or fail — and
the consent-path question on the phase's new share events is answered from query rows rather than
left as an inference.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-09-SUMMARY.md` when done. Record in it: the window chosen
and why, each threshold's counted result, whether threshold 3 became readable via
`mcp__vercel__get_web_analytics`, and the RESEARCH A3 outcome.
</output>

