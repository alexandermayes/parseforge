---
phase: 03-share-loop
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03, OPS-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

user_setup:
  - service: vercel
    why: "Preview deploys sit behind Vercel SSO; an automated fetch of a preview URL needs the protection-bypass secret"
    env_vars:
      - name: VERCEL_AUTOMATION_BYPASS_SECRET
        source: "vercel env pull --environment=preview (Protection Bypass for Automation must be enabled on the project)"

must_haves:
  truths:
    - "The full local OPS-01 gate — typecheck, lint, tests, theme parity, token audit, protected elements, SEO invariants — runs green before anything is deployed."
    - "A preview deployment exists and its awards OG URL, player OG URL and report OG URL each return an image."
    - "The preview's analyze page still emits a canonical with no query string."
    - "docs/OPS-01-SHIP-GATE.md gains a Part 5 recording this phase's preview evidence verbatim, with no sign-off claimed."
    - "The Part 5 record states plainly what a preview cannot show — the Google CMP is a production-only env var, so the EEA/UK consent path is not observable here."
    - statement: "The developer has read the full fifteen-rule award pool and confirmed the D-01 tone bar."
      verification: backstop
    - statement: "A real Discord paste of a preview awards link and a preview player link both unfurl as images with the rows and receipts legible, using a cache-busting param so a stale embed cannot be mistaken for a pass."
      verification: backstop
    - statement: "On a phone-width viewport, both the awards Copy link button and the Share my parse button are reachable without scrolling past the analysis tables."
      verification: backstop
  artifacts:
    - docs/OPS-01-SHIP-GATE.md
  key_links:
    - "docs/OPS-01-SHIP-GATE.md Part 5 is the evidence record plan 03-07 signs — the preview half and the production half live in the same part, in that order, as Parts 3 and 4 already do."
  prohibitions:
    - statement: "MUST NOT record a preview observation as a gate sign-off, and MUST NOT soften or omit a check that could not be performed — an unperformed check is recorded as not-performed with the exact test that would close it."
      status: unresolved
      verification: judgment
---

<objective>
**As the** developer who has to answer for what real raiders see, **I want** the whole share loop
running on a real deployment and reviewed before any of it reaches production **so that** the first
Discord unfurl of a card naming real people is one I have already looked at.

Purpose: the preview half of the OPS-01 gate for Phase 3, including the D-04 award-pool review and
the two things only a real browser and a real Discord channel can show.
Output: a green local gate, a preview deployment with captured evidence, and `docs/OPS-01-SHIP-GATE.md`
Part 5 recording it — explicitly unsigned.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share-loop/03-CONTEXT.md
@.planning/phases/03-share-loop/03-RESEARCH.md
@.planning/phases/03-share-loop/03-02-SUMMARY.md
@.planning/phases/03-share-loop/03-03-SUMMARY.md
@.planning/phases/03-share-loop/03-04-SUMMARY.md
@.planning/phases/03-share-loop/03-05-SUMMARY.md

Project rules that bind this plan:
- Every Vercel command needs `--global-config ~/.vercel-personal`; without it the CLI uses the work
  account and fails with a scope error.
- Preview deploys are `vercel --global-config ~/.vercel-personal deploy --scope loot-list-plus
  --yes` — the `--prod` form is NOT run by this plan.
- Preview deployments sit behind Vercel SSO; an automated fetch needs the protection-bypass secret
  sent as a header or as a query param, never printed, and any pulled env file is deleted after use.
- `NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` and `NEXT_PUBLIC_POSTHOG_HOST` are production-only, so the
  EEA/UK consent path cannot be observed on a preview.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full local gate and open Part 5</name>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` `## Part 1` items 1 through 8 — the checklist to run, including the item plan 03-05 added.
    - `docs/OPS-01-SHIP-GATE.md` `## Part 4` — the structure of a phase evidence part, including how Phase 2.1 recorded an item as explicitly open rather than passing.
    - `.planning/phases/03-share-loop/03-02-SUMMARY.md` — the fifteen-row award pool to transcribe for the developer's D-04 review.
  </read_first>
  <action>
Run the Part 1 local gate in one pass and capture every command's output verbatim: `npx tsc
--noEmit`, `npm run lint`, `npm test`, `npm run theme-parity`, `npm run token-audit`, and `npm run
protected-elements`. Then start a local dev server and run `npm run seo-invariants -- --base
http://localhost:<port>` on a free port, capturing its route table, and stop the server.

A non-zero `npm test` exit blocks everything downstream — do not proceed to the preview deploy with
a red suite, and do not record the row as passing with a caveat.

Open a new `## Part 5 — Phase 3 evidence` section in `docs/OPS-01-SHIP-GATE.md`, after Part 4,
following the structure Parts 3 and 4 use. Add these subsections:
- `### Local gate output` — the captured command output, exit codes and summary lines.
- `### SEO invariants` — the route table the script printed, with the port it ran against.
- `### PostHog instrumentation (pre-deploy grep evidence)` — the grep counts proving exactly one
  call site each for `share_action` with each of its four kinds, for `share_landing`, and for the
  two legacy events that keep firing this phase.
- `### Award pool for review (D-04)` — the fifteen rules transcribed verbatim from the 03-02
  summary as a table of id, title, icon, tone, trigger and stat, so the developer reviews the exact
  shipped pool rather than a description of it.

Write nothing about a deploy yet and claim no sign-off. Every edit to this file is additive.
  </action>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` contains a `## Part 5` heading and the four subsections named above.
    - The Part 5 local-gate subsection contains a captured `Test Files` or `Tests` summary line.
    - The award-pool subsection lists 15 rules.
    - Parts 1 through 4 are unchanged — the diff removes no existing lines.
    - `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run theme-parity`, `npm run token-audit` and `npm run protected-elements` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && npx tsc --noEmit && npm run lint && npm test && npm run theme-parity && npm run token-audit && npm run protected-elements && echo local-gate-ok</automated>
    <fails_when>non-zero exit, or the literal local-gate-ok is absent from the output</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; grep -qF '## Part 5' "$F" || { echo "GATE-FAIL: Part 5 was not opened"; exit 1; }; for S in '### Local gate output' '### SEO invariants' '### PostHog instrumentation' '### Award pool for review'; do grep -A9999 '## Part 5' "$F" | grep -qF -- "$S" || { echo "GATE-FAIL: Part 5 is missing $S"; exit 1; }; done; R=$(git diff -U0 -- "$F" | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { echo "GATE-FAIL: this edit must be purely additive but removed $R lines"; exit 1; }; echo part5-open-ok</automated>
    <fails_when>non-zero exit, or the literal part5-open-ok is absent; the printed removed-lines counter names how many existing lines were deleted</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && for PAIR in 'kind: "report_link":1' 'kind: "player_link":1' 'kind: "awards_link":1' 'kind: "discord_text":1' 'posthog.capture("share_landing":1' 'posthog.capture("share_link_copied":1' 'posthog.capture("discord_copied":1'; do S="${PAIR%:*}"; W="${PAIR##*:}"; N=$(grep -rcF -- "$S" app --include='*.tsx' --include='*.ts' 2>/dev/null | awk -F: '{s+=$2} END {print s+0}'); echo "[$S]=$N want=$W"; [ "$N" -eq "$W" ] || { echo "GATE-FAIL: $S has $N call sites across app/, expected $W"; exit 1; }; done; echo instrumentation-grep-ok</automated>
    <fails_when>non-zero exit, or the literal instrumentation-grep-ok is absent; each printed bracketed counter names the event whose call-site count is wrong</fails_when>
  </verify>
  <done>Every local gate item is green with captured evidence, the exact award pool is written down for review, and Part 5 exists with nothing signed.</done>
</task>

<task type="auto">
  <name>Task 2: Preview deploy, captured evidence, and the developer's real-browser review</name>
  <precondition>`VERCEL_AUTOMATION_BYPASS_SECRET` is available for the preview environment (Protection Bypass for Automation is enabled on the `parseforge` project) — without it every automated fetch of a preview URL is redirected to the Vercel SSO endpoint.</precondition>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` `## Part 4` subsections covering the preview deployment, the bypass handling and the "what this preview cannot show" record — the shape to reuse.
    - `.planning/STATE.md` Blockers/Concerns — the SSO bypass mechanics and the production-only env vars.
    - `.planning/phases/03-share-loop/03-RESEARCH.md` Pitfall 1 — the Discord embed cache and the cache-busting technique.
    - `lib/demo-report.ts` — the demo code, fight and source every URL in this task uses.
  </read_first>
  <action>
Deploy a preview with `vercel --global-config ~/.vercel-personal deploy --scope loot-list-plus
--yes` and record the deployment id and URL. Do not pass `--prod` — production is plan 03-07 and
requires the developer's explicit confirmation.

Fetch the bypass secret with `vercel env pull` into a throwaway path outside the repo, use it as a
request header for the fetches below, and delete the file immediately after. Never print the secret
and never write it into the gate document; record only that the bypass was used, the way Part 4
already does.

Capture, against the preview URL, the status and content type of: the awards OG URL for the demo
report and fight; the player OG URL for the demo report, fight and source; the bare report OG URL;
and the demo analyze page's canonical link. Record each verbatim in Part 5 under a
`### Preview deployment` subsection. Then run `npm run protected-elements -- --base
<preview-url>` and record its row table.

Add a `### What this preview cannot show` subsection stating plainly that
`NEXT_PUBLIC_GOOGLE_CMP_PUB_ID` and `NEXT_PUBLIC_POSTHOG_HOST` are production-only, so the EEA/UK
consent path and live PostHog ingestion are not observable here and are item 7's job after the
production deploy.

Then hand the developer the three things only a person can settle, and record each outcome —
performed with what was seen, or not-performed with the exact test that would close it. Never
soften an unperformed check into a pass:
1. **Award pool tone (D-04).** The developer reads the fifteen-row table recorded in task 1 and
   confirms the D-01 bar: praise alongside light jabs, every jab about a measurable fact the row
   proves, nothing insulting about a named real raider, no worst-player headline.
2. **Real Discord unfurl.** Paste a preview awards link and a preview player link into a Discord
   channel and confirm each unfurls as an image with the rows and receipts legible. Append a
   throwaway query param to each URL, and change it on every retry — Discord caches an embed and
   its proxied image by exact URL for an undocumented period, so re-pasting the same link after a
   fix proves nothing. Look specifically at a row with the longest raider names in the log and
   confirm it clips with an ellipsis rather than overflowing, which is the open A2 assumption from
   RESEARCH.
3. **Mobile reachability (D-13).** On a phone-width viewport, confirm the awards Copy link button
   and the Share my parse button are both reachable without scrolling past the analysis tables.

Record all three outcomes in a `### Developer review (preview)` subsection. Part 5 stays unsigned.
  </action>
  <acceptance_criteria>
    - A preview deployment id and URL are recorded in Part 5.
    - Part 5 records a status and content type for each of the three preview OG URLs and the preview canonical.
    - Part 5 contains the row table from `npm run protected-elements` run against the preview URL.
    - Part 5 contains a `### What this preview cannot show` subsection naming the production-only env vars.
    - Part 5 contains a `### Developer review (preview)` subsection with an explicit performed or not-performed outcome for each of the three checks.
    - No Vercel secret value appears anywhere in the repository.
    - Part 5 contains no sign-off line and no date-stamped approval.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; for S in '### Preview deployment' '### What this preview cannot show' '### Developer review (preview)' 'NEXT_PUBLIC_GOOGLE_CMP_PUB_ID' 'protected-elements'; do grep -A9999 '## Part 5' "$F" | grep -qF -- "$S" || { echo "GATE-FAIL: Part 5 is missing $S"; exit 1; }; done; echo preview-record-ok</automated>
    <fails_when>non-zero exit, or the literal preview-record-ok is absent from the output</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && if grep -A9999 '## Part 5' docs/OPS-01-SHIP-GATE.md | grep -qiE '^#+ *Sign-off'; then echo "GATE-FAIL: Part 5 carries a sign-off before the production deploy has happened"; exit 1; fi; echo part5-unsigned-ok</automated>
    <fails_when>non-zero exit, or the literal part5-unsigned-ok is absent from the output</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && if git ls-files -z | xargs -0 grep -lE 'VERCEL_AUTOMATION_BYPASS_SECRET *= *[A-Za-z0-9]' 2>/dev/null | grep -q .; then echo "GATE-FAIL: a bypass secret value was committed"; exit 1; fi; echo no-secret-committed-ok</automated>
    <fails_when>non-zero exit, or the literal no-secret-committed-ok is absent from the output</fails_when>
    <human-check>Confirm all three preview reviews: (1) the fifteen-row award pool clears the D-01 tone bar — fun to be on, never insulting, no worst-player headline; (2) a preview awards link and a preview player link each unfurl in a real Discord channel as an image with the rows and receipts legible, checked with a fresh cache-busting param and with the longest raider names in the log clipping cleanly; (3) on a phone-width viewport both the awards Copy link button and the Share my parse button are reachable without scrolling past the analysis tables. Any check not performed is recorded as not-performed with the exact test that would close it — never softened into a pass.</human-check>
  </verify>
  <done>A real preview deployment serves all three card branches as images with a clean canonical, the protected-elements gate is green against it, and the three human judgements are recorded honestly with Part 5 still unsigned.</done>
</task>

</tasks>

## Artifacts this phase produces

See `03-01-PLAN.md` → "Artifacts this phase produces" for the full phase list. This plan adds no
code symbols. It adds `## Part 5 — Phase 3 evidence` to `docs/OPS-01-SHIP-GATE.md` with the
subsections `### Local gate output`, `### SEO invariants`, `### PostHog instrumentation
(pre-deploy grep evidence)`, `### Award pool for review (D-04)`, `### Preview deployment`,
`### What this preview cannot show` and `### Developer review (preview)`.

## Flagged Assumptions

**All three phase requirements remain `unresolved` from the deterministic edge probe.** The edges
that only a real deployment can exercise are the ones this plan puts in front of a person: the
Discord unfurl (SHARE-01, SHARE-02) and mobile reachability (SHARE-03). They are recorded as
backstop truths rather than asserted by a test, because no headless-browser or viewport harness
exists in this repo.

**RESEARCH A1 (Discord cache).** The embed cache duration is undocumented; the cache-busting param
is the mitigation, not a workaround for a bug.

**RESEARCH A2 (Satori ellipsis).** Closed or left open by the longest-name row check in this plan's
Discord review — record which.

**RESEARCH A3 (consent path on new events).** Not observable on a preview; carried to 03-07's
item-7 live-traffic check.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository to Vercel | a deploy publishes this code to an internet-reachable host |
| preview host to an automated fetcher | the SSO bypass secret authorises unauthenticated access to a non-public deployment |
| gate document to future readers | a recorded observation becomes the evidence a later phase trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-22 | Information Disclosure | the Vercel protection-bypass secret | high | mitigate | Pulled to a throwaway path outside the repo, used as a request header, deleted immediately after, never printed and never written into the gate document; a committed-secret grep is part of this task's verify |
| T-03-23 | Elevation of Privilege | deploying to production from this plan | high | mitigate | This plan runs the preview form only; the production form is a separate plan behind an explicit developer decision checkpoint |
| T-03-24 | Repudiation | a preview observation recorded as a gate pass | high | mitigate | Part 5 carries no sign-off, enforced by a verify command; every unperformed check is recorded as not-performed with the test that would close it |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are added by this phase (RESEARCH `## Package Legitimacy Audit`: zero new dependencies) |
</threat_model>

<verification>
- Every Part 1 local gate item exits 0, captured verbatim.
- A preview deployment exists and serves all three OG card branches as images.
- `npm run protected-elements` is green against the preview URL.
- Part 5 exists, is additive, and carries no sign-off.
- No secret value is committed.
</verification>

<success_criteria>
The whole share loop runs on a real deployment, the developer has seen the exact award pool and a
real Discord unfurl of a card that names real people, and everything observed — including anything
that could not be observed — is written down before production is even proposed.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-06-SUMMARY.md` when done.
</output>
