---
phase: 03-share-loop
plan: 08
type: execute
wave: 6
depends_on: ["03-07"]
files_modified:
  - docs/OPS-01-SHIP-GATE.md
autonomous: false
gap_closure: true
requirements: [SHARE-01, SHARE-02, SHARE-03, OPS-01]

user_setup:
  - service: google-search-console
    why: "Part 5 rows 4, 5 and 6 need a URL-inspection read of property sc-domain:parseforge.gg. No gsd-executor tool can perform it (no Search Console MCP, no browser automation in the executor's toolset)."
    dashboard_config:
      - task: "Either confirm a gscServer MCP tool is connected in the orchestrator session, or open a signed-in Search Console session for property sc-domain:parseforge.gg"
        location: "https://search.google.com/search-console -> URL inspection (property sc-domain:parseforge.gg)"
  - service: discord
    why: "Part 5 row 8 needs a real Discord crawler unfurl of the two production share links; Discord's embed renderer is an external service with no harness in this repo."
    dashboard_config:
      - task: "Open a Discord channel where throwaway links can be posted"
        location: "Discord desktop or mobile app, any channel you can post in"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/OPS-01-SHIP-GATE.md Part 5 carries a dated `### Search Console pass — counted result` subsection, appended after the existing `Sign-off: LEFT OPEN — not signed` record, recording a coverage state, a last-crawl date and a Google-selected canonical for both `https://parseforge.gg/` and `https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ` (D-05: the canonical is param-free, so this is a no-regression check, not a new-surface check)."
    - "That subsection records whether a recrawl of `/` was requested, or the exact reason it could not be, taken from what the inspection tool stated rather than from a guess about why `/` is crawled-but-not-indexed."
    - "That subsection records the permutation check for `?view=awards`, `?ref=awards` and `?ref=parse` on the analyze route (D-16 strips `ref` client-side and D-05 keeps the canonical param-free, so no permutation should be separately indexed)."
    - "Part 5 carries a dated `### Developer review backstops — counted result` subsection recording, per row, the developer's verbatim verdict for the D-04 award-pool tone review, the real Discord unfurl of both production links, and the D-13 phone-width reachability check — each as a PASS with its verbatim wording, or as an explicit dated NOT OBTAINED carrying its reason and its closing test."
    - "Every edit to docs/OPS-01-SHIP-GATE.md is additive: `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0."
    - statement: "Search Console URL inspection under property sc-domain:parseforge.gg reports `/` and `/analyze/ZjKgNYxVcAqR8pGJ` with indexability and metadata unchanged from the Phase 1/2 baseline, and no `?view=awards`, `?ref=awards` or `?ref=parse` permutation of the analyze route appears as a separately indexed URL."
      verification: backstop
    - statement: "A real Discord channel unfurls both production share links as images, with the award rows and the player receipts legible, and the longest raider names clipping with an ellipsis rather than overflowing the row."
      verification: backstop
    - statement: "The developer states a verdict on the full fifteen-rule award pool against the D-01 tone bar — every jab is about a measurable fact, nothing is insulting about a named real raider, and no rule designates a single worst player (D-04)."
      verification: backstop
    - statement: "On a phone-width viewport, both the awards Copy link button and the player Share my parse button are reachable without scrolling past the analysis tables (D-13)."
      verification: backstop
  artifacts:
    - docs/OPS-01-SHIP-GATE.md
  key_links:
    - "docs/OPS-01-SHIP-GATE.md Part 5 is the single record of this phase's gate; rows 4, 5, 6, 7, 8 and 9 of its nine-row closing table (line 2400) are the rows this plan drives to a recorded outcome, and all nine must carry counted evidence before plan 03-10 may sign."
  prohibitions:
    - statement: "MUST NOT borrow, lower, reinterpret or re-date a count, window, threshold or verdict from another deployment, window, session or phase in order to close a row."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT present any row as passed without counted evidence, and MUST NOT fold a no-data, not-obtained or not-performed result into a pass."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT print, copy, move or paste the value of any credential — Vercel tokens, PostHog keys, ~/.config/gsc/* contents, .env* contents. Name a credential path when a task needs one; never read it out."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT change any application code, and MUST NOT run a production deploy; a belief that a successor deployment is needed is a blocking checkpoint:decision, never a self-authorized command."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT fold the 03-REVIEW.md findings WR-01, WR-02, WR-03 or IN-01 into this closure — they belong to /gsd-code-review --fix."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT remove or rewrite an existing line of docs/OPS-01-SHIP-GATE.md; a superseding status is recorded as a new dated subsection appended after line 2429."
      status: unresolved
      verification: judgment
---

<objective>
**As** the owner of a live site whose traffic comes from organic search, **I want to** close the six
Part 5 rows that only a human or an orchestrator-held tool can close, **so that** the OPS-01 Phase 3
gate stops being blocked on evidence nobody ever went and got.

Purpose: `docs/OPS-01-SHIP-GATE.md` Part 5 has nine open rows (closing table at line 2400). Three
are the Search Console pass that was never executed — the one genuinely failed half of ROADMAP
success criterion 4 — and three are developer-only backstops (D-04 tone, real Discord unfurl, D-13
phone-width reachability) recorded NOT PERFORMED three sessions running. This plan drives all six to
a recorded outcome in one developer sitting: a counted PASS, or an explicit dated NOT OBTAINED with
its reason and its closing test. A fourth consecutive bare PENDING is not an acceptable outcome of
this plan.

Output: two new dated subsections appended to Part 5 after line 2429, both purely additive.

**No application code changes in this plan.** No deploy. No package installs.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/personal/parseforge/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share-loop/03-CONTEXT.md
@.planning/phases/03-share-loop/03-VERIFICATION.md
@.planning/phases/03-share-loop/03-07-SUMMARY.md

Project rules that bind this plan:
- `node`/`npm`/`npx` are not on the default PATH. Prepend `$HOME/.local/node20/bin` before any of
  them.
- Deploys are manual Vercel CLI only and need explicit developer confirmation. This plan deploys
  nothing.
- Never print or commit an environment variable or credential value.
- Edits to `docs/OPS-01-SHIP-GATE.md` are additive appends. 03-07 proved why: an in-place rewrite of
  a PENDING placeholder deletes lines and trips the gate document's own additive-only invariant
  (`03-07-SUMMARY.md`, Deviations #2).
</context>

<who_holds_which_tool>
This is the single reason OPS-01 stayed open through 03-06 and 03-07. Plan around it, do not
rediscover it.

- **The gsd-executor running this plan has** `Read, Write, Edit, Bash, Grep, Glob, Skill,
  mcp__context7__*`. It has no Search Console tool, no PostHog tool, no Vercel analytics tool and no
  browser automation. It cannot URL-inspect, cannot open a phone-width viewport, and cannot post to
  Discord.
- **The execute-phase orchestrator session has** `mcp__claude-in-chrome__*` (navigate, read_page,
  get_page_text, find, computer, resize_window), `mcp__posthog__exec`, and
  `mcp__vercel__get_web_analytics` / `get_deployment` / `list_deployments`. A `gscServer` MCP
  (`mcp__gscServer__inspect_url_enhanced`, `get_sitemaps`, `get_search_analytics`) exists in this
  project's history — it ran successfully on 2026-09-06 against `sc-domain:parseforge.gg` (gate doc
  line 355) — but it was not connected in the session that planned this. Whether it is connected at
  execution time is a fact to probe, not to assume in either direction.
- **The developer holds** a signed-in Search Console session, a real Discord channel, and a real
  phone.

The handoff pattern is 03-07's: evidence the executor cannot obtain arrives in the dispatch as a
`<counted_evidence>` block, and the executor transcribes it verbatim into the gate document — never
re-derived, never extrapolated, never borrowed from another window.
</who_holds_which_tool>

<out_of_scope_recorded_deferral>
`03-REVIEW.md` reported three warnings and one info against this phase's code:

- **WR-01** — `computeAwards()` does not fully honor its own never-throws-on-null-shaped-input
  contract.
- **WR-02** — Kill/Wipe boss-percentage formatting disagrees across the three surfaces this phase
  added.
- **WR-03** — `scripts/protected-elements.mjs`'s canonical check can pass while looking at the
  wrong page entirely.
- **IN-01** — `AwardRow.tone` is computed and shipped but never read.

**All four are explicitly deferred out of this gap closure** and belong to
`/gsd-code-review --fix`. Do not fix them here, do not fold a partial fix into a gate-document
edit, and do not let a review finding become the reason a Part 5 row is left open. Record this
deferral in this plan's SUMMARY so the phase verifier sees the decision rather than reading the
absence of a fix as an oversight.
</out_of_scope_recorded_deferral>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Acquire the Search Console read and the three developer backstop verdicts</name>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2313-2342 (`### Search Console pass: **PENDING**`) — the exact three closing tests, written per route, including the rule that a state is recorded only as the inspection tool states it.
    - `docs/OPS-01-SHIP-GATE.md` lines 2343-2377 (`### Developer review backstops — production URLs`) — the three developer tests and the two production URLs, already rewritten off the preview.
    - `docs/OPS-01-SHIP-GATE.md` lines 1835-1861 (`### Award pool for review (D-04)`) — the fifteen-row award table the tone review reads.
    - `docs/OPS-01-SHIP-GATE.md` lines 2400-2429 (`### Part 5 status update … Sign-off: LEFT OPEN — not signed`) — the nine-row closing table this plan closes rows 4-9 of.
    - `docs/OPS-01-SHIP-GATE.md` lines 355-364 (`### Search Console (post-deploy, 2026-09-06, MCP gscServer, property sc-domain:parseforge.gg)`) — the one Part where this pass was actually executed; reproduce its evidence shape (per-route verdict, `last_crawled` date, `no-data` where Google has never crawled).
    - `.planning/phases/03-share-loop/03-CONTEXT.md` D-01, D-04, D-05, D-13, D-16 — the tone bar, the human-review requirement, the param-free canonical, the mobile-placement rule, and the `ref` strip.
  </read_first>
  <action>
Six rows of Part 5 need evidence this executor cannot obtain. Stop here and hand the orchestrator
the exact six tests below. Do not attempt them with the executor's own tools, do not approximate any
of them from source reading, and do not record any of them as unavailable-this-session — that
outcome has already happened three times and is what this plan exists to end.
  </action>
  <instructions>
**Already done by the executor:** nothing automatable remains. All six items require either a
Google-authenticated session or a human's eyes.

**Rows 4, 5 and 6 — the Search Console pass.** Property `sc-domain:parseforge.gg`. Follow this
ladder in order and record which rung produced the read:

1. Probe whether a `gscServer` MCP tool is connected in the orchestrator session. If it is, run
   `mcp__gscServer__inspect_url_enhanced` for `https://parseforge.gg/` and for
   `https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ`, and use the Pages report or the
   search-analytics read for the permutation check.
2. If it is not connected, and the developer agrees, drive
   `https://search.google.com/search-console` through `mcp__claude-in-chrome__*` in the developer's
   already-signed-in session and read the URL Inspection results off the page.
3. If neither is possible, the developer runs the three inspections and pastes the results verbatim.
4. If none of 1-3 happens, the outcome is `NOT OBTAINED (<UTC date>)` with the reason and the named
   next action.

What to bring back, per URL: coverage state exactly as stated, last crawl date, Google-selected
canonical, the indexability verdict, and whether the page title/description differ from the Phase
1/2 baseline. For `https://parseforge.gg/` additionally: either that a recrawl was requested through
the URL Inspection tool's Request indexing action, or the specific reason it could not be (for
example a spent daily quota). The baseline to compare against, from `.planning/STATE.md`: `/` reads
"Crawled – currently not indexed" with a crawl date of 2026-09-05, predating even the Phase 2
deploy. Do not diagnose why — record what the tool says.

For the permutation check, confirm whether any of these appears as a separately indexed URL:
`https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ?view=awards`, the same route with `?ref=awards`, and
the same route with `?ref=parse`. The expected result is none, because the route's canonical is
always the param-free form (D-05) and `ref` is stripped from the address bar on landing (D-16) — but
expected is not observed, and the observation is the row.

**Row 7 — D-04 award-pool tone review.** Read the fifteen-row table at gate doc lines 1835-1861 and
state a verdict against the D-01 bar: every jab is about a measurable fact, nothing is insulting
about a named real raider, and no rule designates a single worst player. A verdict is either "the
pool as shipped clears the bar" or the specific row that does not.

**Row 8 — real Discord unfurl.** Paste both production URLs into a real Discord channel and confirm
each unfurls as an image with the award rows and the player receipts legible, checking specifically
whether the longest raider names clip with an ellipsis rather than overflowing the row (RESEARCH
Assumption A2 / Pitfall 2). Change the `v=` value on every retry — Discord caches an embed and its
proxied image by exact URL (RESEARCH Pitfall 1); the value already recorded in the gate doc,
`v=1789550356`, is burned, so use a fresh integer such as the current Unix epoch seconds. Awards
link: `https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ?fight=23&view=awards&ref=awards&v=<fresh>`.
Player link: `https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ?fight=23&source=12&ref=parse&v=<fresh>`.
Production carries no Deployment Protection, so no bypass header is involved.

**Row 9 — D-13 phone-width reachability.** Open
`https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ?fight=23&source=12` at phone width, switch to the
Raid tab, and confirm both the awards Copy link button and the player Share my parse button are
reachable without scrolling past the analysis tables. Either a real device or
`mcp__claude-in-chrome__resize_window` against the production URL satisfies this — record which
route was used, since a resized desktop browser and a real handset are different evidence and the
record should say which one this is.

Never substitute one row's evidence for another's, and never mark a row done because the code looks
right. The phase verifier already confirmed independently that the code-side contracts are correct;
what is missing is the external observation.
  </instructions>
  <verification>
After resume, the executor checks that the `<counted_evidence>` block contains one entry per row for
rows 4, 5, 6, 7, 8 and 9, each carrying either a counted/verbatim result or an explicit
`NOT OBTAINED` with a reason and a closing test. A block missing a row is an unmet precondition for
Task 2, not a licence to invent the row.
  </verification>
  <acceptance_criteria>
    - The orchestrator's resume message contains a `<counted_evidence>` block with a separately labelled entry for each of rows 4, 5, 6, 7, 8 and 9.
    - Each Search Console entry names which ladder rung produced it (gscServer MCP, browser-driven session, developer paste, or not obtained).
    - Each entry is either a counted/verbatim result or the literal `NOT OBTAINED` plus a reason and a named closing test.
    - No credential value appears anywhere in the returned evidence.
  </acceptance_criteria>
  <resume-signal>Reply with a `<counted_evidence>` block covering rows 4, 5, 6, 7, 8 and 9. Task 2 transcribes it verbatim and runs no query of its own.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Record the homepage Search Console row end-to-end — one row, from supplied evidence to a verified gate-document record</name>
  <precondition>Task 1's `<counted_evidence>` block is present in this task's dispatch and contains a `https://parseforge.gg/` entry carrying either a coverage state and a last-crawl date, or an explicit `NOT OBTAINED` with its reason.</precondition>
  <reversibility rating="reversible">An additive dated subsection can be corrected by a further additive dated subsection; nothing is overwritten.</reversibility>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` lines 2400-2429 — the end of the file and the nine-row closing table; every append in this plan goes after line 2429.
    - `docs/OPS-01-SHIP-GATE.md` lines 355-364 — the evidence shape to reproduce.
    - `docs/OPS-01-SHIP-GATE.md` lines 2205-2278 (`### Item 7 — counted result`) — how 03-07 recorded a counted result that arrived from outside the executor, including the verbatim-transcription note in its Source paragraph. Match that discipline.
    - `.planning/phases/03-share-loop/03-07-SUMMARY.md` Deviations #2 — why this must be an append rather than an edit of the existing section.
  </read_first>
  <action>
This is the thin end-to-end slice for the whole closure: one open Part 5 row taken all the way from
supplied evidence to a verified record. Get this one right and Task 3 is expansion along the same
proven path.

Append to the end of `docs/OPS-01-SHIP-GATE.md` (after line 2429, inside Part 5, no existing line
touched) a new subsection headed exactly
`### Search Console pass — counted result (<YYYY-MM-DD>, gap closure 03-08)` using today's UTC date.
Open it with a Source paragraph naming the property `sc-domain:parseforge.gg`, which ladder rung
produced the read, who ran it, and the UTC timestamp — then state that every figure below is
transcribed verbatim from that read and is neither re-derived nor estimated.

Then record the homepage row — Part 5 closing-table row 4 — as a bullet group under the literal
label `- **URL:** https://parseforge.gg/` with these five sub-bullets, each using the exact field
label so the record is greppable:

- `  - Coverage state:` the state exactly as the inspection tool worded it.
- `  - Last crawl:` the crawl date the tool reported, or `no-data` if Google has never crawled it.
- `  - Google-selected canonical:` the canonical Google reports it selected.
- `  - Metadata:` unchanged from the Phase 1/2 baseline, or the specific difference found.
- `  - Recrawl requested:` yes with the UTC timestamp, or no with the specific reason the tool gave.

Close the row with a single line of the form `Row 4 verdict: PASS — <why>` or
`Row 4 verdict: NOT OBTAINED (<YYYY-MM-DD>) — reason: <reason> — closing test: <test>`. State the
comparison against the recorded baseline explicitly: the homepage previously read "Crawled –
currently not indexed" with a 2026-09-05 crawl date, so a same-state reading is a no-regression PASS
for a phase that adds no indexable route (D-05), while a state that got worse is a Phase 3 defect
and must be recorded as one.

If the homepage evidence came back not obtained, write that verdict form instead — do not leave the
row absent, and do not write a state the tool did not report.
  </action>
  <acceptance_criteria>
    - `docs/OPS-01-SHIP-GATE.md` contains a heading matching `^### Search Console pass — counted result \(20`.
    - That subsection contains the literal `https://parseforge.gg/` and all five field labels: `Coverage state:`, `Last crawl:`, `Google-selected canonical:`, `Metadata:`, `Recrawl requested:`.
    - That subsection names the property `sc-domain:parseforge.gg` and the ladder rung that produced the read.
    - The subsection contains a `Row 4 verdict:` line reading either `PASS` or `NOT OBTAINED (` with a reason and a closing test.
    - `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
    - Every figure in the subsection traces to the Task 1 `<counted_evidence>` block; none was produced by this executor's own inference.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; S=$(sed -n '/^### Search Console pass — counted result (/,$p' "$F"); [ -n "$S" ] || { echo "GATE-FAIL: no dated Search Console counted-result subsection was appended"; exit 1; }; for T in 'sc-domain:parseforge.gg' 'https://parseforge.gg/' 'Coverage state:' 'Last crawl:' 'Google-selected canonical:' 'Metadata:' 'Recrawl requested:' 'Row 4 verdict:'; do printf '%s\n' "$S" | grep -qF -- "$T" || { echo "GATE-FAIL: the counted-result subsection is missing the field $T"; exit 1; }; done; echo gsc-home-row-ok</automated>
    <fails_when>non-zero exit, or the literal gsc-home-row-ok absent from the output; the printed GATE-FAIL line names either the absent subsection or the missing field label</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -E '^-[^-]' | head -20; echo "GATE-FAIL: the Part 5 close must be purely additive"; exit 1; }; echo part5-additive-ok</automated>
    <fails_when>non-zero exit, or the literal part5-additive-ok absent from the output; the printed removed-lines counter and the listed removed lines name exactly what was deleted</fails_when>
  </verify>
  <done>Part 5 carries a dated Search Console counted-result subsection whose homepage row states a coverage state, a crawl date, a Google-selected canonical, a metadata comparison and a recrawl disposition, and the gate document lost no lines.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the record — the analyze route, the permutation check, and the three developer backstops</name>
  <precondition>Task 2 has committed its subsection, and Task 1's `<counted_evidence>` block carries entries for rows 5, 6, 7, 8 and 9.</precondition>
  <reversibility rating="reversible">Additive dated subsections; a later dated append can correct any entry without rewriting it.</reversibility>
  <files>docs/OPS-01-SHIP-GATE.md</files>
  <read_first>
    - `docs/OPS-01-SHIP-GATE.md` — the `### Search Console pass — counted result` subsection Task 2 just appended; Task 3 extends that same subsection rather than starting a new one for the Search Console rows.
    - `docs/OPS-01-SHIP-GATE.md` lines 2343-2377 (`### Developer review backstops — production URLs`) — the three exact tests and the two production URLs whose outcomes Task 3 records.
    - `docs/OPS-01-SHIP-GATE.md` lines 1835-1861 (`### Award pool for review (D-04)`) — the table the D-04 verdict refers to, so the recorded verdict can name a row by its title.
    - `.planning/phases/03-share-loop/03-VERIFICATION.md` `behavior_unverified_items` and `human_verification` — the three backstop truths in the exact wording the phase verifier will re-read.
  </read_first>
  <action>
Extend the `### Search Console pass — counted result` subsection Task 2 created with two more bullet
groups, using the same five field labels so the whole subsection stays uniformly greppable:

- `- **URL:** https://parseforge.gg/analyze/ZjKgNYxVcAqR8pGJ` — closing-table row 5. Same five
  sub-bullets. Finish with `Row 5 verdict: PASS — <why>` or the `NOT OBTAINED (` form. The route was
  confirmed untouched by this phase's `git log`, so the bar is indexability and metadata unchanged
  from the Phase 1/2 baseline.
- `- **Permutation check:**` — closing-table row 6. One sub-bullet per permutation, naming it
  literally: `?view=awards`, `?ref=awards`, `?ref=parse`, each recording whether it appears as a
  separately indexed URL and where that was read (Pages report, site: query, or search-analytics).
  Finish with `Row 6 verdict: PASS — <why>` or the `NOT OBTAINED (` form. Say explicitly which
  source was consulted; "none found" from an unconsulted report is not an observation.

Then append a second new subsection headed exactly
`### Developer review backstops — counted result (<YYYY-MM-DD>, gap closure 03-08)` with three
labelled entries, each carrying the developer's wording verbatim rather than a paraphrase:

- `- **Row 7 — D-04 award-pool tone review:**` the verdict as stated. If a row failed the D-01 bar,
  name that award title and record it as a FAIL with the named row — do not soften it, and do not
  edit the pool here (a pool edit is application work and belongs to a separate plan).
- `- **Row 8 — real Discord unfurl:**` per link, whether it unfurled as an image, whether the award
  rows and player receipts were legible, and whether the longest raider names clipped with an
  ellipsis or overflowed. Record the exact two URLs pasted including the fresh `v=` value used, so
  the evidence is reproducible. An overflow finding is a real FAIL against RESEARCH Assumption A2,
  recorded as such with its closing test.
- `- **Row 9 — D-13 phone-width reachability:**` whether both buttons were reachable without
  scrolling past the analysis tables, plus which route produced the observation (real handset, or
  `mcp__claude-in-chrome__resize_window` at a named viewport width) and the URL used.

Every entry ends in exactly one of three states: `PASS` with its evidence, `FAIL` with what was
seen and its closing test, or `NOT OBTAINED (<YYYY-MM-DD>)` with its reason and its closing test.
Close the subsection with a one-line roll-up naming which of rows 4 through 9 are now counted and
which are not, so plan 03-10's close-out can read the state off one line.

Do not touch application code. Do not modify `.planning/REQUIREMENTS.md` or `.planning/WINDOWS.md`
in this plan — both belong to 03-10, which owns the ledger propagation once all nine rows are
counted.
  </action>
  <acceptance_criteria>
    - The `### Search Console pass — counted result` subsection contains the literals `/analyze/ZjKgNYxVcAqR8pGJ`, `?view=awards`, `?ref=awards` and `?ref=parse`.
    - That subsection contains a `Row 5 verdict:` line and a `Row 6 verdict:` line, each reading `PASS`, `FAIL` or `NOT OBTAINED (`.
    - `docs/OPS-01-SHIP-GATE.md` contains a heading matching `^### Developer review backstops — counted result \(20`.
    - That subsection contains the literals `Row 7 — D-04`, `Row 8 — real Discord unfurl` and `Row 9 — D-13`, each followed by the developer's verbatim wording.
    - The Row 8 entry records the exact two production URLs pasted, including the fresh `v=` value.
    - The Row 9 entry names whether the observation came from a real handset or a resized browser, at a named viewport width.
    - `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
    - `git status --porcelain` shows no change to any file outside `docs/OPS-01-SHIP-GATE.md` and this phase's planning artifacts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && F=docs/OPS-01-SHIP-GATE.md; S=$(sed -n '/^### Search Console pass — counted result (/,$p' "$F"); for T in '/analyze/ZjKgNYxVcAqR8pGJ' '?view=awards' '?ref=awards' '?ref=parse' 'Row 5 verdict:' 'Row 6 verdict:'; do printf '%s\n' "$S" | grep -qF -- "$T" || { echo "GATE-FAIL: the Search Console counted-result subsection is missing $T"; exit 1; }; done; B=$(sed -n '/^### Developer review backstops — counted result (/,$p' "$F"); [ -n "$B" ] || { echo "GATE-FAIL: no dated developer-backstop counted-result subsection was appended"; exit 1; }; for T in 'Row 7 — D-04' 'Row 8 — real Discord unfurl' 'Row 9 — D-13'; do printf '%s\n' "$B" | grep -qF -- "$T" || { echo "GATE-FAIL: the developer-backstop subsection is missing $T"; exit 1; }; done; echo gsc-and-backstops-ok</automated>
    <fails_when>non-zero exit, or the literal gsc-and-backstops-ok absent from the output; the printed GATE-FAIL line names the missing route, permutation, verdict line or backstop row</fails_when>
    <automated>cd /Users/alexander.mayes/Code/personal/parseforge && R=$(git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'); echo "removed-lines=$R"; [ "$R" -eq 0 ] || { git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -E '^-[^-]' | head -20; echo "GATE-FAIL: this expansion must be purely additive"; exit 1; }; echo expansion-additive-ok</automated>
    <fails_when>non-zero exit, or the literal expansion-additive-ok absent from the output; the printed removed-lines counter names how many existing lines were deleted</fails_when>
    <automated>export PATH="$HOME/.local/node20/bin:$PATH"; cd /Users/alexander.mayes/Code/personal/parseforge && { [ -d node_modules ] || npm ci; } && npx tsc --noEmit && npx vitest run && echo local-gate-ok</automated>
    <fails_when>non-zero exit, or the literal local-gate-ok absent from the output — this plan changes no code, so a red typecheck or a failing test means something outside its scope was touched</fails_when>
  </verify>
  <done>All six rows this plan owns — the two Search Console URL inspections, the permutation check, and the three developer backstops — carry a dated recorded outcome in Part 5, each a PASS with evidence, a FAIL with what was seen, or an explicit NOT OBTAINED with its reason and closing test.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan adds no code symbols. It appends two subsections to `docs/OPS-01-SHIP-GATE.md` Part 5,
after line 2429:

| Artifact | Kind | Where |
|---|---|---|
| `### Search Console pass — counted result (<date>, gap closure 03-08)` | new dated subsection (rows 4, 5, 6) | `docs/OPS-01-SHIP-GATE.md`, appended after line 2429 |
| `### Developer review backstops — counted result (<date>, gap closure 03-08)` | new dated subsection (rows 7, 8, 9) | `docs/OPS-01-SHIP-GATE.md`, appended after the above |

Not produced here and owned by later plans: `### Item 7 — re-measured counted result` (03-09),
`### Part 5 close-out` and the sign-off heading (03-10), `### OPS-01 Addendum 4` in
`.planning/REQUIREMENTS.md` (03-10), the `.planning/WINDOWS.md` status changes (03-10). No helper
script is created — none is needed, and adding one would be new untested code in a documentation
closure. The API-coverage detector was run over this phase's scope and returned `detected: false`,
so no `COVERAGE.md` is produced.

## Flagged Assumptions

**All three phase requirements came back `unclassified` / `unresolved` from the deterministic edge
probe, and none is auto-resolved here.** Each is surfaced explicitly:

- **SHARE-01 (unresolved).** The awards card's capability is proven live, but its two human-only
  truths — the D-04 tone bar and a real Discord unfurl of the awards link — are rows 7 and 8 of
  this plan. If either comes back NOT OBTAINED, SHARE-01's backstop stays open and 03-10 may not
  present it as met.
- **SHARE-02 (unresolved).** The per-player permalink's OG image is proven live and visually
  confirmed by the phase verifier; its open truth is the same Discord unfurl (row 8, player link).
- **SHARE-03 (unresolved).** The protected-elements half is fully verified; the open truth is D-13
  phone-width reachability (row 9).

**Search Console is an external service with no harness in this repo.** Its three rows are
`verification: backstop` truths: a verifier that cannot see explicit recorded evidence must abstain
rather than infer a pass from the code-side canonical assertions, which prove something adjacent but
different.

**A resized desktop browser is not a handset.** If row 9 is satisfied via
`mcp__claude-in-chrome__resize_window`, the record must say so and name the viewport width, so a
later reader can judge how strong that evidence is rather than assuming a real device.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| externally-sourced readings to the gate record | Search Console states, Discord render outcomes and phone-width observations are transcribed into a document that gates a milestone |
| gate document to future phases | a row recorded closed becomes the evidence Phases 4 through 7 build on |
| developer session to this repo | a signed-in Search Console session and a Discord channel are reached through the developer's own credentials, which must never enter the repo or the transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-29 | Repudiation | a Part 5 row recorded as passed without the external observation | high | mitigate | Every row must end in PASS-with-evidence, FAIL-with-what-was-seen, or NOT OBTAINED-with-reason-and-closing-test; the `must_haves` prohibitions forbid folding a not-obtained result into a pass, and the verify commands assert each verdict line exists |
| T-03-30 | Tampering | the committed gate document's existing evidence | high | mitigate | Both tasks append only; the proven additive-only check (`git diff -U0 \| grep -cE '^-[^-]'` equals 0) runs in both tasks' verify blocks |
| T-03-31 | Information Disclosure | the developer's Google/Discord session and any credential path | high | mitigate | No task reads a credential; `~/.config/gsc/*`, Vercel tokens and `.env*` are named but never opened, and the acceptance criteria require the returned evidence to contain no credential value |
| T-03-32 | Spoofing | a permutation "none found" claimed from a report nobody opened | medium | mitigate | Row 6 must name which source was consulted (Pages report, site: query, or search-analytics); an unsourced "none found" fails the row's acceptance criteria |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new dependencies (`03-RESEARCH.md` `## Package Legitimacy Audit`: "No external packages are introduced by this phase"); this plan modifies one documentation file, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- Part 5 carries a dated `### Search Console pass — counted result` subsection covering both
  inspected URLs and all three permutations, each with its verdict line.
- Part 5 carries a dated `### Developer review backstops — counted result` subsection covering
  rows 7, 8 and 9 with the developer's verbatim wording.
- `git diff -U0 -- docs/OPS-01-SHIP-GATE.md | grep -cE '^-[^-]'` prints 0.
- `npx tsc --noEmit` and `npx vitest run` are green — this plan changed no code.
- No row reads only as unavailable-this-session; every one has a dated disposition.
</verification>

<success_criteria>
Six of the nine open Part 5 rows have moved from "nobody went and looked" to a dated, sourced,
recorded outcome — and any row that still could not be obtained says so in writing, with the exact
test that closes it and the reason it did not happen this time.
</success_criteria>

<output>
Create `.planning/phases/03-share-loop/03-08-SUMMARY.md` when done. Record in it: which ladder rung
produced the Search Console read, which rows came back PASS / FAIL / NOT OBTAINED, and the explicit
deferral of `03-REVIEW.md`'s WR-01, WR-02, WR-03 and IN-01 to `/gsd-code-review --fix`.
</output>
