# token-audit report

This report is generated, never hand-written, by `scripts/token-audit.mjs` — it walks `app/`, `components/` and `lib/` for hardcoded Tailwind palette classes (`palette-class`) and six-digit hex colour literals (`raw-hex`), checks each against a reasoned allowlist (below), and inventories `app/globals.css`'s `@theme` categories. Regenerate it with `npm run token-audit -- --markdown docs/TOKEN-AUDIT.md` — never edit this file by hand, or it will silently drift from the codebase it describes.

## Findings

app/components/LandingHero.tsx:52: raw-hex: #D4A843 [ALLOWLISTED: Particles renders to a <canvas> 2D context and parses this prop via hexToRgb() for per-frame alpha compositing — canvas fillStyle cannot resolve a CSS custom property (var()), so this one call site must pass a literal hex value. See the vendored-effect-component-defaults entry for the same constraint inside particles.tsx itself.]
app/manifest.ts:11: raw-hex: #0c0c0f [ALLOWLISTED: The Web App Manifest spec's background_color/theme_color fields are read by the browser directly from the manifest JSON before any stylesheet loads — there is no CSS engine or custom-property cascade available at manifest-parse time.]
app/manifest.ts:12: raw-hex: #D4A843 [ALLOWLISTED: The Web App Manifest spec's background_color/theme_color fields are read by the browser directly from the manifest JSON before any stylesheet loads — there is no CSS engine or custom-property cascade available at manifest-parse time.]
app/og/route.tsx:11: raw-hex: #0c0c0f [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:12: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:13: raw-hex: #a1a1aa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:14: raw-hex: #52525b [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:20: raw-hex: #fbbf24 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:21: raw-hex: #a78bfa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:22: raw-hex: #60a5fa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:23: raw-hex: #4ade80 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:24: raw-hex: #9ca3af [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:64: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:64: raw-hex: #F59E0B [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:64: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:75: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:75: raw-hex: #F59E0B [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:94: raw-hex: #FFFFFF [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:115: raw-hex: #fafafa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/og/route.tsx:157: raw-hex: #fafafa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.]
app/opengraph-image.tsx:19: raw-hex: #0c0c0f [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:31: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:31: raw-hex: #F59E0B [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:31: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:62: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:62: raw-hex: #F59E0B [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:75: raw-hex: #a1a1aa [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:100: raw-hex: #D4A843 [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
app/opengraph-image.tsx:118: raw-hex: #52525b [ALLOWLISTED: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.]
components/ui/border-beam.tsx:59: raw-hex: #ffaa40 [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/border-beam.tsx:60: raw-hex: #9c40ff [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/magic-card.tsx:27: raw-hex: #262626 [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/magic-card.tsx:29: raw-hex: #9E7AFF [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/magic-card.tsx:30: raw-hex: #FE8BBB [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/particles.tsx:87: raw-hex: #ffffff [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/shimmer-button.tsx:21: raw-hex: #ffffff [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/shine-border.tsx:20: raw-hex: #000000 [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/shine-border.tsx:33: raw-hex: #000000 [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/sparkles-text.tsx:76: raw-hex: #9E7AFF [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/sparkles-text.tsx:76: raw-hex: #FE8BBB [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/sparkles-text.tsx:89: raw-hex: #9E7AFF [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
components/ui/sparkles-text.tsx:89: raw-hex: #FE8BBB [ALLOWLISTED: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.]
lib/constants.ts:27: raw-hex: #C79C6E [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:28: raw-hex: #F58CBA [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:29: raw-hex: #ABD473 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:30: raw-hex: #FFF569 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:31: raw-hex: #FFFFFF [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:32: raw-hex: #0070DE [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:33: raw-hex: #69CCF0 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:34: raw-hex: #9482C9 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:35: raw-hex: #FF7D0A [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:36: raw-hex: #C41E3A [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:37: raw-hex: #00FF96 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:252: raw-hex: #60A5FA [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:253: raw-hex: #4ADE80 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:254: raw-hex: #C084FC [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]
lib/constants.ts:255: raw-hex: #FBBF24 [ALLOWLISTED: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.]

## Allowlist

- app/og/route.tsx (whole file) — reason: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property; this exception is already documented in the file.
- lib/constants.ts declarations ending in _HEX (CLASS_COLORS_HEX, ROLE_COLORS_HEX) — reason: Satori-only raw-hex mirrors of the token-backed maps, introduced so app/og/route.tsx (which cannot resolve custom properties) has a value to read — same reason as the og-route-whole-file exception.
- app/opengraph-image.tsx (whole file) — reason: Satori (next/og's ImageResponse) is a static image renderer with no CSS engine and cannot resolve a CSS custom property — same reason as the og-route-whole-file exception.
- app/manifest.ts (whole file) — reason: The Web App Manifest spec's background_color/theme_color fields are read by the browser directly from the manifest JSON before any stylesheet loads — there is no CSS engine or custom-property cascade available at manifest-parse time.
- components/ui/{border-beam,magic-card,particles,shimmer-button,shine-border,sparkles-text}.tsx (default prop values) — reason: Vendored magicui-style effect components; their hex-valued default parameters are unreachable fallback values — every call site in this codebase supplies an explicit color/gradient override (see app/components/LandingHero.tsx, DpsComparison.tsx, ReportUrlForm.tsx, ComparisonSummary.tsx, and AnalyzeClient.tsx). Left as shipped, matching the meteors.tsx precedent (CLAUDE.md) of not touching vendored effect-component internals beyond what a call site needs. Particles additionally requires a literal hex string for its own hexToRgb() canvas-color parsing — a canvas 2D context fillStyle cannot resolve a CSS custom property.
- app/components/LandingHero.tsx (Particles color prop) — reason: Particles renders to a <canvas> 2D context and parses this prop via hexToRgb() for per-frame alpha compositing — canvas fillStyle cannot resolve a CSS custom property (var()), so this one call site must pass a literal hex value. See the vendored-effect-component-defaults entry for the same constraint inside particles.tsx itself.

## @theme category inventory

- colour (required): present
- radius: present
- font: present
- spacing (required): present
- motion (required): present
- elevation (required): present

## Summary

- Total findings: 57
- Allowlisted: 57
- Non-allowlisted (gate-relevant): 0
- Palette-class findings: 0
- Raw-hex findings: 57
- Missing required @theme categories: none
